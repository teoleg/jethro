{
  "app": "./gradlew -q run",
  "watch": {
    "include": ["src/main/**"],
    "exclude": ["build", ".gradle"]
  },
  "context": {
    "@aws-cdk/core:checkSecretUsage": true,
    "@aws-cdk/aws-iam:minimizePolicies": true,
    "aws-cdk:enableDiffNoFail": true,

    "//": "Deployment inputs — override with `cdk deploy -c key=value` or edit here.",
    "githubRepo": "teoleg/jethro",
    "githubBranch": "claude/new-session-smb8v6",
    "//createOidcProvider": "set FALSE on a shared account where the GitHub OIDC provider already exists",
    "createOidcProvider": true,
    "//alertEmail": "required for the budget alarm — pass -c alertEmail=you@example.com",
    "alertEmail": "",
    "monthlyBudgetUsd": "100",
    "//instanceType": "t3a.xlarge = 4 vCPU/16GB AMD x86, cheapest at this size; m6i.xlarge for non-burstable",
    "instanceType": "t3a.xlarge"
  }
}
