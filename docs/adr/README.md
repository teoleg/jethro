# Architecture Decision Records

Index of ADRs for the Jethro trading platform. See [template.md](template.md) for the format.

| # | Title | Status | Implementation |
|---|-------|--------|----------------|
| [0001](0001-record-architecture-decisions.md) | Record architecture decisions | Accepted | ✅ Implemented |
| [0002](0002-backend-language-java-with-cpp-hot-paths.md) | Backend in Java 21, C++ reserved for latency-critical hot paths | Accepted | ✅ Implemented |
| [0003](0003-event-driven-service-architecture.md) | Event-driven services around a streaming backbone | Accepted | ✅ Implemented |
| [0004](0004-messaging-backbone-kafka.md) | Kafka (Amazon MSK) as the messaging backbone | Superseded by 0012 | ✖ Superseded |
| [0005](0005-data-storage.md) | Aurora PostgreSQL for state, Kafka + S3 for tick history | Accepted | ◐ Partial |
| [0006](0006-ui-stack.md) | UI in TypeScript + React (Vite), streaming over WebSocket | Superseded by 0028 | ✖ Superseded |
| [0007](0007-aws-runtime-ecs-fargate-cdk.md) | Runtime on ECS Fargate, infrastructure as code with AWS CDK (Java) | Accepted | ◐ Partial |
| [0008](0008-domain-model-books-instruments-positions.md) | Domain model: books, instruments, positions, marks | Accepted | ✅ Implemented |
| [0009](0009-market-data-provider-abstraction.md) | Market data provider abstraction | Accepted | ✅ Implemented |
| [0010](0010-ai-algo-engine-embedded-vs-external-model.md) | AI-driven algo engine — model inference SPI, external API first, embedded behind triggers | Accepted | ◐ Partial |
| [0011](0011-finops-cost-monitoring-in-platform.md) | FinOps in the platform — finops-service, tagged AWS costs, Costs tile in the UI | Accepted | ○ Not started |
| [0012](0012-redpanda-backbone-idempotent-consumers.md) | Redpanda as the Kafka-API backbone; at-least-once + idempotent consumers | Accepted | ✅ Implemented |
| [0013](0013-dev-cost-posture-single-node-aws.md) | Dev cost posture — one EC2 node runs the compose stack; managed services behind triggers | Accepted | ✅ Implemented |
| [0014](0014-trading-core-in-process-market-path.md) | Fuse the market path into trading-core — in-process ticks, durable log for transactions only | Accepted | ◐ Partial |
| [0015](0015-single-jvm-modular-monolith.md) | Single-JVM modular monolith — one app now, extraction seams preserved | Accepted | ✅ Implemented |
| [0016](0016-local-slm-tier-agentic-elements.md) | Local SLM inference tier — Ollama adapter first, agentic elements pulled forward | Accepted | ✅ Implemented |
| [0017](0017-attention-first-ui.md) | Attention-first UI — agents curate with a deterministic floor; grids become drill-down | Accepted | ✅ Implemented |
| [0018](0018-ai-trade-suggestions-human-in-loop.md) | AI trade suggestions — frontier tier proposes, deterministic guardrails gate, human executes | Accepted | ✅ Implemented |
| [0019](0019-simulated-auto-execution.md) | Simulated auto-execution — deterministic strategy may auto-trade in sim, hard-gated off real brokers | Accepted | ✅ Implemented |
| [0020](0020-enriched-risk-model.md) | Multi-asset quant foundation — OpenGamma Strata as the analytics substrate, exact money ledger on top | Accepted | ✅ Implemented |
| [0021](0021-operational-chat.md) | Operational chat — SLM parses the question, deterministic code answers, every turn audited to Postgres | Accepted | ✅ Implemented |
| [0022](0022-llm-hypothesis-layer-bounded-autonomy.md) | LLM hypothesis layer — model synthesises structured theses, quant layer computes, a deterministic risk envelope decides auto-execute vs human approval | Accepted | ✅ Implemented |
| [0023](0023-yahoo-market-data-adapter.md) | Yahoo Finance market-data adapter — a free, delayed, dev/demo-only provider behind the ADR-0009 port | Accepted | ✅ Implemented |
| [0024](0024-finnhub-realtime-market-data.md) | Finnhub real-time market data — free WebSocket equities feed, composed with Yahoo/sim for the rest | Accepted | ✅ Implemented |
| [0025](0025-realistic-execution-cost-model.md) | Realistic simulated execution — spread/fee cost model, working-order matching, cancel/TIF | Proposed | ✅ Implemented |
| [0026](0026-correlated-factor-market-simulator.md) | High-fidelity market simulator — correlated cross-asset factor model, regime switching, calibrated from real history | Proposed | ✅ Implemented |
| [0027](0027-evaluation-and-risk-rigor.md) | Evaluation & risk rigor — hypothesis outcome scoring, out-of-sample backtests, VaR + firm breaker, day boundary | Proposed | ✅ Implemented |
| [0028](0028-ui-static-pages-react-deferred.md) | UI stays server-served static pages; React deferred behind concrete triggers | Accepted | ✅ Implemented |
| [0029](0029-runtime-feed-switching.md) | Runtime feed switching and hard sim/live/replay data separation | Accepted | ✅ Implemented |
| [0030](0030-schema-registry-serde.md) | Schema-registry serde — writer-schema resolution on the wire | Accepted | ✅ Implemented |
| [0031](0031-sim-control-panel.md) | Sim control panel — live UI dials over the simulator for model testing | Accepted | ✅ Implemented |
| [0032](0032-empirical-sim-volume-liquidity.md) | History-anchored market simulation (sim price engine) | Accepted | ✅ Implemented |
| [0033](0033-flow-aware-algos-and-depth.md) | Flow-aware algorithms (volume confirmation + liquidity sizing) and synthesized depth | Accepted | ✅ Implemented |
| [0034](0034-news-driven-sim-shocks.md) | News-driven sim shocks — news moves the tape (correlated volume surge + bp momentum) | Accepted | ✅ Implemented |
| [0035](0035-rag-for-ai-layer.md) | Retrieval-augmented context for the AI layer — local embeddings + pgvector | Accepted | ◐ Partial |
| [0036](0036-ai-signal-balance.md) | AI signal balance — measure directional skew, feed it back to the model, never quota | Accepted | ✅ Implemented |
| [0037](0037-clean-vs-comprehensive-pnl.md) | Clean vs comprehensive P&L — lock realized FX at booking, break out translation | Proposed | ◐ Partial |
| [0038](0038-minimum-variance-proxy-hedging.md) | Minimum-variance proxy hedging — beta to index futures, DV01 to Treasury futures, direct FX | Proposed | ◐ Partial |
| [0039](0039-hedge-lifecycle-band-triggers.md) | Hedge lifecycle — always-flat deterministic hedging, advisory-first | Proposed | ◐ Partial |
| [0040](0040-structural-sector-hedging.md) | Structural sector hedging — a history-free fundamental floor under the statistical hedge | Accepted | ◐ Partial |
| [0041](0041-estimation-rigor.md) | Estimation rigor — covariance burn-in, par-curve bootstrap, honest VaR windows | Accepted | ✅ Implemented |
| [0042](0042-hedge-proxy-selection.md) | Hedge proxy selection — best measured proxy, tradability-gated, switch hysteresis | Accepted | ✅ Implemented |
| [0043](0043-per-instrument-strategy-selection.md) | Per-instrument strategy selection from out-of-sample results (no blind global algo) | Proposed | ✅ Implemented |
| [0044](0044-regime-aware-strategy-selection.md) | Regime-aware strategy selection from a price-derived trend detector (no regime oracle) | Accepted | ✅ Implemented |
| [0045](0045-news-advisory-signals.md) | News-driven advisory signals — multi-source RSS → queued SLM sector-classify → bounded, audited overlay | Accepted | ◐ Partial |
| [0046](0046-no-sim-tick-archive.md) | Don't archive SIM ticks — a seeded sim run is reproducible, not stored | Proposed | ⏸ Deferred |
| [0047](0047-postgres-vs-lmdb-role-split.md) | Keep PostgreSQL as the system-of-record; LMDB stays derived-only (re-evaluation) | Proposed | — n/a (decision) |
| [0048](0048-persist-strategy-actions.md) | Persist deterministic strategy actions (entries/exits + reasons) so the UI survives restart | Proposed | ○ Not started |
| [0049](0049-ai-signals-never-order-backtest-gated.md) | AI/news signals never place orders — the deterministic backtest is a hard gate | Accepted | ✅ Implemented |
| [0050](0050-social-media-adversarial-source.md) | Social media as an adversarial source — spam/credibility/corroboration controls | Accepted | ◐ Partial |
| [0051](0051-price-derived-volatility-regime.md) | Risk-off sizing from a price-derived volatility regime (no sim-regime oracle) | Accepted | ✅ Implemented |
| [0052](0052-live-strategy-tuning-panel.md) | Live, DB-persisted strategy tuning — every dial editable, every change audited as its own provenance | Accepted | ✅ Implemented |
| [0053](0053-learned-advisory-signal.md) | Learned advisory signal — a model predicts a tradeable label, backtest-gated (no price-curve oracle) | Accepted | ◐ Partial |
| [0054](0054-event-keyed-hypothesis-identity.md) | Event-keyed hypothesis identity — the model classifies the catalyst, deterministic code dedups on it | Accepted | ✅ Implemented |
| [0055](0055-signal-fusion-target-portfolio.md) | Signal fusion and a target portfolio — signals stop placing orders | Accepted | ✅ Implemented |
| [0056](0056-multi-source-market-data-coverage.md) | Multi-source market-data coverage — source precedence via a freshness-guarded merge | Accepted | ✅ Implemented (guard + Yahoo fallback + Alpaca; no priority table — freshness subsumes it) |
| [0057](0057-intraday-strategy-family.md) | Intraday strategy family — VWAP-deviation reversion first, pairs deferred | Proposed | ☐ Not started |
| [0058](0058-var-partial-history-coverage.md) | VaR coverage — include names with sufficient history, revalue on the common window | Proposed | ✅ Implemented (Oleg-directed batch) |
| [0059](0059-fusion-execution-gate.md) | Fusion execution gate — trade only OOS-validated, convicted names (stop the churn) | Proposed | ✅ Implemented (Oleg-directed batch) |
| [0060](0060-dynamic-discovery-driven-universe.md) | Dynamic discovery-driven universe — daily promotion into the refdata master, provisional refdata | Accepted | ✅ Implemented (gate + dry-run proposer; runtime refdata write path + eviction; a promoted name is a first-class tradable instrument in the refdata universe — no monitor-only; enabled. Owner dropped monitor-only probation) |
| [0061](0061-paper-execution-any-feed.md) | Paper auto-execution runs on any feed (amends ADR-0019's sim-only routing) | Accepted | ✅ Implemented (removed the `feedMode==SIM` veto in FusionExecutor + AUTO hedging; execution stays internal SimulatedExecutor — paper on real marks; ADR-0015 is the real-money guard) |
| [0062](0062-live-edge-gated-execution.md) | Gate execution on positive *measured live* edge, not only sim-OOS | Proposed | ◐ Partly built by ADR-0064 (the reduce-only, cost-aware half is live in fusion). Still open: the stricter per-`(name, algo)` positive-edge requirement to route at all |
| [0063](0063-continuous-improvement-agent.md) | Continuous improvement agent — automate the observe→diagnose→fix→rerun loop, gated on the PnL/exposure objective | Proposed | ◐ Scaffolding landed, inert (Claude Code headless on Max; one local cycle report→analyse→change→test→commit→push→rebuild+restart via `ops/loop-control.sh`; `report.md`+logs added to system-report.py). Never edits the deterministic floor (invariant 7 / ADR-0016); real money stays behind ADR-0015. Not enabled — awaits owner Accept + `loop-control.sh on`. Open: open-position restart safety; logs-in-bundle done) |
| [0064](0064-cost-aware-edge-gate.md) | Gate risk-increasing fusion trades on measured edge net of measured cost | Proposed | ✔ Built (fusion goes reduce-only unless a source's measured expectancy beats measured round-trip slippage with significance; monotone — it can only remove trades — and self-healing as the measurements move) |
| [0065](0065-target-book-spans-held-positions.md) | The fusion target book spans held positions, and a risk-reducing delta needs no conviction | Proposed | ✔ Built (a held name with no fresh forecast is planned flat and worked down at the existing adjustment rate; reducing deltas skip the ADR-0059 conviction floor and the ADR-0049 support veto; hedge book excluded; the deterministic floor — breaker + guardrail — untouched) |
| [0066](0066-ewmac-trend-forecast-source.md) | A continuous, self-calibrating trend sensor (EWMAC) as a fusion forecast source | Proposed | ✔ Built (`trend` publishes a vol-normalised, efficiency-ratio-weighted crossover per name into the forecast registry every cycle and records its calls in the phase-1 telemetry; self-normalising so it is scale- and feed-agnostic; no gate relaxed — edge gate, conviction floor, backtest veto, guardrail and breaker all still upstream of any fill) |
| [0067](0067-expectancy-weighted-source-trust.md) | Weight fusion sources by measured expectancy, not by hit rate | Proposed | ✔ Built (source trust is Φ of the source's own t-statistic of expectancy, replacing the floored hit-rate advantage that read every below-coin-flip source as identical; credibility shrinkage, min-sample floor and MIN/MAX bound unchanged; Σ-normalised, so it rotates conviction between sources and cannot resize the book) |
| [0068](0068-equity-relative-firm-risk-budget.md) | Equity-relative firm risk budget & bleed cutoff — the danger-cut machinery exists (guardrail + firm breaker) but its loss/drawdown dials are big-book absolutes that go toothless on a reset | Proposed | ✗ Not built — the dials are the deterministic floor, which the improvement loop may not edit; a drafted config recalibration was reverted unshipped. Both steps are Oleg's to ratify (see the ADR's status note) |
| [0069](0069-scale-relative-hedge-no-trade-band.md) | The hedge no-trade band is scale-relative, not an absolute dollar floor | Proposed | ✔ Built (`HedgeAdvisor.noTradeBand` = min(ADR-0039 $10k guard, 25% × max(|target|,|held|) notional), so an absolute guard can no longer strand a hedge smaller than itself; large-book behaviour bit-for-bit unchanged, and a zero target is now always reachable) |
| [0070](0070-range-reversion-forecast-source.md) | A continuous mean-reversion sensor (range position) as a fusion forecast source — the chop-regime counterpart of the ADR-0066 trend sensor | Proposed | ✔ Built (`reversion` publishes a bounded Donchian range-position reading per name, faded and weighted by `1 − ER` so it refuses to stand in front of a trend; self-normalising and scale-/feed-agnostic; records its calls in the phase-1 telemetry so it must earn its measured expectancy — deliberately NOT the trend source negated; no gate relaxed) |
| [0071](0071-sensor-warm-restart-from-mark-history.md) | Continuous forecast sensors warm-restart from the durable mark history instead of re-learning the stream after every redeploy | Proposed | ✔ Built (`SensorWarmup` replays the LMDB `md.marks` series into the `trend` and `reversion` sensors on first sight of a name, thinned to the sensor's own cadence and stopping at a hole; seed prices are never recorded as telemetry calls, and the store is now namespaced by feed mode per invariant 8 — no sizing, gate or floor changed) |
| [0072](0072-per-name-execution-cost-in-the-edge-gate.md) | Execution cost is a per-name property — once a source clears the desk-wide ADR-0064 hurdle, each name is re-tested against its own measured round-trip cost before risk may be put on in it | Proposed | ✔ Built (the gate carries the passing source's gross expectancy plus the per-instrument measured round trip from the ADR-0025 TCA table, and fusion clamps a name to reduce-only when its own round trip costs more than that edge; unmeasured and rate-quoted names are never vetoed on an assumed cost; no new dial, no gate loosened, deterministic floor untouched) |
| [0073](0073-feed-mode-scoped-daily-close-series.md) | The daily close series is feed-mode scoped — a handover between feeds is not a market move | Proposed | ✔ Built (`daily_close` gains `feed_mode`, PK `(day, instrument, feed_mode)`; both session writers stamp `Provenance.mode()` and the bootstrap history is tagged `SEED`; `DailyCloseSeries` admits only the running mode + seed and takes a return only within one stream, so VaR and per-name vol stop measuring a feed handover as a ±40–75% market day; V47 re-tags history deterministically from `firm_equity.feed_mode`; no risk formula, dial or gate changed) |
| [0074](0074-credibility-counts-the-sample-the-estimate-was-made-from.md) | Source trust is shrunk over the sample its estimate was made from — credibility counts RESOLVED observations (flats included, as the expectancy itself does) and the hard min-sample floor is removed | Proposed | ✔ Built (`TelemetryWeights` credibility switches from wins+losses to `resolved`, so a flat-heavy source is no longer permanently "thin"; the 1.0 floor — which was strictly MORE trusting than the shrunk value for every below-average source, laundering measured-negative evidence into neutral — is gone, leaving Bühlmann `shrinkage-k` as the single continuous thin-sample dial; `jethro.fusion.weights.min-sample` removed; weights are still ratios normalised downstream, so conviction rotates and the book cannot be scaled; deterministic floor and every gate untouched) |
| [0075](0075-per-name-cost-on-both-sides-of-the-edge-gate.md) | The edge gate tests every name against its own measured round-trip cost on BOTH sides — the desk-wide verdict is the same test at the cheapest round trip the desk can actually pay, not a blended hurdle | Proposed | ✔ Built (`EdgeGate` evaluates `(avgReturnBps − cost_n)/stdErrorBps ≥ tHurdle` per name, with an unfilled name charged the desk's measured blend so its bar is unchanged; strictly tighter than the ADR-0072 raw-mean veto for any name costing more than the blend, and only a name measured cheaper than the blend can gain permission; a measured-negative source still clears nothing at any cost; no new dial, deterministic floor untouched) |
| [0076](0076-diversification-multiplier-on-the-weights-actually-used.md) | Size the diversification multiplier from the concentration of the weights actually used, not from the source count | Proposed | ✔ Built (`ForecastCombiner` computes `DM = 1/√(Σwᵢ² + ρ(1−Σwᵢ²))` over the normalised weights — the count formula evaluated at the inverse-Herfindahl EFFECTIVE number of sources; identical to the old rule at equal weights (cold start, `weights.mode=equal`), never larger than it by Cauchy–Schwarz, and never below 1; a source held at the MIN floor for measured-negative expectancy no longer buys a full extra unit of diversification leverage; amends the ADR-0067 invariant to "re-weighting can only SHRINK the book"; no new dial, deterministic floor untouched) |
| [0077](0077-expectancy-standard-error-across-emission-cohorts.md) | Measure a source's expectancy standard error across emission cohorts (Fama–MacBeth), not across observations — a whole cross-section emitted in one burst is ONE draw of the market, not 23 | Proposed | ✔ Built (`SignalScoring.aggregate` groups resolved observations into cohorts by entry time within `jethro.signals.cohort-window-seconds`, takes the point estimate as the mean of cohort means and `stdErrorBps = sd(cohort means)/√B`, and reads 0 when B<2; measured 2.23× understatement on the live `trend` cohorts (t −5.25 → −2.31) and `reversion`'s entire +4.03 bps reading is one cross-section; identical to the i.i.d. estimator for a staggered emitter and identical in the point estimate for equal-sized cohorts; `resolved`/`minSample`/`tHurdle` unchanged; strictly widens the standard error, deterministic floor untouched) |
| [0078](0078-fusion-sizes-in-the-instruments-own-contract-terms.md) | The fusion order path sizes and rounds in the instrument's OWN contract terms — cash-at-risk becomes a quantity by dividing by `price × contractMultiplier`, the money value of one unit, not by price | Proposed | ✔ Built (`TargetPlanner.targetQuantity` divides by `price × multiplier` so `|qty| × price × multiplier` — the same expression `PositionRisk` uses for `netExposure` — equals the `unitNotional` asked for in every asset class; `FusionPlanner`/`FusionLifecycle` gain a `multiplierFor` lookup wired to `InstrumentRefSource`, and an unknown spec plans FLAT rather than sizing as if it were a share; `TargetPlanner.tradableQuantity` rounds toward zero at whole units for multiplier 1 (byte-identical for every equity and FX name) and at the order/fill quantity scale for a CONTRACT, which is what the ADR-0039 hedge advisor already submits on ES; equities and FX unchanged exactly, futures/bonds/swaps no longer carry 50×–80,000× the intended exposure; no new dial, deterministic floor untouched) |
| [0079](0079-portfolio-diversification-multiplier-on-the-target-book.md) | Scale the fused target book by its MEASURED portfolio diversification, not name by name — a cross-section that is really one bet must not carry N per-name budgets of it | Proposed | ✔ Built (`PortfolioRiskNormaliser` computes `PDM = min(1, σ_indep/σ_actual)` over the EWMA(λ=0.94) daily-return covariance `CovMath` already prices parametric VaR and the ADR-0038 hedge advisor with, and scales every covered target before the edge gate clamps; `σ_indep` is the risk the per-name `unit-notional-usd` dial ALREADY implies, so no book-level money number is introduced and Oleg's dial keeps its meaning; uncorrelated names give PDM 1 and a byte-identical book, N perfectly correlated names give 1/√N, the cap at 1 means an internally-hedged book is never levered up (the ADR-0076 one-way property), and the uniform positive scale flips no name's side; an uncovered name enters neither the sums nor the scaling — no measurement, no claim; `portfolioRiskMultiplier` + `covarianceCoveredNames` surfaced on the target book; no new dial, deterministic floor untouched) |
| [0080](0080-holding-period-matches-the-evidence-horizon.md) | The desk's HOLDING PERIOD is derived from the horizon its edge is measured over — one measurement horizon held per round trip paid, so the edge gate stops crediting an hour of return against one round trip while the desk turns over every 43 seconds | Proposed | ✔ Built (`TargetPlanner.adjustmentRateFor` returns `a = 1 - exp(-cycle/horizon)`, the unique fraction whose exposure time constant `τ = -cycle/ln(1-a)` equals `jethro.signals.horizon-seconds` exactly; `FusionConfig` derives it from the same property the telemetry is configured by so the two cannot drift, and `jethro.fusion.adjustment-rate=0` now means DERIVE with a positive value as an escape hatch — the dial is replaced by an identity, not a chosen number; `TargetPlanner.orderDelta` splits the gap at flat and rates ONLY the risk-increasing part, so a cut trades in full in one cycle (previously an asymptotic ~7-cycle grind paying a round trip per step and leaving a one-unit residual) while adding is ~60× slower; attacks the measured churn — 11 MSFT legs in 13 minutes netting to exactly zero, `-$895.94` realised on `$140.73` of fees; no new dial, deterministic floor untouched) |
| [0081](0081-the-hurdle-is-read-against-the-distribution-the-statistic-follows.md) | The edge gate's significance hurdle is read against the distribution its statistic ACTUALLY follows — Student's t on the cohorts its standard error was estimated from, not a fixed normal quantile | Proposed | ✔ Built (new pure `Significance` supplies the Student-t upper tail via the regularized incomplete beta identity `P(T>t) = ½·I_{ν/(ν+t²)}(ν/2, ½)` with Lentz continued fraction + Lanczos log-Γ, reusing the single existing Φ; `EdgeGate.Params.tHurdle` is reinterpreted as a CONFIDENCE level converted once to `α = 1 - Φ(tHurdle)` — 2.0 ⇒ α = 0.02275, value and calibration unchanged — and `SourceEdge.clears` tests `p ≤ α` on `cohorts - 1` degrees of freedom, since ADR-0077's standard error is ESTIMATED from those cohorts; a cross-sectional source emits ONE cohort per measurement horizon, so B is single-digit for hours and at B=3 a fixed 2.0 was a ~91% test wearing a 97.7% label; exactly a no-op as df → ∞, strictly conservative below it; evidence ordered by p-value, which is the only statistic comparable across sources with different df; degenerate inputs read p = 1; numerics pinned to published t-table points, the Cauchy and df=2 closed forms, and exact factorials; no new dial, no sizing change, deterministic floor untouched) |
| [0082](0082-the-evidence-horizon-is-measured-not-dialled.md) | The horizon the desk's edge is measured over is CHOSEN BY THE DATA, not by a dial — every call is graded over a geometric ladder of horizons and the edge gate selects the rung the evidence supports, paying the multiple-testing haircut for having looked | Proposed | ✔ Built (new pure `HorizonLadder` builds the ladder `base, base/4, base/16` at the 4x span ratio the trend sensor already uses -- shipped `jethro.signals.horizon-rungs=3` => 3600/900/225s, 1 rung reproduces the old behaviour exactly -- and selects the rung with the smallest best p-value among those whose gate opens, ties breaking to the LONGER horizon; `SignalTelemetry.record` opens one observation per rung on one shared entry instant and mark, with `horizon_seconds` in the open-call key and the resolved query so the per-rung sample limit stops a 16x-faster rung starving the base; `EdgeGate.Params.alpha()` becomes `(1-Phi(tHurdle))/m` (Bonferroni -- valid under the strong positive dependence between nested measurements, so EVERY rung including the base faces a strictly HARDER bar than before) and `Decision` states the horizon it judged; the selected rung drives the gate verdict, the ADR-0055 source weights AND -- via ADR-0080's identity evaluated per cycle in `FusionLifecycle` -- the holding period, so grading, weighting and holding can no longer disagree; attacks the measured deadlock: `reversion` +13.51bps at 88% but only 3 cohorts because a cross-sectional source emits ONE cohort per horizon, book flat at $0 gross with PnL frozen ten cycles; shortening the horizon is NOT a way to buy significance -- expectancy shrinks with the period while the round trip does not, so a short rung clears only if the edge is genuinely fast, and both cases are pinned as worked tests; no money dial, deterministic floor untouched) |
| [0083](0083-per-name-budget-is-split-by-measured-volatility.md) | Split the per-name cash budget by each name's MEASURED volatility, not equally — every covered name contributes the same standalone RISK instead of the same cash | Proposed | ✔ Built (new pure `VolatilityBudget` scales each covered target by `kᵢ = σ_ref/σ̃ᵢ` over the same EWMA(λ=0.94) daily-return covariance that prices parametric VaR, the ADR-0038 hedge advisor and the ADR-0079 multiplier, where `σ_ref` is the HARMONIC mean of the winsorised σ — the unique reference for which `Σkᵢ = |C|`, so the covered budgets sum to exactly what they summed to before and `jethro.fusion.unit-notional-usd` is UNCHANGED, keeping Oleg's meaning as the cash for a name of TYPICAL volatility; closes the volatility half of the concentration problem whose correlation half ADR-0079 prices — its own `σ_indep = √(Σ eᵢ² Σᵢᵢ)` benchmark is unequal across `i` precisely because `Σᵢᵢ` is; measured 7.4x σ dispersion on the live universe (EURUSD 0.435%/day to NVDA 3.236%/day), so flat cash silently concentrated the book; applied BEFORE the correlation control since "how much of this book is one bet" is only well posed once the names are comparable; each covered name's standalone risk `notionalᵢ × σ̃ᵢ` is identical by construction, the book is capped at the gross it replaced so an estimated σ can never lever it up, `kᵢ > 0` flips no name's side, and an uncovered name keeps the flat dial — no measurement, no claim; nearest-rank winsorisation bites only above `100/p` covered names, documented with the structural `Σkᵢ = |C|` bound below it; `volBudgetNames`/`volBudgetDispersion`/`volBudgetLeverCap` surfaced on the target book; one convention dial (`vol-budget.winsor-pct`, moves no total), no money dial, deterministic floor untouched) |
| [0084](0084-the-desk-posts-to-enter-and-crosses-to-exit.md) | The desk POSTS to enter and CROSSES to exit — a risk-INCREASING fusion delta rests as a DAY LIMIT at its own arrival mark, a risk-REDUCING one still crosses as MARKET | Proposed | ✔ Built (`FusionExecutor` prices a risk-increasing delta at the instrument's mark read from the SAME cache the order module stamps as the TCA arrival price — so a passive fill's measured implementation shortfall is exactly zero rather than the difference between two clocks — stated at the 6dp price scale and rounded AWAY from the side being bought (DOWN for a BUY, UP for a SELL) so rounding can only improve the posted price; exits stay MARKET, the same asymmetry ADR-0065/0080 already apply to the gates and the adjustment rate, because a cut that waits for a better price is not a cut; `FusionLifecycle` retires its own working orders — scoped by the `fusion:` idempotency-key prefix, so never the hedge advisor's or an operator's — at the TOP of each tick before the book is read, so the plan is never stacked on stale intent; new `OrderService.workingOrders()`/`lastPrice(id)`/`cancel(id, reason)`; attacks the measured deadlock directly — reversion's +5.63bps at 1.35bps standard error over 17 cohorts can survive a 1.95bps round trip, while every tradable name's MEASURED round trip is its full spread (MSFT 2.87 to GOOGL 20.11) and the only name that clears is ES at 0.35bps, which the ADR-0049 selector never measures and so vetoes: thirteen cycles at $0 gross; the hurdle, alpha, Bonferroni haircut and degrees of freedom are ALL untouched — only the measured cost the same test reads falls; no dial and no money number is introduced, the limit IS the mark; deterministic floor untouched)
| [0086](0086-the-fusion-desk-cuts-on-its-own-volatility.md) | The fusion desk CUTS a position on its own measured volatility — a name is worked flat once its mark has retraced from the best level seen since the position was opened by more than k x the name's own sigma over the desk's own holding horizon | Proposed | ✔ Built (new pure `StreamVolatility` measures per-name sigma from the MARK STREAM — EWMA of squared log returns at the fusion cadence, `sigma_h = sigma_d * sqrt(h/d)`, running mean until a full span is absorbed and silent before that (ADR-0066's lesson), seeded on first sight from the durable mark store since its warm-up exceeds the process lifetime (ADR-0071) — because EVERY sigma the desk currently prices risk with comes from `daily_close` and covers 3 of 23 planned names and NONE of the names actually held, so the parametric VaR reports its whole covered exposure as skipped; new `TrailingRiskCut` trails from the PEAK (max for a long, min for a short — the chandelier exit, LeBeau/Kaufman ch.23) rather than from entry, so it implements let-winners-run and is strictly tighter than an entry stop, never looser; runs LAST, after the edge gate, and can only ever set a target FLAT and clamp the delta to a reduction, so an estimated sigma can never lever the book up and an unmeasured name is left exactly as planned; the cut period and the re-arm cooldown are both the DERIVED holding horizon (ADR-0080 at the ADR-0082 rung), not dials; closes the gap that the fusion path — the sole order origin, carrying the only source clearing the gate, whose mean-reversion payoff is short optionality — had no per-name cut at all while the legacy path has had one since ADR-0019; one dial, `risk-cut.sigma-multiple=3.0`, PLACEHOLDER — Oleg to set, the conservative end of the 2.5-3x ATR chandelier convention read against a horizon sigma; worked example pinned as a test (0.3 ES short, trough 5440: 5600 = 2.9412% holds, 5605 = 3.0331% cuts against a 2.9851% trigger, delta +0.300000 in one cycle); deterministic floor untouched. Note: ADR-0085 was written and then reverted by the improvement loop's scorer, so that number is retired and unused.) |
| [0089](0089-the-fusion-desk-measures-correlation-on-the-stream-it-trades.md) | The fusion desk measures CORRELATION on the stream it trades — the two sizing controls that look at the book rather than at a name are given a covariance that actually covers the names being planned | Proposed | ✔ Built (new pure `StreamCovariance` measures the EWMA of return cross-products over SYNCHRONISED per-cycle mark snapshots, fed the same prices the plan was made from and seeded by the new `SensorWarmup.jointSeedSamples` — the durable mark history replayed as snapshots on ONE bucket grid of the store's provider clock, last mark carried forward exactly as the live mark-cache read does, truncating at a hole on the existing gap tolerance; closes the gap that ADR-0079's concentration multiplier and ADR-0083's volatility budget both read a `daily_close` covariance which under ADR-0073 needs several admissible sessions in the RUNNING feed mode, so on a young stream it covers none of the planned book — the parametric VaR on the same covariance reports its whole exposure as skipped — and both controls fall through to leave the book exactly as planned, while the planner is short nearly every name at once and gross rises cycle over cycle; ONE estimator per cycle chosen by coverage of the book being planned with ties to the incumbent daily estimate, never a blend of two sampling periods, disclosed as `covarianceBasis` on the target book; both consumers are homogeneous of degree ZERO in the covariance (PDM is a ratio of two quadratic forms; the vol budget divides by a harmonic mean of the same cross-section and winsorises by rank) so the per-interval sampling period cannot move a size and no trading-day convention is invented — pinned as a test; one-way by construction, PDM capped at 1 and the vol budget capped at the gross it replaced, so a fresh statistic can only ever shrink the book; worked example pinned as a test (two names printing 100/110/99 together ⇒ ρ = 1 ⇒ PDM = 1/√2, the 1/√N ADR-0079 promises; reversed, the cap discards a ratio above 1); two convention dials only (`stream-covariance.enabled`, `.span=120` = one hour at the 30s cadence, the same span as the ADR-0086 σ sensor), `unit-notional-usd` untouched, deterministic floor untouched) |
| [0090](0090-a-change-of-view-is-not-an-exit.md) | A change of view is NOT an exit — only a FLAT target is worked at full speed; every other move of the target is a rebalance worked at the derived partial-adjustment rate, in both directions | Proposed | ✔ Built (one branch in `TargetPlanner.orderDelta`, narrowing ADR-0080's Decision 2: that ADR rated only the risk-INCREASING half of a gap and traded every reduction in full, justified as "a cut that waits is not a cut" — but the method never sees WHY the target moved, so it read a mere change of view as a danger cut; with a mean-reverting source dominating the fused forecast the combined view crosses the held position many times inside one measurement horizon, and each crossing liquidated the whole accumulated position and started rebuilding the other way, so τ_in = h while τ_out = 0 and the desk paid the full cost of a round trip while never reaching the size at which the measured edge could pay for it — visible order by order in the live window as a seven-minute 3-share grind into AAPL followed by a single 101-share reversal, with the firm's fee bill the large majority of its total loss on a gross book two orders of magnitude below the notional traded; every control that actually means "get out" says so by setting the target FLAT, so the `target = 0` branch leaves the ADR-0086 chandelier stop, the ADR-0065 orphan unwind and the ADR-0027 breaker / pre-trade guardrail bit-for-bit unchanged; what remains is a rebalance, and rating it makes the policy the exponential smoother ADR-0080's own identity describes — `posₜ₊₁ = (1−a)·posₜ + a·aimₜ`, e-folding toward the aim in exactly one horizon in BOTH directions, pinned as a test over three legs (up, down, through flat); it can never lever the book up since `a ≤ 1`; accepted trade-off stated in the ADR: ADR-0083's volatility budget and ADR-0079/0089's concentration multiplier express themselves by SCALING a target rather than flattening it, so their de-risking is also worked over one horizon now — they are sizing controls, not danger detectors, and the danger detectors flatten; worked example pinned as a test (long 101 → target −210 at the shipped a = 0.008298707361: −2 shares where ADR-0080 traded −102; target 0 → the whole gap, unchanged); no new dial and no new money/risk number — only which half of the existing rate applies) |
| [0091](0091-the-desk-does-not-net-against-its-own-hedge.md) | The desk does NOT net against its own hedge — the fusion planner measures its gap against the books it ROUTES INTO, never against the hedge overlay | Proposed | ✔ Built (`FusionConfig.firmPositions` → `routedBookPositions(risk, hedgeBook)`, which skips positions in the configured `jethro.hedge.book` exactly as the neighbouring `heldInRoutedBooks` already did; the two suppliers handed to the fusion lifecycle disagreed — one said the hedge book is not ours, the other summed its quantities as ours — and the failure the first was written to prevent arrived by the second route: with `current = s + h` the planner reads its gap as `target − (s + h)` and opens an equal, opposite `−h` in a STRATEGY book every time the hedger acts, while the hedger's own target (measured over cash equities only) is unchanged by that sale, so neither loop sees the other's control variable and the fixed point is `s = target − h`: the firm holds `|target − h| + |h|` of the contract where the economics call for `|target|`, the hedge is EXACTLY cancelled — on the balance sheet and in the fee bill, contributing nothing to net — and the offsetting leg grows with `h`, a ratchet with no fixed point in gross; measured live on the ADR-0042 selected proxy: `MACRO −0.060884 ES` against `HEDGE +0.050697 ES`, firm gross $30,408 for a firm net of −$2,776, 47% of the firm's gross exposure carrying 9% of its net, with the planner's own target book reporting `ES: current = +0.011200` — a POSITIVE current on a contract the strategy book is short — and the hedge book the firm's single worst position, its loss exceeding the whole firm's and almost all of it realised on round trips rather than lost on a view; worked example pinned as a test (at convergence the old read leaves the strategy leg at `target − h = −0.210797` and the new one at `target = −0.160100`, the gross removed being exactly `h`, and one cycle of the old loop plans a sale of precisely what the hedger just bought); a signed `BigDecimal` sum with one book filtered out — no new dial, no new money or risk number, no rounding introduced, every instrument the hedge book does not hold unaffected quantity for quantity, hedger and deterministic floor untouched) |
| [0092](0092-a-forecast-scalar-is-measured-not-promised.md) | A forecast scalar is MEASURED, not promised — each continuous source is rescaled to the size its own readings actually are | Proposed | ✔ Built (new pure `ForecastScalars` keeps one EXPANDING mean of |claim| per source, pooled across instruments, and `ForecastRegistry` publishes `clamp(claim × TARGET_ABS / max(TARGET_ABS, mean|claim|))`; every continuous mapper's uncapped arithmetic moves into a `…Claim(…)` accessor the `from…` mapper is defined in terms of, so the measured quantity and the published one cannot drift; closes the gap that the four phase-2 scaling constants are CLAIMS about how big a source's readings are which nothing has ever checked — measured live, trend runs `E|f| = 15.2` against a promised 10 with a median of 18.9 and 11 of 23 names pinned exactly at the ±20 cap, reversion `14.7` with 6 of 23 pinned, so over a third of the cross-section is clipped and a clipped forecast carries NO cross-sectional information: eleven names reading the identical +20 are indistinguishable to the planner and the source has degenerated from a forecast into a sign function, while sizing is linear in the forecast so the inflated scale is also an inflated book (~$381k planned against ~$19k held) and the turnover bill to grind toward it; expanding rather than decaying per Carver, Systematic Trading 2015 ch.7 — the estimand is a structural property of the sensor, not a regime, so there is no half-life to invent; measured on the CLAIM never the output, so rescaling cannot feed back into its own estimate, and causal — the scalar applied to a reading comes from the readings strictly before it, so a name's forecast cannot depend on where in the cycle's cross-section it was published; deliberately ONE-WAY, `max(TARGET_ABS, …)` bounds the scalar at 1 so an over-delivering source is scaled DOWN and an under-delivering one is left exactly as it is rather than levered up on an estimate — ADR-0087 and ADR-0088 were both reverted for growing exposure with no PnL and an estimate that can only shrink cannot repeat that; ordinal sources (hypothesis conviction, social channel counts) excluded as a declared scale with no claimed E|reading| to hold them to; below min-sample the scalar is 1.0, bit-for-bit the previous behaviour; worked example pinned as a test (a source measured at E|claim| = 15 ⇒ scalar 2/3; two names claiming 18 and 24 went 900 vs 1,000 shares — an 11% gap on a reading 33% stronger — and now go 600 vs 800, the full 33% expressed and both smaller; a claim of 45 still clips, the cap is un-jammed not removed); two convention dials only (`forecast-scalar.enabled`, `.min-sample=30`, the same small-sample convention as `edge-gate.min-sample`), no money number authored, `unit-notional-usd` untouched, deterministic floor untouched) |
| [0094](0094-the-no-trade-band-is-measured-against-the-aim-not-the-target.md) | The no-trade band is measured against the AIM, not the target — a buffer the desk can actually be inside | Proposed | ✔ Built (new pure `PositionBuffer` runs LAST in the fusion cycle and re-derives every delta from an **aim** — the ADR-0080 exponential path `aim + a·(target − aim)`, seeded at the held position — trading only when `|aim − held|` exceeds `positionBufferFraction ×` the name's average position, and then only back to the buffer's NEAR EDGE; closes the finding that the ADR-0055 band (`|target| × bufferFraction` against the gap TO the target) has never suppressed one order and structurally cannot, because ADR-0080 partial adjustment deliberately holds the desk below its target so the gap is a large fraction of it every cycle — measured live, all thirteen planned names with a non-zero gap traded at exactly the derived rate `0.032784` while the desk held 5–30% of its own target (AAPL −7 against −142, JNJ 47 against 260, GOOG 38 against 131), and the resulting turnover cost `ALPHA` `$268.61` of fees against roughly `$283` of gross alpha, i.e. 95% of the gross; cost here is PROPORTIONAL to quantity (fee a fixed fraction of notional, slippage in bps) so the lever is suppressing the target's oscillation rather than the order count, and under proportional costs the optimal policy is provably a no-trade region traded at its boundary (Constantinides JPE 1986; Davis & Norman Math. of OR 1990) — Gârleanu-Pedersen's smooth partial adjustment is the QUADRATIC-cost solution; exposure unchanged by construction because the aim path IS the position the old policy converged to; buffer width 0.10 is Carver's published convention (Systematic Trading 2015; Advanced Futures Trading Strategies 2023), dimensionless, and the average position is DERIVED per name from this cycle's own arithmetic `|target| × TARGET_ABS / |forecast|` — no money number authored, Oleg's `buffer-fraction=0.5` left exactly as set and documented as superseded at the routing stage; worked example pinned as a test (AAPL forecast −9.64, target −142.319300, held −7: band 14.763413, aim −11.436294, gap −4.436294 ⇒ NO ORDER where the old policy sold 4 shares every cycle; once intent has drifted to −40 the gap of 33 breaches the buffer and 18.236587 trades); a FLAT target is never buffered — the ADR-0086 cut, the ADR-0065 orphan unwind and every 'get out' control snap the aim to zero and trade the whole position exactly as ADR-0090 works it — and a shut ADR-0064/0075 gate clamps the order reduce-only AND re-seeds the aim to where the desk will actually be, so intent cannot accumulate behind it; firm breaker, pre-trade guardrail and ADR-0049 support untouched; `position-buffer.enabled=false` restores the ADR-0080 deltas byte for byte) |
| [0097](0097-a-source-must-earn-the-right-to-steer-the-book.md) | A source must EARN the right to steer the book — a view that fails the desk's own significance test is held at the MINIMUM combination weight | Proposed | ✔ Built (`EdgeGate.demonstratesEdge` — the desk's existing ADR-0077/0081/0082 significance test at ZERO cost, factored out of `SourceEdge.clears` so the gate and the weights cannot drift — admits a source to the vote, and `TelemetryWeights.compute(stats, params, admission)` holds every non-admitted source at `Params.min()`; closes the gap that the desk runs the SAME statistic through two consumers with two answers: the gate asks whether risk may be taken and the weights ask whose view counts, but the weighting statistic is `Φ(t)`, a PROBABILITY whose entire dynamic range is `t ∈ [−2.5, 2.5]`, so past the hurdle it is flat and cannot tell a source that barely clears from one that clears fivefold — measured live, the edge gate passed exactly ONE source (reversion, t = 10.65, p = 2.9e-13) while `social` (t = 1.82, p = 0.039, FAILING the desk's own test) carried 83% of its weight, and on the desk's LARGEST position (`JPM`, $13,288 gross, planned −162.58 shares) social's `−16.00 × 1.757 = −28.11` out-voted reversion's `+1.29 × 2.106 = +2.72` roughly ten to one and reversed the sign of the only view with a demonstrated edge; the test is gross of execution cost deliberately, keeping ADR-0067's separation intact (cost decides whether to trade, weights decide whose view counts — charging it here would charge one measurement twice); STRICTLY ONE-WAY since `min` is the lower clamp bound `compute` already applies, so a demotion can only lower a weight and a measured-negative source is never inverted into a contrarian bet — unlike ADR-0087 (removed this floor) and ADR-0093 (replaced `Φ(t)` with an effect size), both of which the scorer reverted and neither of which this repeats; inert when no source is admitted, which is safe because the gate's test is this test at a non-negative cost so nothing can clear the gate that fails admission and the desk is reduce-only there anyway, and which avoids the one case where demotion would RAISE the ADR-0076 multiplier by flattening the weight vector; where it does bite it concentrates the weights, raising `Σw̄²` and lowering `DM` — `JPM` goes 1.1944 → 1.1299 and its fused view −7.66 → ≈−1.5, under `min-forecast-to-route`, so the desk stops adding to a $33k short taken on evidence it had already judged insufficient; worked example pinned as a test (STRONG 500/39 cohorts +10.0 bps sd 6.0 ⇒ SE 0.960769, t 10.4083, p ≈ 4e-13 ⇒ admitted; WEAK 51/31 cohorts +3.25 bps sd 10.7 ⇒ SE 1.921797, t 1.69112, p ≈ 0.0505 > α 0.0227501 ⇒ held at 0.25 exactly, STRONG bit-for-bit unchanged), plus one-way, all-fail-is-a-no-op, null-admission-is-byte-identical and cost-monotonicity tests; NO new dial and no new statistic — `edge-gate.min-sample` and `edge-gate.t-hurdle` are the gate's own, no money/risk/exposure number is authored, the combiner normalises by `Σw` so this rotates conviction and cannot scale the book, and the deterministic floor is untouched) |
| [0098](0098-the-hedge-neutralizes-only-exposure-that-stands-clear-of-its-own-churn.md) | The hedge neutralizes only the systematic exposure that stands clear of its OWN churn | Proposed | ✔ Built (`HedgeTargetChurn` — an EWMA(λ=0.94, the same RiskMetrics decay `CovMath` uses for the firm covariance) of the squared STEP in the raw hedge-target notional, sampled once per hedge cooldown — plus `HedgeAdvisor.shrinkToChurnBoundary`, the soft threshold `T' = sign(T)·max(0, |T| − k·σ)` applied to the sized target before the ADR-0039 delta/band path; closes the gap that ADR-0039 holds the book target-flat with a ZERO rebalance floor, so the target is `−Σβ·E` at every evaluation no matter how small or how noisy that number is — measured live, `Σβ·E = −$195.49` against $37,162 of firm gross (0.5%: the strategy book is already nearly beta-neutral) while the desk sent one ES order per cooldown, alternating BUY/SELL at $250–$3,000 a time, and the HEDGE book lost $509.21 against a firm total of $326.72 — of which under $50 is fees plus 355 ES fills at a measured 0.204 bps, so the loss is DIRECTIONAL, a signed proxy bet taken on noise, and adversely signed at that since `−Σβ·E` on a mean-reversion book is long the proxy after a rally and short after a selloff, i.e. a momentum position on ES, the one view the desk measures as significantly negative; STRICTLY ONE-WAY — `|T'| ≤ |T|` and the sign can never flip, so an over-estimated σ can only make the hedge smaller and never lever the book up; a REAL hedge is untouched (the ADR-0038 $456,000 worked example at σ = $900 goes 1.676711 → 1.673402 ES, a 0.2% trim) and a target inside its own churn is set flat with the residual unwound by the ordinary delta path; σ is MEASURED from the running stream, not dialled, so it is feed-agnostic and needs no re-tuning on a live feed; the first step seeds the EWMA rather than decaying from zero and σ is published only from the second step, so warm-up cannot bias it low and a warming estimator shrinks nothing; the executing `HedgeLifecycle` is the ONLY writer of the series so a `/api/hedging` poll reads σ and never samples it; `rawTargetNotionalUsd` + `churnSigmaUsd` surfaced on the endpoint for audit; `churn-sigma-multiple = 0` restores pre-ADR-0098 behaviour exactly and every existing advisor test passes unchanged through the no-σ overload; exact-decimal worked examples pinned as tests (oscillating 0→1000→0 ⇒ σ = 1000.00 exactly; 0→100→300 ⇒ v = 0.94·10,000 + 0.06·40,000 = 11,800 ⇒ σ = 108.63; −$1,799,999.88 raw at σ = $280,000 ⇒ −5.428571 ES, exactly one contract less); the one dial `jethro.hedge.churn-sigma-multiple = 1.0` is a one-standard-error soft threshold marked `PLACEHOLDER — Oleg to set`, no money level is authored, and the deterministic floor — pre-trade guardrail and firm drawdown breaker — is untouched) |
| [0099](0099-a-name-is-charged-its-own-quoted-round-trip-before-its-first-fill.md) | A name is charged its OWN quoted round trip before its first fill, not the blend of the names the desk already trades | Proposed | ✔ Built (`QuotedSpreadCost` — `roundTripBps = 20000·(ask−bid)/(ask+bid)`, one full quoted touch = one round trip (Grinold & Kahn 2e ch.16; Almgren & Chriss 2000), written over `(ask+bid)` so the `/2` in the midpoint cancels into a single exact division — plus `withQuotedFallback`, merged into the ADR-0075 per-name cost map in `FusionConfig.selectRung` from the live `QuoteCache` (ADR-0025); closes the gap that ADR-0075 tests every name against its OWN measured round trip but a name is only measured AFTER it has traded, so an unfilled name is charged the desk BLEND and the veto arrives one discovery loss too late — the desk's two worst names are its two most expensive ever filled, GOOGL at a measured 20.11 bps round trip (−$161.84) and SAP at 8.16 bps (−$85.46), together over half of what the whole firm has made, both entered while charged the 1.48 bps blend against a source whose measured net edge is +9.07 bps at t = 9.32, and six names the desk has NEVER filled (`BRK.B`, `NFLX`, `ORCL`, `GS`, `TSLA`, `GOOGL`) quote the same 20.0 bps touch with ~$120k of planned gross queued behind them; the estimator is validated against the desk's own realised TCA and errs conservatively where it errs — ratio 0.99 on GOOGL and 0.98 on SAP where ADR-0084's passive limit is superseded and the desk crosses, ~1.5–2.1 on the tight names where it does not; MEASURED ALWAYS BEATS QUOTED, the first fill displacing the estimate permanently, so this is only ever a bootstrap; STRICTLY ONE-WAY via `max(deskBlend, quoted)` — no name's hurdle can fall below today's and `EdgeGate`'s test is monotone in cost, so it can only ever remove a trade and never add one; the desk-wide verdict is PROVABLY untouched since that verdict is taken at the cheapest round trip in map-plus-blend and nothing added is below the blend, so `mayIncrease()`, the selected rung and the source weights that read it cannot move; rate-quoted names excluded on the execution-cost model's own rule (`"SWAP".equals(assetClass)`) because a bp of a swap RATE is additive and not a fraction of notional; held positions in an excluded name stay reduce-only, never force-closed; NO new dial and NO new number — the value is the live quote or the existing blend, it is feed-agnostic by construction (invariant 9: a venue's top of book live, the ADR-0025 synthesised touch in sim), and the deterministic floor is untouched) |
| [0106](0106-a-round-trip-that-pays-the-desk-is-not-a-cost.md) | A round trip that pays the desk is not a measurement of cost | Proposed | ✔ Built (`QuotedSpreadCost.withQuotedFallback` — a NON-POSITIVE measured per-name round trip is treated as NO measurement and takes the ADR-0099 fallback `max(deskBlend, quotedRoundTrip)` (or the blend where the name has no two-sided quote), while a positive measurement is still returned untouched so ADR-0084’s genuine passive saving is preserved (AAPL measures 0.889 against a 2.00 quoted touch — one half-spread, exactly what posting to enter should save); closes the gap that the ADR-0075 cost `C` is an implementation shortfall against the ARRIVAL mark and so carries the price DRIFT over the fill window as well as the spread, and under ADR-0084 the desk posts to enter — a passive order fills precisely when the price comes to it, so entry drift is systematically favourable and on a name where it exceeds the exit’s half-spread the round trip comes out NEGATIVE, i.e. signal booked as an execution rebate; live `NQ` read −0.25445 bps and, because ADR-0075 states the desk-wide verdict at the CHEAPEST entry in the map, that one reading became the desk’s cost and `netEdgeBps = avgReturnBps − cheapest` came out ABOVE the expectancy actually measured for every source at once (`reversion` graded 8.683 against a measured 8.429, `trend` −5.977 against −6.232) — the gate that exists to make the desk pay for its trading handing it 0.25 bps of edge no signal produced — while ADR-0101’s `if (!(costBps > 0))` guard collapsed that same name to the desk’s NARROWEST no-trade buffer (0.10 Carver floor against the 0.3192 its blend implies), re-trading the least honestly-priced name the most often; corrects the `EdgeGate` javadoc’s prior claim that “a negative, i.e. price improvement, is a real reading the gate must use” — true of a ONE-WAY slippage, false of a ROUND TRIP, whose sign is fixed by construction since both legs cross the touch or wait for it and the fee is charged twice; STRICTLY ONE-WAY and asserted as tests — every per-name hurdle is ≥ what it is today and `EdgeGate`’s test is monotone in cost, so this can only ever REMOVE a trade and never add one, and the desk-wide cheapest can only rise, so exposure cannot grow because of it; NO new dial and NO new number — the threshold is the SIGN of a cost, arithmetic rather than appetite, and the replacement value is the name’s own live quote or the desk’s own blend, both already measured (invariant 7 / ADR-0016); exact worked example pinned as tests (`NQ` quote `20000 × (19786.142008 − 19785.884790) / (19786.142008 + 19785.884790) = 0.12999… bps` ⇒ `max(1.3449575609756097, 0.12999…)` = the blend; desk-wide cheapest moves from −0.25445 to `ES`’s real 0.41852998; `reversion` net edge `8.42860925 − 0.41852998 = 8.01007927`, t = 9.198 against a 2.0 hurdle — NO source changes verdict, the one that passed still passes and the four that failed still fail); feed-agnostic (invariant 9) — reads only the desk’s own measured series and the running feed’s own quote, no level, no class assumption, no sim special-case; deferred: decompose the TCA shortfall into spread and drift so the bias is removed on EVERY name rather than only where it changes sign — the reading suggests the whole series is biased low, which would make every cost-keyed control on this desk too permissive) |
| [0107](0107-a-change-of-view-is-still-not-an-exit.md) | A change of view is still not an exit — an exit is what a control ORDERED | Proposed | ✔ Built (`PositionBuffer.bufferedDelta` — the unbuffered full-speed branch is keyed on the planner's TARGET being flat rather than on the AIM reading zero, and the part of a gap that crossed flat in ONE step is worked at the ADR-0080 derived rate `a = 1 − e^(−c/h)`; closes the gap that ADR-0090's rule ("a change of view is a rebalance, not an exit") was keyed on a flat target in `TargetPlanner.orderDelta`, but ADR-0094 moved the order derivation into `PositionBuffer`, which keyed it on `aim.signum() == 0` — faithful when only a flat target could zero the aim, and no longer so once ADR-0102's clamp began holding an inverted intent at flat in one step, which on this desk is the STEADY STATE and not a corner case since the only source clearing the edge gate is a mean-reverting one whose forecast crosses the held position repeatedly inside the horizon the rate is derived from: every crossing was read as a cut and liquidated the whole accumulated position in one MARKET order, the exact round trip ADR-0090 exists to prevent, and ADR-0102's own worked example records it as intended behaviour (`gap = 0 − 104 ⇒ delta = −104.000000`); the clamp is right, classifying its output as an exit is what was wrong — nothing ORDERED that cut, and every control that means "get out" says so by planning the name FLAT; the cost is turnover, which is the dominant identified drag here (fees dominate turnover and are a function of quantity traded, and the tape shows the signature plainly: AAPL bought 1/3/3/4/5/7/8 then sold 22 and 13, JNJ bought 19/9/28/6 then sold 14/14/34), compounded by ADR-0084's execution asymmetry — an entry POSTS and pays no spread while the crossing-liquidation CROSSES and pays the full spread on everything it built for free — and it is also why the desk holds a small fraction of its own planned book, never getting far before the next crossing empties it; STRICTLY ONE-WAY and asserted as a test over every held/aim/target sign combination — `|edge| ≤ |gap|` and `a ∈ [0,1]`, so the order is never larger and never of a different sign than before, it can never open or enlarge a position, can never slow a cut a RISK CONTROL ordered (a flat target returns on the first branch, unbuffered and unrated, so the ADR-0086 chandelier stop, the ADR-0065 orphan unwind, the ADR-0027 breaker and the pre-trade guardrail keep their exact semantics) and can never slow a SAME-SIDE de-risk, which reaches the buffer by a rated aim step and is excluded by the `sgn(aim) = sgn(held)` branch; the aim path is byte-identical so INTENDED position is unchanged; NO new dial and NO new number — the rate is ADR-0080's, the band is ADR-0101's measured width, the bound is ADR-0102's target (invariant 7 / ADR-0016); exact-decimal worked example pinned as tests, correcting ADR-0102's own (JNJ held +104 against target −219.420787 at forecast −12.64: band `173.592395 × 0.10 = 17.359240`, step `93.397005` clamped to 0, gap `−104.000000`, edge `−86.640760` to the near buffer edge, all of it unwinding ⇒ `× 0.0327838995179941 = −2.840422` instead of `−104.000000`; next cycle aim `−7.193469` against a book still long 101 ⇒ gap `−108.193469`, edge `−90.834229`, delta `−2.977900` — the unwind cannot stall because the aim marches to the new side as the holding shrinks, and the position e-folds to flat over the ONE measurement horizon the edge gate priced); the honest trade-off is that positions persist longer so average gross exposure RISES — the claim under test is that turnover saved plus edge captured over the horizon it was graded on exceed the carry, and the loop's scorer settles it; feed-agnostic (invariant 9) — every input is measured off the running stream) |
| [0108](0108-the-evidence-budget-is-counted-in-cohorts.md) | The evidence budget is counted in COHORTS, not rows — a wide source must not starve itself | Proposed | ✔ Built (`SignalTelemetryStore.resolvedCohorts` + `SignalScoring.aggregate(String, List<Cohort>, long)` — each source's expectancy is estimated from the most recent `jethro.signals.cohort-limit` emission COHORTS per horizon instead of the most recent `sample-limit` ROWS, with the ADR-0077 gap-cut grouping pushed into SQL where the rows live and each cohort returned as its sufficient statistics `(n, Σx, Σx², wins, losses)`; closes the gap that the row bound predates ADR-0077 — it dates from ADR-0055 phase 1 when every observation WAS an independent draw — so since ADR-0077 the statistics have been bounded in the wrong unit and the budget was spent at a rate set by CROSS-SECTION WIDTH: a source calling ~23 names per burst was pinned at ~25–60 independent draws forever and could never accumulate another however long the desk ran, while a one-name-at-a-time source got the full 500 (live: `reversion`@225s and `trend`@225s both capped at 500 rows ⇒ 58 cohorts, against `momentum`@225s uncapped at 234 rows ⇒ 186 cohorts), so the edge gate's power was capped by breadth rather than by evidence and the desk has been REDUCE-ONLY for its whole life on this feed — `mayIncrease: false`, `deltaQty: 0` on every name of a nine-name planned book against a live `grossExposure` of `$0.00`, and a book at zero exposure earns nothing whatever the rest of the machinery does; verified against the live table with the gate's own arithmetic (α = Φ̄(2.0)/3 = 0.007583 after the ADR-0082 haircut, Student's t on cohorts−1 df, net of the cheapest round trip the desk can pay, `ES` at 0.4214 bps): `reversion`@225s moves from 500 rows / 58 cohorts / 10.35 bps / t = 0.99 / p = 0.16237 — shut — to 4 856 observations / 250 cohorts / 6.268 bps / t = 2.50 / p = 0.00653, OPEN, the point estimate FALLING while the standard error falls faster (√(250/58)), i.e. a smaller and better-estimated edge rather than a friendlier test; the same read demotes `trend`, which today carries the ENTIRE fused book (all nine targets read `sources: 1`, weight 1.23) on a 3 600 s reading of +72 bps that clears nothing (t = 0.88), to a measured-significantly-NEGATIVE −5.19 bps t = −2.47 at the rung the desk would actually hold, i.e. ADR-0097's MINIMUM combination weight; permission arrives NAME BY NAME under the ADR-0075 per-name cost test and is deliberately narrow at first — only `ES` (0.4214) clears today, `AAPL` (0.8765) reads p = 0.01098 and the desk blend p = 0.01778 — widening only as √B accrues; NO arithmetic changed — expectancy is still the mean of cohort means, the standard error still sd(cohort means)/√B, and `SignalScoringTest` pins the in-memory and SQL groupings to the same `Stats` on the same data so the two cuts cannot drift; reads LESS from the database than the bound it replaces (one row per cohort, not one per observation) while covering an order of magnitude more history; the number is NOT a money/risk dial — a measurement sample size that sizes nothing (invariant 7 / ADR-0016) — and its error direction is stated: lowering it widens the standard error, the strictly conservative way to be wrong; `t-hurdle`, `min-sample`, `hypotheses`, the cost model and the per-name veto are untouched, and nothing below the deterministic floor moves; feed-agnostic (invariant 9) and still `feed_mode`-scoped (ADR-0029); the superseded `resolvedObservations` + `Resolved` are REMOVED so there is no second row-bounded way to sample the table; rejected alternative recorded so it is not re-attempted — cross-sectional demeaning of each cohort was measured on the live table first and moves `reversion`@3600s only from t = 1.04 to t = 1.14 because point estimate and standard error shrink together (222 → 26 bps, 929 → 98 bps) while any one-name cohort collapses to identically zero; the honest trade-off is that gross exposure rises from `$0.00`, which a book at zero cannot avoid, and the loop's scorer settles whether the desk earns on it) |
| [0109](0109-a-bad-print-is-not-evidence.md) | A bad print is not evidence — the jump guard's threshold bounds the expectancy too | Proposed | ✔ Built (`SignalTelemetryStore.resolvedCohorts` + `.discardedCount` + `/api/signals/discards` — a resolved signal observation whose realised directional move REACHES the desk's own corporate-action / bad-print threshold for that instrument's asset class (`jethro.trading.mark-jump-bps.<CLASS>`, resolved through `instrument.asset_class` exactly as the mark cache's `JumpThresholds` resolves it — NO new number is introduced) is excluded from the Fama-MacBeth expectancy, at READ time in the same SQL ADR-0108 moved the cohort grouping into, before the gap-cut, so the history already in the table is healed rather than only future rows and `signal_observations` stays intact as the audit record; closes the gap that ADR-0108 uncapped the sample (58 → 313 cohorts on `reversion`@225s) and the gate STILL read `mayIncrease: false` against `grossExposure $0.00` because the ADR-0077 standard error is a plain sample sd with an unbounded influence function and ONE cohort was setting it: a 23-name burst whose entry marks are the SIMULATOR's start levels (`AAPL 188.94`, `MSFT 429.14`, `ES 5438.30`, `GOOGL/TSLA/ORCL ~99.8`) resolved against the live alpaca tape (`AAPL 337.12`, `MSFT 390.37`, `ES 7454.75`), booking GOOG `174.768112 → 323.580000` = +8 515 bps and NQ `19 773.35 → 28 677.00` = +4 503 bps over 225 SECONDS as market returns, giving that cohort a +573 bps mean where every other cohort in the sample sits inside ±35 bps — dropping it alone takes `reversion`@225s from 4.55 bps / se 1.90 / t = 2.18 / p = 0.0152 (SHUT against α = 0.00758) to 2.78 bps / se 0.55 / t = 4.33 / p = 0.00001; verified with the PRODUCTION query against the live table before shipping: the exclusion removes 2 observations of 5 101 at `reversion`@225s and 2 of 5 159 at `trend`@225s (0.04%), at most 7% in any of the fifteen source × rung cells, every rung's dispersion falls to a physically sensible magnitude (the 3 600 s standard error 145 → 14 bps — the tell that the estimator was measuring bad prints rather than the market), and EXACTLY ONE cell changes verdict — `reversion`@225s t = 2.17 → 4.05, p ≈ 0.00003, OPEN — while every measured-negative source measures MORE negative (`trend`@225s t = −2.19 → −4.68, `social`@225s −0.64 → −1.87, `momentum`@225s unchanged at −3.28) and `reversion`@900s (p = 0.045) and @3 600s (t = 0.61) stay shut, i.e. this is the removal of a price the desk could never have traded at and NOT a device for buying significance — the surviving point estimate FALLS 4.55 → 2.63 bps while the standard error falls further; the test is on the ABSOLUTE move so it cannot favour a direction; applying a per-UPDATE threshold across a whole HORIZON is deliberately LOOSER than the guard itself, the conservative direction for a rule that deletes evidence; it FAILS OPEN — a class set to 0 disables it exactly as it disables the jump guard, and an absent or unusable threshold map degrades to 'count everything', never to 'count nothing', pinned by `BadPrintExclusionTest`; what is excluded is COUNTED and served at `/api/signals/discards` and in the loop report, never dropped in silence (CLAUDE.md data-path rule); consequences that follow automatically and are intended — the selected rung moves 3 600 s → 225 s so the ADR-0080 holding rate becomes `a = 1 − e^(−30/225)`, and `trend`, which carries essentially the whole planned book at weight 1.26 while measuring t = −4.68 at that rung, is demoted to the ADR-0097 MINIMUM weight; rejected alternative recorded so it is not re-attempted — winsorizing/trimming the cohort means (Hampel 3-MAD) was tested NUMERICALLY first and caps 7–20% of cohorts rather than the 0.27% the rule is calibrated for, flipping `reversion`@900s from t = −0.24 to t = +7.95, which is redefining the estimand rather than removing contamination; nothing below the deterministic floor moves, no money/risk dial is introduced, still `feed_mode`-scoped (ADR-0029) and feed-agnostic (invariant 9); the tape handover that PRODUCED the contaminated cohort — the mark source changing under a running session without the invariant-8 epoch change — is NOT fixed here and is tracked in the deferred register; the honest trade-off is that gross exposure rises from `$0.00`, which a book at zero cannot avoid, and the loop's scorer settles whether the desk earns on it) |
| [0110](0110-a-change-that-never-reached-the-jvm-is-not-a-change.md) | A change that never reached the JVM is not a change — the loop verifies its own deploy | Proposed | ✔ Built (`ops/improve-loop.sh` step 5 + `ops/loop-control.sh` + `ops/enable-loop.sh` — after running `JETHRO_DEPLOY_CMD` the loop polls `/api/ops/jvm` and computes the answering process's boot time as `now − uptimeSeconds`, treating the deploy as VERIFIED only if the app answers AND that boot time is at or after the moment the deploy started; deploy-mechanism agnostic by design — no pidfile, unit name or container convention assumed — and it asks the only question the scorer's validity rests on, whether a process started by THIS deploy is serving the endpoints the next cycle will read; closes the gap that step 5 ran the deploy command and moved on, its success never checked and never asked about again, so when this box's crontab was re-installed carrying the systemd EXAMPLE from `ops/README.md` (`sudo systemctl restart jethro`) on a box that runs the app from `scripts/run-local.sh`, every cycle logged `Unit jethro.service not found` / `deploy command FAILED` and carried on: ADR-0108 and ADR-0109 were built, committed, pushed, baselined and SCORED against a JVM that started before either existed, both recorded ⚠️ MIXED 'no material change' — a true statement about a binary that never ran the change, entered in the ledger as if it were a finding ABOUT the change, which with no human in the loop does not merely waste cycles but MANUFACTURES EVIDENCE, since the next agent reads the verdict, concludes the idea was wrong and moves to a different lever while every artefact it reads is internally consistent; worse, the `./gradlew :app:bootJar` half of that command kept SUCCEEDING, rewriting `app/build/libs/app-0.1.0-SNAPSHOT.jar` underneath the LIVE JVM, which then threw `ClassNotFoundException: org.springframework.util.PatternMatchUtils` on every lazily-loaded class — a failed deploy actively damaging the running process; the fallback is the repo's own `scripts/svc.sh restart app`, correct on this box for two reasons that are the decision and not incidental — it STOPS the app before rebuilding so the jar is never rewritten under a live JVM, and `run-local.sh` re-reads `local.env` so provider, keys, heap and profile come back IDENTICAL (a restart that silently reverted `PROVIDER` to the script default would be a feed switch, an invariant-8 epoch event and not a restart); `JETHRO_DEPLOY_CMD` UNSET now means that repo default rather than 'do nothing', `=none` is the explicit review-before-live mode, and `loop-control.sh` no longer bakes an EMPTY value into the cron line (which would have overridden the new default with 'deploy nothing') — the same edit fixes a latent `set -e` trap where a false `[ -n … ] && ENVP=…` at the end of an AND-list aborted the script before the cron line was installed; when neither the configured command nor the fallback restarts the app the loop says so in the words the next reader needs, that the next cycle's verdict will describe code that never ran; NO money/risk/PnL number is touched and nothing below the deterministic floor moves — this is the loop's own plumbing, not the desk's; the honest costs are stated in the ADR: the fallback can restart the app with a command the operator did not choose (bounded — repo script, same config, `=none` opts out), a verified deploy costs up to `JETHRO_DEPLOY_TIMEOUT` (default 300 s) of polling and a failed one twice that, and the next scored window contains THREE deployments' worth of code (ADR-0108 and ADR-0109 finally reaching the JVM, plus this) so its vector will not attribute cleanly to any one of them; deferred, not rejected, and in the deferred register — teaching `scripts/score-change.py` to REFUSE to score a commit it cannot prove was deployed, the deeper fix and the right eventual home for the rule, kept out of this change because the scorer is the invariant-7 authority for every money number in the ledger and should not move in the same breath as the plumbing that feeds it; also deferred, verifying a build IDENTITY (git sha in the jar vs `/actuator/info`) rather than a restart, which would additionally catch a restart onto a STALE jar — revived the first time that is observed) |
| [0111](0111-a-source-measured-losing-is-stood-down-not-floored.md) | A source measured LOSING is stood down, not floored — unproven and disconfirmed are different findings | Proposed | ✔ Built (`EdgeGate.contradictsEdge` + `TelemetryWeights.compute` — a source is classified into THREE states instead of two: DEMONSTRATED (its expectancy clears the desk's own hurdle) keeps its measured weight, UNPROVEN (the sample cannot tell) is held at the ADR-0097 floor and keeps voting, and CONTRADICTED — the IDENTICAL test at the same α on the same Fama-MacBeth standard error with the measured SIGN REVERSED — is stood down to weight 0 and leaves the vote entirely; no second statistic, no new dial, nothing chosen, and the three states are total and mutually exclusive for any α < ½ so the classification cannot be ambiguous; closes the gap that ADR-0097 read 'has not demonstrated an edge' as ONE verdict and gave the floor to both the source it cannot judge and the source it has judged and found LOSING — live at the ADR-0082 selected 225s rung `trend` measures −1.5033 bps on 451 cohorts (t = −3.89) and `momentum` −2.9177 bps on 197 (t = −3.02), consistently negative at every rung of the ladder rather than in one window, yet each carried the 0.25 floor against `reversion`'s 2.899, and because `ForecastCombiner` normalises by Σweights a floored source with an OPPOSITE-signed forecast subtracts conviction from every name it calls: GOOG this cycle combined to 13.319070 (average (15.134·2.899 − 13.974·0.25)/3.149 = 12.823139, Σw²ₙ = 8.466701/9.916201 = 0.8538178, DM = 1/√0.9269089 = 1.0386636) against 15.134 with the disconfirmed source stood down — 13.6% of the desk's conviction on that name handed to a measured loser; that 13.6% is decisive because the desk sits exactly at its own margin — the ADR-0075 per-name cost gate admits only a name whose own round trip clears `2.0087 − t_crit·0.4271 = 0.969 bps`, which of eight planned names is AAPL alone at 0.897 (JPM 1.075, JNJ 1.204, MSFT 1.338, GOOG 1.854, GOOGL 20.106, unmeasured names at the 1.327 blend), and AAPL then misses its ADR-0101 no-trade band by roughly 8% (the endpoint publishes aim −122.704078 against target −134.528168 at forecast −9.007; the half-width is DERIVED here from those published inputs to explain the observed `deltaQty: 0` and is not itself a measurement) because that band is derived from the AVERAGE position and is forecast-INDEPENDENT while the aim is linear in the forecast — live consequence being eleven names with live targets, `deltaQty` zero on every one and firm `grossExposure $0.00` while the PnL growth flag reads stale; STRICTLY ONE-WAY and asserted as tests — `min(w, 0)` can only LOWER a weight and it floors at zero, so a losing source is never inverted into a contrarian bet (the canonical overfit, Harvey/Liu/Zhu RFS 2016, and the reason ADR-0067 chose a bounded Φ), and at least one non-zero weight always survives since a stand-down fires only when some source IS admitted and no source can be both; ADR-0097's guard is unchanged — with nothing admitted the desk is reduce-only anyway and the weights are left exactly as measured; a stood-down source also stops counting toward the ADR-0076 diversification multiplier, which is the honest reading since a view the desk will not act on is not breadth; NOTHING here prices, sizes or gates money — weights are dimensionless conviction ratios and the money boundary is `TargetPlanner`, downstream and in exact decimal (invariant 1 / invariant 7 / ADR-0016) — and the deterministic floor (pre-trade guardrail, ADR-0027 breaker, ADR-0086 risk cut) is untouched; self-calibrating and feed-agnostic (invariant 9) since the verdict is re-read from the running stream's own telemetry every cycle, so a source that recovers returns to the floor and then to its measured weight with no edit; honest costs stated in the ADR — this RAISES exposure (a stronger forecast is a larger target, and if the reversion edge does not survive contact the loss is bigger, not smaller), it CONCENTRATES the desk onto one source where trend/momentum were the only other voices, a name whose ONLY fresh forecasts are all contradicted now combines to zero which is a FLAT target and therefore an unbuffered exit (the pre-existing ADR-0065 no-view-no-position path, not a new mechanism, and impossible while any admitted source has a fresh view on the name), and it does NOT fix the binding constraint — seven of eight names are blocked by the ADR-0075 cost gate on an honest reading that measured execution cost (0.90-1.85 bps/name) is close to what the measured edge can pay, so REDUCING ROUND-TRIP COST is the next lever and no re-weighting substitutes for it; rejected alternatives recorded so they are not re-attempted — inverting a contradicted source into a contrarian bet, widening the hurdle so the losers fail more slowly (a dial where a measurement will do), and attacking the ADR-0101 band instead, whose average-position multiplicand is correct precisely because it makes the half-width forecast-independent, which is what Grinold-Kahn's `2C/(λσ²)` actually is) |
| [0112](0112-a-name-the-desk-cannot-price-is-priced-from-its-own-prints.md) | A name the desk cannot price from fills or quotes is priced from its OWN PRINTS — the blend is a closed loop | Proposed | ✔ Built (`EffectiveSpreadCost` + a third rung in `FusionConfig.selectRung`'s per-name cost ladder — a name with neither a measured round trip (ADR-0075) nor a live two-sided quote (ADR-0099) is charged the EFFECTIVE SPREAD ITS OWN PRINT SERIES IMPLIES, by Roll's implicit estimator `s = 2·√(−cov(rₜ, rₜ₋₁))` (Roll, JF 39(4) 1984; Hasbrouck, *Empirical Market Microstructure* ch. 4), which is one round trip in exactly the sense the other two rungs use the term — ADR-0099 states the same identity for a quote and the TCA rung is one-way shortfall doubled, so all three are measurements of the same quantity in the same units; closes the gap that on a feed publishing prints WITHOUT A BOOK — this desk's feed, where every mark carries `bid: null, ask: null` so `quotedRoundTripByInstrument` returns empty every cycle and ADR-0099 is inert — what is left is the desk BLEND, and the blend is a CLOSED LOOP: a name is measured only after it fills, an unmeasured name is charged the blend, and when the blend does not clear the ADR-0075 hurdle every unmeasured name is held reduce-only, so it never fills, so it is never measured; live at the ADR-0082 selected 225s rung `reversion` measures +1.913961 bps on a 0.403198 standard error, so the most a name may cost and still clear t ≥ 2 is 1.107565 bps against a blend of 1.326575 — the blend is DEARER THAN THE HURDLE, so the tradable universe had frozen to whichever names happened to have filled before the hurdle tightened (of 27 names with a live mark, 8 carry a measured cost and only ES 0.430, AAPL 0.897 and JPM 1.075 sit under it) with the book at $0.00 gross for over an hour, a non-empty target book and every `aim` reading exactly 0.0; FLOORED at `cheapestMeasured = min(blend, min(per-name costs))` — the cheapest round trip the desk has ACTUALLY PAID anywhere: the desk may infer that an unfilled name is cheaper than the average of the names it already trades, never that it is cheaper than anything it has ever executed — and that floor is also the safety PROOF, since `EdgeGate` takes the desk-wide verdict at the cheapest entry in the map-plus-blend and no entry this rung adds can be below it, leaving the desk-wide verdict, every source's `netEdgeBps` and the ADR-0101 buffer's reference cost BIT-FOR-BIT UNCHANGED (asserted as `deskWideMinimumIsInvariant`) so only a per-name hurdle moves; this is the FIRST rung that can LOWER a per-name hurdle, deliberately — ADR-0099 was one-way (`max(blend, quoted)`) because it had no basis for trading off two different constructions and this one does, same quantity, same units, same stream the sensors consume — and it cuts BOTH ways, a name whose own tape reads dearer than the blend is charged MORE than today and stays shut; DECLINES TO MEASURE rather than inventing one when the autocovariance is ≥ 0 (a trending or runs-driven tape where Roll's model does not hold — it never takes a root of `|cov|`, the classic misuse, pinned as a test), below 60 distinct-price steps, or across a hole in the series, falling back to the blend exactly as today (invariant 7 / ADR-0016: an absent measurement never becomes a number); repeated conflated prints are COLLAPSED rather than counted as zero-return steps, which would drag the autocovariance toward zero and UNDERSTATE the spread — the unsafe direction — the same rule the ADR-0070/0071 sensors already apply to a repeated stale price; exact worked example pinned as a test (`p = 100.00, 100.02, …` over 200 steps ⇒ `a = ln(1.0002) = 1.9998000266e-4`, `cov = −a²`, `s = 2a = 3.9996 bps` against a true touch of 1.9998 — a PERFECTLY ALTERNATING tape reads DOUBLE, Roll's known behaviour when signs alternate more than i.i.d., and over-stating a cost is the safe direction for a gate; the odd-step case is pinned too); known bias stated and accepted — Roll UNDER-states on sparse prints and a negligible bounce reads a near-zero spread rather than declining, which the floor bounds but does not eliminate, and there is a selection bias in WHICH names get an estimate (only those with negative autocovariance) though not in the estimate itself; feed-agnostic (invariant 9) since the input is whatever price series the running feed publishes — no level, no asset-class assumption, no configured spread — and rate-quoted names are excluded on the same unit rule the other two rungs apply; the three constants (`MIN_RETURNS=60`, `MAX_RETURNS=600`, `ROLL_LOOKBACK_INTERVALS=20`, the last expressed in the planning cadence so it scales with the configured interval) are sample sizes and a window length, statistical shape rather than money/risk/exposure dials, and none of them sizes anything — the estimator gates a name and sizes nothing; prices stay exact decimal and only the dimensionless log-return ratio becomes a `double`, exactly the boundary `StreamVolatility` already draws (invariant 1); one durable-history range read per still-unpriced price-quoted name per 30s planning cycle, off the ring buffer, names already carrying a cost skipped outright; the deterministic floor — pre-trade guardrail, ADR-0027 breaker — is untouched; HONEST COST: this RAISES exposure by design, and if the reversion edge does not survive contact with the newly-admitted names the loss is larger, not smaller; rejected alternatives recorded so they are not re-attempted — lowering the ADR-0075 t-hurdle so the blend clears (loosens the test for every name and treats a measurement problem as a confidence problem), charging an unmeasured name the cheapest measured cost outright (a number with no relationship to the name it prices, across a cross-section spanning two orders of magnitude), letting a name trade once to DISCOVER its cost (an unbounded deliberate loss, the very thing ADR-0099 argued against), and synthesising a quote from the feed (inventing the number the whole ladder exists to avoid); deferred, not rejected — the Corwin-Schultz (2012) high-low estimator, arguably more robust but needing bar high/low series the desk does not keep for the recent window, revived if Roll proves noisy) |
| [0113](0113-a-sensor-advances-on-prints-not-on-cycles.md) | A continuous sensor advances on PRINTS, not on cycles — the `stale` flag is a warm-load marker, not a freshness measure | Proposed | ✔ Built (`PrintClock` + a per-mark gate in `TrendForecastLifecycle`/`ReversionForecastLifecycle` — a mark is fed to a continuous sensor only when its PROVIDER timestamp is strictly newer than the last one that sensor consumed for the name, i.e. only when the tape actually printed; closes the gap that both sensors carried the comment "never advance the sensor's windows on a repeated stale price" guarded by `MarkSnapshot.stale()`, which is a WARM-LOAD marker set by `MarkCache.loadStale` and cleared permanently by the first live tick — so the guard fired for seconds after a restart and was inert for the rest of the process life, exactly the situation it was written for; live at 22:00Z the seven Alpaca equities carried ingest ages of 180-558 ms against provider clocks 122.8 MINUTES old (the 20:00Z cash close), ES/NQ 62.8 minutes, every one reporting `stale: false`, so the 10 s reversion sensor had been fed the same closing print several hundred times per name as if each were an observation; the cost is corruption rather than silence, in the two places that matter — the SCALE ESTIMATOR (both sensors divide by an EWMA of their own typical magnitude, a run of repeated prices is a run of zero-magnitude readings that drags that denominator toward zero, so at the reopen an ordinary move divided by a near-zero scale reports an extreme conviction: the ADR-0066 pin reached from the other side, at the session open, on the source carrying the dominant fusion weight) and the MEASURED EXPECTANCY THAT GATES MONEY (every published reading is booked as a directional call in the signal telemetry, so a frozen name books a fresh call every cycle off the same dead price and every one resolves at exactly zero realised return — structural zeros entering the cohort statistics the ADR-0075 edge gate reads, shrinking a source's standard error with observations carrying no information, i.e. manufactured significance in the test that decides whether the desk may put risk on); also restores `ForecastRegistry`'s freshness window, which exists so "a source that has gone quiet drops out rather than lingering as a phantom vote" and could never fire while the sensors re-submitted a view off a dead price every cycle; NO DIAL and nothing to calibrate — this is not an age threshold, so there is no "too old" to choose and no number needing provenance (CLAUDE.md / invariant 7), just the parameter-free question "did the tape print since I last looked?", which reads a live feed, a 15-minute-delayed feed, a replay and a simulated clock identically with no edit (invariant 9, nothing sim-specific and nothing feed-specific); provider time because it is the market's honest clock (invariant 5) and ingest time only reflects our poll cadence, which is precisely why it cannot answer this; it also makes ADR-0071's STATED INVARIANT TRUE — the warm-restart seed walks the durable mark series, keyed by provider time and holding one point per distinct print, so the seed always advanced per print while the live path advanced per cycle and the two halves of a sensor's history were different series; asserted against a COUNTERFACTUAL sensor whose session never halted: a gated sensor reads the reopen bit-for-bit identically to one that never saw the halt, and the reversion sensor is exactly where its last real print left it, while the ungated one un-warms itself flat and peaks several times higher on the SAME resumed prints (the bound in the test is deliberately loose — direction and order, not a fixture); HONEST COST, and it is real: the desk holds NO view on a name whose tape has stopped, so overnight and through any halt the equities leave the planned cross-section entirely, and a name with no fresh view is planned FLAT, which under the ADR-0065 no-view path is an EXIT rather than a hold (the order path already rejects on absent market data and the direction is reduce-only, but it is a behaviour change and not a small one); warm-up also becomes slower for genuinely sparse names, which now accumulate at their own print rate rather than the cycle rate — close on a live tape (AAPL prints every ~12 s against a 10 s cadence) but a name whose print interval exceeds the registry freshness window can never hold a view; this change LOWERS exposure, it does not raise it; rejected alternatives recorded so they are not re-attempted — a provider-age threshold on `MarkSnapshot` (a risk-gating number that would have to be chosen per asset class against print intervals spanning four orders of magnitude on this desk, where the correct statement needs no threshold at all), widening `SensorWarmup`'s gap tolerance so the seed bridges the halt (treats a symptom in the wrong direction: the observed seeds of 1 of 241 samples on every equity are the CONSEQUENCE of a real hour-scale hole at the newest end of the series, and bridging it would feed the sensor the jump instead of the duplicates), and suppressing only the telemetry record while the sensors keep advancing (fixes the statistic and leaves the scale estimator decaying — the half that puts on the position); deferred, not rejected — stopping the sensors on a session CALENDAR, which would say more than the tape can (a halt is not a close) but needs per-instrument trading hours the platform does not have and would be a second answer to a question the print clock already answers exactly; also deferred and in the register, the SAME defect downstream where `FusionLifecycle` feeds `StreamVolatility` (the ADR-0086 risk cut's σ) and `StreamCovariance` (ADR-0089) on the cycle clock — a σ decayed by non-prints makes a name look riskless to the control meant to stop it) |
| [0114](0114-the-warm-start-seed-is-counted-in-the-names-own-prints.md) | The warm-start seed is counted in the name's OWN prints, not in our poll cadence | Proposed | ✔ Built (`SensorWarmup.consumptionStepMillis` + the lookback and hole tolerance in `seedPrices` re-expressed in it — the seed's two derived quantities, how far back to read and how large a break stops the walk, are now counted in the step at which the LIVE sensor actually consumes that name, `max(poll interval, the name's median inter-print gap)`, measured from its own stored series; closes the half of ADR-0113 that was left behind: that ADR changed the unit the sensors accumulate in — a warm-up of 241 samples is now 241 PRINTS, not 241 cycles — while the ADR-0071 seed kept deriving both quantities from the poll cadence alone, which silently assumes the tape prints at least as often as we poll, the exact assumption ADR-0113 removed from the live path; measured live at 23:00Z from `/api/history` the median inter-print gap is 19.9 s on the Treasury curve, 90 s on NQ, 778 s on GBPUSD and 1,199 s on ES against a 10 s reversion cadence, so for ES every ORDINARY 20-minute print interval exceeded the 300 s tolerance and read as an outage — the walk broke at the first one and the seed was 1 sample of 241, exactly what the WARN log reported for ES, GBPUSD, NQ and every equity — while the Treasury curve failed the other way, its gaps clearing the tolerance but 241 prints at ~20 s needing 80 minutes of history against a lookback asking for exactly 80, seeding 147 of 241; because the live path now accumulates at that same print rate no non-equity name could ever warm, the ADR-0071 failure ('a sensor whose warm-up exceeds the process lifetime never speaks at all') re-entered through ADR-0113's door, with `reversion` carrying fusion weight 2.90 as the only source with measured edge (t = 3.55) and `/api/fusion/targets` publishing `instruments: 0` — the desk holding no view on anything, all night, every night; the step is `max` of the two clocks because the live sensor takes at most one mark per cycle AND at most one per print (ADR-0113), so it advances at the SLOWER of them, and the thinning rule is left untouched since `≥ intervalMillis` already yields `max(poll, actual gap)` per accepted point and was therefore already correct; the two constants (`×2` lookback, `×30` tolerance) are UNCHANGED — only their unit moves — so NO DIAL is added, nothing is configured and nothing fitted, the statistic being re-read from the running stream at first sight of each name so a live feed, a delayed feed, a replay and a simulated clock are each read on their own terms with no edit (invariant 9); the median rather than the mean because print gaps are heavy-tailed — one overnight break would drag a mean across every other reading — and a median needs no outlier rule chosen; explicitly NOT the alternative ADR-0113 considered and rejected ('widen the gap tolerance so the seed bridges the halt'): the tolerance is a multiple of the name's OWN typical interval, so a 12 s equity tape that then stops for three hours at the cash close is still a hole and still truncates the seed at the contiguous tail, pinned as `stillTruncatesAtACashCloseHaltOnAFastTape`, and a fast tape is sampled bit-for-bit as before (`aTapeFasterThanThePollIsSampledExactlyAsBefore`) so this change may only ever help a name whose tape is slower than the poll; one extra history range read per slow name per process life, at first sight only, and none at all for a fast name; NOTHING here prices, sizes or gates money — the class handles a price series and a count of samples, prices stay exact decimal (invariant 1) and gaps are timestamps — and every downstream gate (ADR-0075 edge gate, ADR-0059 conviction floor, ADR-0049 backtest veto, pre-trade guardrail, ADR-0027 firm breaker) is untouched; HONEST COSTS stated in the ADR — on a name with only two or three stored points the median is a poor estimate of its cadence and an over-large step could bridge a break that is genuinely an outage, feeding the sensor a jump, bounded by the store's 12 h retention but not eliminated; and warming these names is not free money, it RAISES exposure by admitting names the desk currently cannot see, so if the reversion edge does not survive contact with them the loss is larger, not smaller; rejected alternatives recorded so they are not re-attempted — reading the store's whole retained window and skipping the cadence estimate (correct in principle, ~43,000 points × 35 names of `BigDecimal` parsing on first sight, rejected on cost on this box and not on correctness), configuring a per-instrument print cadence in refdata (a number per name per session that must be maintained and would be wrong the moment liquidity changes, when the stream already states it), lowering the sensors' warm-up spans so sparse names warm sooner (those spans define what the signal IS — a 20-minute range, an EWMAC16/64 — so shortening them to fit a data problem changes the signal rather than the sampling and degrades the fast names that work today), and leaving it so the equities carry the desk (concedes ~13 h/day outside the cash session and every non-equity name permanently, on a desk whose PnL growth flag already reads stale); deferred and in the register — `jointSeedSamples`, the ADR-0089 covariance seed, carries the same mis-denomination but needs the FASTEST member's cadence rather than each name's own, since a covariance is a statement about contemporaneous returns and widening the common grid to the slowest member would throw away the fast names' information) |
| [0115](0115-session-hours-trading-gate.md) | The pre-trade guardrail opens no new exposure outside the US session — only risk-reducing (flattening) orders are admitted when the market is closed | Proposed | ✔ Built (`SessionGate` on `PreTradeGuardrail` rejecting risk-adding orders when closed; `UsTradingCalendar.isRegularSessionOpen` = trading day + 09:30–16:00 ET; `TradingCalendar.isTradingSessionOpen()` defaults always-open so SIM/REPLAY are unaffected, only `WallClockSessionCalendar` restricts; wired in `RiskConfig` behind `jethro.trading.session-gate.enabled` default true; tests in `PreTradeGuardrailTest` + `UsTradingCalendarTest`; motivated by a fresh LIVE epoch opening unmanageable after-hours AAPL/GOOG/JPM positions) |
| [0116](0116-evidence-based-change-scoring.md) | Score a change by evidence over an evaluation window (sign + statistical significance of per-cycle risk-adjusted PnL), not a single-cycle $50 delta — with a GOOD/BAD/INCONCLUSIVE verdict, changes HELD `MIN_CYCLES` before scoring, and the loop told to hold rather than churn | Proposed | ✔ Built (`evaluate_window` + `window_since` in `score-change.py`, `cmd_score` defers until MIN_CYCLES heartbeats accrue then judges by `t = mean·√n/stdev` of risk-adjusted per-cycle return with a `T_HURDLE`; INCONCLUSIVE kept-not-reverted; `MIN_CYCLES`/`T_HURDLE`/`GROSS_FLOOR` env knobs recorded per snapshot; prompt rewritten to HOLD a change under evaluation and to work on EDGE (establish/validate a signal through the OOS gate) rather than re-weight edgeless sources; unit-tested rising→GOOD, falling→BAD, noise→INCONCLUSIVE, grew-flat→BAD) |
| [0116](0116-the-risk-cut-sigma-advances-on-prints-not-on-cycles.md) | The risk cut's per-name σ advances on PRINTS, not on cycles — a republished mark is not a return | Proposed | ✔ Built (`StreamVolatility.update(id, price, providerTimestamp)` gated on the ADR-0113 `PrintClock`, called from `FusionLifecycle.applyRiskCut` with the provider time it already holds as `markTimeFor` for the ADR-0071 seed; closes the deferred row ADR-0113 opened against itself — that ADR gated the two FORECAST sensors and left the RISK sensor feeding on the cycle clock, and `StreamVolatility`'s own javadoc already stated the rule it failed to enforce ("a fabricated zero return would bias the estimate toward *this name does not move*, which is the dangerous direction for a control that decides when to cut") because its guard was written for a NULL price while the actual source of fabricated zeros is the last-value republish; the planner reads a conflating mark cache once per 30 s cycle, so on a quiet tape the same price reaches the sensor over and over and each one is absorbed as r = ln(p/p) = 0, with two consequences both in the direction that costs money — THE CUT DISTANCE COLLAPSES, since ADR-0086 cuts when the adverse excursion from the peak exceeds k·σ_h so σ IS the stop distance, and the EWMA variance decays by (1−α) per fabricated zero: worked at span 4 on a name warmed on ±1% steps, σ_Δ = 0.995% and the 3σ trigger over a 3600 s horizon sits at 32.70%, but twenty republished marks later (ten minutes of quiet at the fusion cadence, nothing like an overnight) σ_Δ = 0.0060% and the trigger sits at 0.198%, 165× tighter, so the desk stops itself out on the noise the position was sized to sit through, pays the spread and then holds the name reduce-only for a full holding horizon; at the production span of 120 the per-cycle decay is slower and the exposure window therefore LONGER, running from a few minutes of quiet out to ~11 h, past which the variance underflows to exactly zero and the sensor goes silent — so a weekend is safe by accident while a lunch lull, a halt, a thin name and the whole after-hours stretch are not — and THE WARM-UP FILLS WITH SILENCE, since `returns++` fires on every republish so a name reaches its span and starts speaking a σ built from observations that never happened, the same manufactured significance ADR-0113 found in the telemetry, here in the number that gates a stop; NO DIAL and nothing to calibrate, the parameter-free question "did the tape print since I last looked?" rather than a staleness threshold that would be a risk-gating number needing a value per asset class against print intervals spanning four orders of magnitude on this desk; the gate is on the CLOCK and never on price equality, so a name that genuinely printed 100.00 twice keeps that honest zero return (`aGenuineReprintAtTheSamePriceIsStillAnObservation`); the two-argument `update` is untouched and treats every call as a print, which is what a replay of the durable series (one point per distinct print) and a backtest bar already are, so every existing caller and test is byte-identical, and a null provider timestamp is admitted per `PrintClock` because declining to measure the clock must not silence the sensor (`withNoProviderClockEverySampleIsAdmitted`); asserted against a counterfactual ungated sensor fed the same quiet tape, whose trigger comes in under a hundredth of the gated one, with the bound deliberately loose — direction and order of magnitude, not a fixture; HONEST COST and it is a real loss of protection — the warm-up now advances at each name's own print rate, so a slow name takes materially longer to become measured and until then the risk cut makes no claim about it and leaves it exactly as planned (ADR-0086's documented "no measurement, no claim"), which on this desk means ES at ~1,200 s between prints needs on the order of days of prints from cold at span 120, and the ADR-0071/0114 seed is now the ONLY thing covering that gap; also honest — the σ a frozen name carries is genuinely STALE rather than decayed, right as of the last print and not evidence about what the reopen will do, this ADR asserting only that stale beats fabricated; rejected alternatives recorded so they are not re-attempted — a staleness threshold on the mark, flooring σ at a minimum so a decayed estimate cannot produce a ~0 trigger (treats the symptom with an invented risk number and leaves the measurement itself wrong), and replacing the σ-scaled distance with a fixed percentage stop (discards the one property that makes a chandelier exit sound, a distance in the name's own units, and swaps a measurement for a dial); DEFERRED and left in the register, the ADR-0089 `StreamCovariance` half of the original row — a covariance is a statement about CONTEMPORANEOUS returns, so a name cannot be dropped from a synchronised snapshot without breaking the pairing, and the carry-forward it does instead is the Epps bias already registered separately, whose fix is a grid/estimator change rather than this gate) |
| [0117](0117-the-warm-restart-seed-is-counted-in-prices-not-returns.md) | The warm-restart seed is counted in PRICES; the estimator counts RETURNS — so the replay must be one point longer | Proposed | ✔ Built (`StreamVolatility.warmupPrices()` and `StreamCovariance.warmupSnapshots()` = `warmupSamples() + 1`, and the two `FusionLifecycle` seed call sites plus their "still cold" WARN now ask for and quote that number; both mark-stream risk estimators are denominated in RETURNS and both say so in their own `update` — the first price of a series "is a price, not yet a return" — while the ADR-0071 replay handed them `warmupSamples()` POINTS, and N points yield N−1 returns, so the seed landed exactly one return short EVERY time, for EVERY name, at ANY depth of history: arithmetic, not a data shortage, since `seedPrices` caps the walk at the sample count it is asked for; the defect printed its own diagnosis in the two places the store supplied everything asked for — "risk-cut σ sensor still cold for NQ after seeding 120 of 120 stored prices" and "fusion covariance still cold after seeding 120 synchronised snapshots of 4 name(s)" — with `/api/fusion/targets` reading `streamVolMeasuredNames: 1` against `covarianceCoveredNames: 6` and `riskCuts: []`; ONE SAMPLE MATTERS HERE because ADR-0113/0116 moved both estimators onto the tape’s clock, so the missing return no longer arrives on the next 30 s cycle but on the next genuine PRINT — against ADR-0114’s measured gaps (~90 s NQ, ~13 min GBPUSD, ~20 min ES) the warm restart hands over a sensor still mute for a large fraction of a thirty-minute process lifetime, and the covariance is worse since it needs the PAIR to print so its delay is the slower member’s and with no covered name the ADR-0089 concentration control makes no claim about the whole book — which is the exact ADR-0071 failure ("a sensor whose warm-up exceeds the process lifetime never speaks at all") re-entered one sample wide; the harm runs the expensive way in both estimators: a cold σ means ADR-0086 makes no claim and a LOSING position is not cut, a cold covariance means a concentrated book is not shrunk; NO DIAL and no number needing provenance (invariant 7 / ADR-0016) — `+1` is the count of returns derivable from a price series, the requirement stays derived from each estimator’s own `span` rather than restated by the caller, `warmupSamples()` is unchanged and keeps its meaning so every existing caller and test is byte-identical, and no arithmetic, decay or gate moves; tests pin the boundary as hand-checkable arithmetic — `theSeedNeedsOneMorePriceThanTheSensorCountsReturns` (span 4: the replay 100/101/100/101 is FOUR prices and only THREE returns, a fifth price completes the fourth) and `theJointSeedNeedsOneMoreSnapshotThanTheEstimatorCountsReturns` (span 2: two snapshots give one joint return and leave every pair cold simultaneously); HONEST COSTS in the ADR — this is EARLIER protection, not free protection, and an armed risk cut can cut, so a name whose σ is now measured at boot may be stopped out in a window where it previously would have been left alone and that can lose money in a whipsaw; one extra history point read per name and the lookback widens by one step; and it repairs neither a name genuinely short of history (AAPL at 99 of 120 this run) nor one whose price never moves (the Treasury curve republishes a daily par yield, variance exactly zero, correctly silent); rejected alternatives recorded so they are not re-attempted — redefining `warmupSamples()` to mean prices (the internal gates are in returns, so the redefinition would have to be undone inside each class, MOVING the off-by-one rather than removing it, and silently changing what every existing caller asserts), having `SensorWarmup` add the `+1` itself (it cannot know whether its consumer counts prices or returns — `EwmacTrendForecaster.warmupSamples()` is already correctly in prices, its `slowSpan + 1 + scaleWarmup` carrying this same first-price term, so the shared helper would break the sensors that have it right), and lowering the spans so the estimators warm sooner (the span defines what the statistic IS; shortening it to fit a counting bug changes the risk measurement to work around arithmetic)) |
| [0118](0118-a-flat-aim-under-a-shut-gate-is-an-exit-not-a-rebalance.md) | A flat aim in a name the gate has shut is an EXIT, not a rebalance | Proposed | ✔ Built (`PositionBuffer.apply` works the whole holding when the settled aim is FLAT **and** the ADR-0064/0075 edge gate has put the name reduce-only; the two conditions are conjunctive and each alone is deliberately insufficient — a shut gate must not liquidate a book that is merely on hold, and a flat aim on its own is ADR-0090's churn case; the trap arises because a flat aim has TWO origins: a control that planned the name flat, which `nextAim` snaps to zero and `bufferedDelta` already works in full, or ADR-0102's `withinTarget` clamp, which fires for exactly one reason — the held position is on the side the current forecast opposes — and reaches the buffer as an ORDINARY REBALANCE measured against a band scaled by `|target| x TARGET_ABS / |forecast|`, the average position implied by a target a shut gate forbids the desk ever to take, so the no-trade region is sized by a position it may not hold and any wrong-side holding smaller than it is frozen at `deltaQty: 0` every cycle indefinitely, not wound down; diagnosed on the live book of 2026-07-28 where the desk was SHORT 1 AAPL against its own target of +6.031064 at forecast +0.436599, giving an average position of 138.137520 and a band of 13.813752 against a gap of 1.000000, and had then hedged that unwanted short with ES on the EQUITY axis so it was paying GROSS EXPOSURE ON BOTH LEGS for a position no control wanted, with no path out short of a risk cut firing or the sources going silent; NOT ADR-0080's mistake again — that policy lost money because a mean-reverting forecast crosses the held position many times inside one horizon and liquidating on each crossing pays a full round trip per wobble, which needs the desk to be able to REBUILD, and the gate condition is exactly what removes that: with the gate shut the only trade available in the name IS the cut, and a gate that is OPEN leaves every path byte-identical (`anOpenGateStillRebalancesAWrongSideHoldingRatherThanLiquidatingIt`); NO NUMBER introduced — the branch resolves to flat, which is not a size, and every dial, band, rate and cost stays exactly as measured (invariant 7 / ADR-0016), exact decimal throughout (invariant 1), strictly above the deterministic floor; STRICTLY ONE-WAY by construction — it resolves to flat and nothing else, so `|held + delta| = 0 < |held|` and it can never open, enlarge or flip a position, and a name held flat is untouched (`aShutGateStillOpensNothingWhenFlat`); tests pin the live arithmetic as hand-checkable decimals — `aWrongSideHoldingUnderAShutGateIsExitedNotFrozen` asserts the band at 13.813752 and the freed delta at +1.000000, `theExitIsNotReopenedWhileTheGateStaysShut` shows the following cycle trades nothing so there is no round trip to churn, `theTrappedExitOnlyEverReducesExposure` runs the long-side mirror; HONEST COSTS in the ADR — exiting in full pays the whole round trip now rather than spreading it, a forecast that flips back while the gate is still shut leaves the desk flat in a name it would have held (optionality it could not act on anyway while the gate is shut), and the exit CRYSTALLISES the position's unrealised PnL, which on a wrong-side position is most likely a loss; rejected alternatives recorded so they are not re-attempted — reverting to ADR-0080's work-every-reduction-in-full, scaling the buffer band by the AIM rather than the target (ADR-0101's derivation does read `band = a·(2C/μ)`, but Carver's floor is deliberately a fraction of the AVERAGE position so it does not collapse as the aim shrinks, and at a flat aim the gap would then be rated at `a = 0.0083` and rounded toward zero by `tradableQuantity` into no order at all — the position stays frozen by a SECOND mechanism), setting the target flat for every reduce-only name (a far stronger policy than the gate states, and a round trip charged against an oscillating significance test), and a residual accumulator (right shape for the rounding trap, wrong for this one — the suppressed delta here is EXACTLY zero, so an accumulator would carry nothing; registered as deferred instead)) |
| [0119](0119-a-forecast-is-sized-by-the-agreement-behind-it.md) | A combined forecast is sized by the AGREEMENT behind it, not by its mean alone | Proposed | ✔ Built (`ForecastCombiner.combine` multiplies the fused conviction by `agreement = |Σwᵢfᵢ| / Σwᵢ|fᵢ|`, the fraction of the sources' GROSS conviction that survives as a NET view — the ADR-0113/0100 efficiency ratio read ACROSS sources instead of over time — and surfaces it on `FusionPlanner.Target.agreement` and `/api/fusion/targets`; the defect it closes is that only the MEAN reached sizing, so two sensors fighting each other to a small residual produced the SAME position as two quiet sensors agreeing on it, despite carrying nothing like the same confidence about the sign — the averaging step correctly shrinks the posterior mean under conflict but nothing was widening the posterior variance, and the ADR-0076 DM then MULTIPLIED the surviving residual back up on a diversification assumption the disagreement itself contradicts; diagnosed on the live book of 2026-07-28, where the desk's entire equity risk was ONE name and it was the single name in the cross-section whose sensors flatly contradicted each other, trend +11.222357223010645 against reversion −10.408272759827817 netting to −1.5229724354072096 after a DM of 1.1497986707349463, sized at −14.915227 shares and then hedged with ES so gross exposure was paid on BOTH legs — and whose aim had flipped from +6.031064 LONG to −14.915227 SHORT between two consecutive thirty-minute cycles, because the sign of a difference of two near-equal large numbers is noise, while every other name in that cross-section had its sources pointing the same way; NO NUMBER introduced and nothing to calibrate — the scalar is a ratio of the running sums the average is already formed from, so no weight vector is materialised, no new input is read and no risk/money dial needs provenance (invariant 7 / ADR-0016), the forecast layer stays dimensionless and the `TargetPlanner` money boundary is unmoved in exact decimal (invariant 1), strictly above the deterministic floor; STRICTLY ONE-WAY by the triangle inequality — `agreement ≤ 1` always so the combined value can only SHRINK and the book can never be grown, with EQUALITY exactly when every contributing forecast shares a sign, which makes every agreeing name and every single-source name byte-identical to the pre-ADR-0119 desk, the sign is never flipped, and the all-zero 0/0 case returns 1 where the net view is already zero — the third rule in the same family as ADR-0076 (re-weighting cannot grow the book) and ADR-0098 (churn shrinkage is one-way); COMPOSES WITH ADR-0118 rather than duplicating it, since as sources converge on cancellation the aim goes flat and ADR-0118's flat-aim-under-a-shut-gate branch then EXITS the position instead of freezing it — that branch could not fire on 2026-07-28 because the aim was still large, and this is the rule that makes it reachable; tests pin the arithmetic by hand — `weightsTiltTheAverageAndSetTheDiversificationMultiplier` (+20 at weight 3 against −20 at weight 1: net |60−20| = 40 over gross 60+20 = 80 ⇒ agreement 0.5 and the value halves to 5.547001962252292), `aResidualOfTwoFightingSensorsIsNotAFullConvictionPosition` (the live AAPL pair ⇒ 0.12321282549722257, and the SAME weighted mean from UNANIMOUS sources is shown to differ by exactly that factor), and `agreementIsOneWayAndNeverTouchesSourcesThatShareASign` sweeps the whole forecast grid asserting bounds, exact-1-on-same-sign, shrink-only and sign preservation; HONEST COST in the ADR — trend and reversion are STRUCTURALLY opposed at different horizons, so a genuine trend fighting a genuine reversion is sized down even when one of them was right, trading expected return for a lower variance of being wrong about the sign, which is the correct side of that trade while measured LIVE expectancy is negative but should be revisited if the edge gate opens and the book is systematically under-sized; also honest — the scalar is one cycle's noisy estimate, bounded and one-way so the worst case is a position smaller than intended, never larger; rejected alternatives recorded so they are not re-attempted — estimating ρ and letting the existing DM handle it (`DM = 1/√(h + (1−h)ρ)` is INCREASING as ρ falls, so a measured anti-correlation makes the multiplier LARGER and amplifies exactly the residual to be shrunk: the DM answers a breadth question, disagreement is a confidence question), computing the DM per-observation from realised dispersion (mechanically the RECIPROCAL of this ratio, levering every cancellation to full size against Carver's own book-level constant), a hard agreement threshold that vetoes the name (an invented cut-off with no provenance, and a discontinuity that would flip a name between full size and flat on estimation noise), widening the no-trade band under disagreement instead (suppresses TRADING while leaving the TARGET wrong — the ADR-0118 freeze in a new costume), and shrinking at the portfolio layer (`PortfolioRiskNormaliser`/`VolatilityBudget` control TOTAL risk and would scale the confident names down alongside the contradicted one; the error is in one name's conviction and belongs where the conviction is formed)) |
| [0120](0120-a-cohort-is-one-sweep-of-the-cross-section.md) | A cohort is one SWEEP of the cross-section, identified by its names — not by a clock gap | Proposed | ✔ Built (`SignalTelemetryStore.resolvedCohorts` cuts cohorts by the running maximum of each name's occurrence count instead of by a clock gap, with `SignalScoring.sweepCohortMeans` as the pinned reference implementation of the same partition; the defect it closes is that ADR-0077 identified one draw of the market by `jethro.signals.cohort-window-seconds=60` on the explicit assumption — still written into `SignalScoring`'s javadoc and the dial's own comment — that a source emits its whole cross-section "in one ~200ms burst", which is FALSE on this desk: the trend/reversion sensors publish per name as each name's mark updates, so one pass over the eight-name equity cross-section spans MINUTES (live 2026-07-28, trend@3600s: 13:34:36 NQ through 14:02:32 JPM is ONE pass over 28 minutes, and the 60s rule cut that pass and the next into ELEVEN cohorts) — measured across every source and horizon the gap rule reports ≈2.2x more cohorts than there were passes (trend@3600s 16 vs 7, reversion@3600s 13 vs 6, trend@900s 59 vs 25, reversion@900s 52 vs 20, trend@225s 131 vs 68, reversion@225s 118 vs 55); this is the ANTI-CONSERVATIVE direction on a control that governs exposure — more cohorts narrows the ADR-0077 standard error AND inflates the degrees of freedom of the Student-t reference distribution ADR-0081 installed precisely because df is what the verdict turns on at these sample sizes — i.e. exactly the √(1+(n−1)ρ̄) understatement ADR-0077 exists to prevent, readmitted through the grouping rule instead of through the averaging; the deeper fault is that a gap rule measures HOW FAST THE SCHEDULER WALKS THE UNIVERSE, not how often the market was drawn, so a sensor tuned to publish faster would have silently multiplied the gate's apparent evidence without one new observation; NO NUMBER introduced and the dial is RETIRED rather than retuned — a sweep is identified by its own content, so `cohort-window-seconds` is deleted with nothing replacing it and the plumbing removed from `SignalConfig`/`SignalTelemetry` (invariant 7 / ADR-0016); RETROACTIVE — the partition is computed at read time from stored observations, so the whole 7-day rolling history re-scores immediately with no migration; late joiners and lone repeaters are handled without a special case (a warming sensor's first call carries occurrence 1, which cannot raise the running maximum, so it joins the pass in progress, while a source calling ONE name hour after hour still gets one cohort per call because those really are independent draws); DOES NOT OPEN THE GATE and the book stays flat and reduce-only — the only reading whose t rises sharply is momentum@3600s on 2 sweeps, which `jethro.fusion.edge-gate.min-sample=30` resolved observations blocks outright (momentum has 4), and every source clearing min-sample sits between t = −0.57 and t = +0.19 against the cheapest measured round trip; the MEAN moves too, in both directions (trend@3600s −2.41 → −8.40 bps, reversion@3600s 3.64 → 23.99 bps) because Fama–MacBeth weights each draw equally, which is what makes this a RE-SPECIFICATION rather than a one-way loosening of the hurdle; the SQL is the one untested part (it joins reference data and this repo has no database harness) so it was executed against the live schema before shipping, and its failure mode is safe — a broken cohort read returns no cohorts, every source reads zero-cohort, and `EdgeGate` requires `cohorts ≥ 2`, so the gate fails SHUT; `SweepCohortTest` pins the mechanism on a worked example (two three-name passes 200s apart per name ⇒ sweep means 0.0020 and 0.0040, where the 60s rule returned SIX cohorts for TWO passes) plus repeat-opens-the-next-sweep, lone-repeater, late-joiner, order-independence, the live eight-name pass, and the nameless-call drop; explicitly NOT DONE here and left as the natural successor — now that a cohort really holds ~8 names from one interval, asking whether a source's expectancy is skill or merely the market factor times its net directional tilt (the desk hedges net equity toward flat under ADR-0019, so beta is not P&L it keeps); rejected alternatives recorded so they are not re-attempted — retuning the window to ~30 minutes (replaces one wrong number with another, still measures scheduler cadence, and is fragile in BOTH directions since too long merges the genuinely independent hourly NQ draws the sweep rule keeps separate), a fixed time bucket such as the measurement horizon (a pass straddling a boundary is split, same artefact on a different clock), and an explicit publication epoch stamped by the sensor (cleanest in principle and worth revisiting, but it needs a schema column, a migration and a change to every emitter, and would apply only to observations recorded AFTER the change — no retroactive power, which is the whole benefit here)) |
| [0121](0121-cross-sectional-residual-reversion-is-its-own-measured-source.md) | Fade the residual against the peer group, not the raw move — as its own measured source | Proposed | ✔ Built (`CrossSectionalReversionForecaster` + `CrossSectionalReversionLifecycle` publish a fifth forecast source `xsreversion` into the same `ForecastRegistry` every other source pushes to, on the identical ADR-0066/0070 contract — it records every reading in the phase-1 telemetry and must clear the ADR-0064 edge gate on its OWN measured expectancy before it sizes anything, and while the gate is reduce-only it can only change how a held position is worked down; the problem it attacks is that NO source has demonstrated OOS edge, so the recent ledger is a wall of INCONCLUSIVE produced by tuning the combiner/gate/sensor mechanics — re-weighting sources with no edge cannot create edge — while the live telemetry of 2026-07-28T19:00Z does say one specific thing: `reversion` is the only source positive-signed at EVERY rung (avgReturnBps 0.6500729504188325 at 225s/77 cohorts, 1.2532288851764135 at 900s/29, 6.940456315814394 at 3600s/8) while `trend` is negative on both its best-sampled rungs (-0.21794567180606306 at 225s/87, -8.299129091035356 at 3600s/10), none significant against a 0.6278285714285714 bps blended round trip; the literature's reading of exactly that shape is that the documented reversal is IDIOSYNCRATIC (Lehmann QJE 1990, Lo & MacKinlay RFS 1990, Khandani & Lo JIM 2007) — fading a name that is down because the whole cross-section is down is a bet on the market factor, which carries ~zero expectancy over an hour plus the reversal trade's full turnover cost, and which ADR-0019 hedges toward flat anyway so it is not P&L the desk keeps; the computation per sweep, in the FEED's clock (ADR-0071 one-clock-only): a name is admitted only if it printed in BOTH HALVES of the lookback window — a dial-free rule, so a name that stopped printing cannot contribute a stale partial return as if it were current and a late joiner cannot contribute a minute's move against everyone else's quarter hour — its move is ln(P_last/P_first)/√span so fast and slow printers are comparable in vol time (print rates on this desk span seconds to tens of minutes, and un-normalised the slow printer gets a spurious negative residual every sweep), peers are the name's ASSET CLASS from the instrument master (invariant 9 — an FX pair in an equity cross-section would read every FX residual as ~0), and within a group of at least min-peers the score is −clamp((r − median)/(1.4826·MAD), ±max-abs-z); median/MAD not mean/σ because groups here are single-digit and one bad print would otherwise reverse everyone else's sign — 50% breakdown point, and 1.4826 = 1/Φ⁻¹(¾) is Hampel's consistency constant, not a dial; the sensor emits the WHOLE cross-section at once, which is exactly one ADR-0120 cohort, so the gate's standard error and Student-t df count it correctly with no special case; NO money, risk or exposure number introduced (invariant 7 / ADR-0016) — the reading is a dimensionless robust z, prices stay exact decimal (invariant 1) and convert to double only for the log ratio, and the three dials carry provenance in application.properties (lookback-seconds=900 IS the desk's own middle measurement rung so the residual is formed over the window its expectancy is scored over, min-peers=4 and max-abs-z=4.0 stated as MINE and arbitrary); warm-started from the durable mark history because this desk redeploys every 30 minutes and a cold quarter-hour window would spend half of every process life unable to speak — the ADR-0071 failure mode, and worse here; tests pin the arithmetic on a hand-worked four-name cross-section (spans 600s, r = ln(ratio)/√600, median 0.0003049180427927458, MAD 0.0003024116722429597, scale 0.000448355545267412 ⇒ scores −1.1230391517579659 / −0.2259423671952245 / +0.2259423671952245 / +1.595210081419047, i.e. SHORT the biggest gainer and LONG the only faller in a group that is up on average — the common move removed, which is why it is a different source from ADR-0070 rather than a rescaling of it) plus both-halves admission in both directions, min-peers, asset-class separation, clamping, span-normalisation equivalence, window pruning, out-of-order rejection and the zero-dispersion no-view case; HONEST COSTS in the ADR — the gate's Bonferroni divides α by RUNGS only (hypotheses: 3), not by sources, so a fifth source makes the un-corrected source multiplicity worse and a passing xsreversion reading deserves more scepticism than the gate expresses; peer groups on an 18-measured/8-tradable universe will often fall below min-peers and stay silent; and a new source enters at the neutral 1.0 fusion weight while MEASURED sources sit below it, shifting combined forecasts before it has earned anything — harmless only because the gate is reduce-only; rejected alternatives recorded so they are not re-attempted — tuning the weights/gate again (what produced the INCONCLUSIVE wall; no admission bound or hurdle edit changes a measurement, per the 2026-07-28 finding that retracted exactly such a change), making ADR-0070's sensor cross-sectional IN PLACE (destroys the one positive-signed track record the desk has and leaves no way to tell which construction earned it), and rank-based decile sorting (with 4–8 names a rank carries almost no magnitude information, and the MAD scale already bounds the outlier problem); DEFERRED with a stated trigger — beta-adjusted residuals r_i − β_i·r_market are the better statistic but need a per-name β estimated on the stream, and a β = 1.0 placeholder is a mistake this repo has already paid for; revive once StreamCovariance (8 names today) covers the traded cross-section) |
| [0128](0128-historical-data-is-periodically-refreshed-not-frozen-at-first-load.md) | Historical data is PERIODICALLY refreshed, not frozen at first load | Implemented | ✔ Built (`HistorySeeder` was one-time — `if (have >= windowDays) return;` — so the `daily_close` SEED history froze at its first-load date (ops showed a constant 7/20/2021→7/20/2026 range) and every reader — historical + parametric VaR, vol-targeting, hedge covariance — ran on a stale right edge; now it runs on a daemon scheduler at `jethro.hedge.history-refresh-hours` (default 24), seeding at boot then refreshing daily, skipping when the SEED tail is both sufficient and within STALE_DAYS(4) of today and otherwise re-fetching to roll the window forward; the upsert changed from `do nothing` to `do update` so a refresh re-anchors the whole SEED series to ONE consistent scale and appends the new tail — returns (all VaR/covariance/vol-targeting use) are invariant to the uniform rescale and SEED is a separate feed_mode PK partition from the live-accumulated stream so ADR-0073 is preserved; wiring is automatic — every consumer reads `daily_close` via `DailyCloseSeries`, so keeping the source current updates them all with no per-consumer change; the OOS-backtest bars file rolls daily via the ADR-0127 loop step; training bars (ADR-0053, learned-signal) remain one-time as a stated follow-up; full context + risk + trading tests green) |
| [0134](0134-orders-carry-the-trigger-that-originated-them.md) | An order records the TRIGGER that originated it, not only why its status changed | Implemented | ✔ Built (`orders.reason` is a STATUS-TRANSITION reason, so `OrderService.routeApproveAndFill` writes it only on the failure branches — guardrail REJECT, no-market-data REJECT, IOC CANCEL, ADR-0084 re-plan CANCEL — while the happy path NEW → ROUTED → FILLED passes a literal `null` at every step; live shape matches the code exactly, every FILLED and ROUTED order in a 60-order window carrying null while every CANCELLED one carries text, i.e. the only orders explained are the ones that never traded, which is why four consecutive loop cycles could not name a trigger and instead guessed mechanisms and falsified them a cycle later; the information is not missing, it is dropped — every deciding call site already holds a sentence (`signal.rationale()`, `thesis()`, the hedge advisor `rationale()`, and for fusion the target forecast plus entry / reduce / exit-to-flat / ADR-0086 stop-cut) and none of it reaches `submit`; `NewOrder` gains a nullable `originReason` written by `OrderStore.insertIfAbsent` into a new `orders.origin_reason` column (V50) AT INSERT, before any status exists, and no transition ever overwrites it, so it is present on exactly the orders that traded and a REJECTED row now carries BOTH the want and the refusal; ADV child slices inherit the parent trigger (slicing changes HOW the risk goes on, not WHY); persistence-only, so `common-domain` and the messaging contract are untouched (invariant 4); TELEMETRY ONLY — nothing reads the field back, so a null origin cannot change what the desk trades, and the deterministic floor, all money math and every sizing path are byte-identical; expected to score ⚠️ INCONCLUSIVE on the ADR-0116 window because it moves no money, which is the correct outcome for buying evidence; `-Pci test` green) |
| [0135](0135-an-unestimable-view-holds-the-position-it-does-not-liquidate-it.md) | An unestimable view holds the position; it does not liquidate it | Reverted | ↩ REVERTED 2026-08-03 — `scripts/score-change.py` scored the implementing commit ❌ BAD at the close of its ADR-0116 window (the ledger row carries the computed vector and t-statistic); the scorer's own revert hit a conflict on the loop report files and did not land, so the code was reverted manually the next cycle, keeping this record. The defect-level verification PASSED — the `fusion exit — target decayed to flat` trigger fired zero times on the window that exercised the guarded branch — but holding the position through an unestimable view kept risk deployed against a view already measured as uninformative, and that carry did not earn. NOT to be re-attempted as specified; a superseding ADR should consider decaying the inventory at the partial-adjustment rate while breadth is absent, so silence costs neither a round trip nor a full position's carry. Originally built as: (ADR-0124 correctly returns agreement 0 at ONE effective source — `1 − Σŵᵢ²` is the residual degrees of freedom of the weighted variance and is zero there, so the dispersion is UNESTIMABLE rather than zero — but that 0 multiplies the combined forecast to exactly 0, `TargetPlanner.targetQuantity` maps 0 to a target of flat, and ADR-0090 works a flat target IN FULL, so a collapse in source BREADTH executes as a decision to LIQUIDATE the whole position in one cycle; the ADR-0134 origination triggers landed last cycle record it exactly, source counts falling 4 → 3 → 2 through the equity cash close and then every routed name exiting on `fusion exit — target decayed to flat [forecast=-0.0, sources=1]`, which is structural rather than a change of view because since ADR-0113 the price-driven sensors advance only when the tape PRINTS and so fall silent at every close, leaving only the snapshot-based cross-sectional source; `Combined` gains `estimable`, false in exactly that one circumstance, and `FusionPlanner` then targets the inventory ALREADY HELD with a zero delta — no exit, and equally no entry, since an uncorroborated view may not size a position either; deliberately out of scope and byte-identical: a name with NO source is still swept flat by ADR-0065, and two or more sources netting to zero is a MEASURED view of flat and still exits in full; the deterministic floor is untouched — the ADR-0086 trailing cut, firm drawdown breaker, pre-trade guardrail, edge gate and every risk-reducing stage still run against the held target and can still flatten it; accepted cost is real overnight/weekend gap risk the desk previously flattened away by accident, bounded by the trailing cut and the breaker rather than by the forecast, with the flag surfaced on `/api/fusion/targets` so an indefinitely held name is visible rather than silent; `-Pci test` green) |
| [0136](0136-below-the-conviction-floor-a-name-is-held-or-flat-never-re-sized.md) | Below the conviction floor a name is HELD or FLAT — never re-sized | Reverted | ↩ REVERTED 2026-08-04 — `scripts/score-change.py` scored the implementing commit `e3b33679d` ❌ BAD at the close of its ADR-0116 window (the ledger row carries the computed vector, t-statistic and cycle count); the scorer's own revert hit a conflict on the loop report files and did not land, so the two running-code paths were reverted by hand the next cycle and `ConvictionFloorRoutingTest` deleted, keeping this record. The defect-level verification PASSED on both halves — splitting the order log at the JVM boot time, sub-floor `fusion reduce toward a smaller target` orders went to zero while `fusion exit — target decayed to flat` still routed at a sub-floor forecast, and `fusion_targets` proved the suppression was real rather than an absence of opportunity (NEE/JNJ/NVDA still planned non-zero sub-floor deltas that never became orders) — so the mechanism did exactly what it specified and the objective STILL got worse, which means the turnover it removed was not what was costing the money. This ADR said so itself under "Scope, stated honestly"; the window has now priced that admission. THREE mechanisms have now scored BAD against this bleed (ADR-0135, its revert, and this) — read together they say the cost is not in WHICH orders the conviction floor lets through but in the BREADTH COLLAPSE that empties the book at the equity close (effective sources → 1, every combined forecast decays to flat, the whole book liquidated in one sweep of `fusion exit — target decayed to flat [forecast=±0.0, sources=1]`), a sensor-availability defect UPSTREAM of every routing rule. NOT to be re-attempted as specified, and the next attempt must not be another variation on the conviction floor. Originally built as: (the ADR-0059 conviction floor `jethro.fusion.min-forecast-to-route=5.0` stops the desk churning a weak or oscillating signal, and ADR-0065 waived it for every risk-REDUCING delta on the stated ground that applying it there "would permanently trap exactly the positions whose view has decayed to nothing" — a reason about getting OUT, but keyed on `TargetPlanner.isRiskReducing` alone, which is equally true of a PARTIAL rebalance toward a smaller non-zero target, so the two halves of one cycle contradicted each other: the desk declared a view too weak to open a position on and then traded on it anyway, every cycle, at the ADR-0080 rate, paying fee and spread each time; caught in the ADR-0134 origination triggers for the 2026-08-03 18:00–19:00Z window, where BAC held short took SIX separate `BUY 1` fills tagged `fusion reduce toward a smaller target` at forecasts 0.3474462086964614 / 0.5363969925205933 / 0.7016479414426884 / 2.835935684486436 / 1.72006937226672 / 1.82687778188073 — every one below the 5.0 floor, every one leaving the short open — with GOOG (-1.548, -3.930) and NVDA (-4.886, -3.415) doing the same thing in the same window; the rule is now that below the floor a name has exactly TWO states, held or flat, and is never re-sized — the floor is waived for an order that takes the position exactly flat and for nothing else, implemented as the pure static `FusionLifecycle.clearsConvictionFloor` at the one place the routing decision is made; the exemption is keyed on the ORDER LANDING FLAT rather than on `targetQty == 0` because a close does not only arrive as a flat target — ADR-0118's trapped-exit branch in `PositionBuffer` plans the whole position out while `targetQty` is still non-zero, since the ADR-0102 clamp put the aim at flat with the target on the other side — so keying on what the order does to the POSITION is what makes "never trapped" hold for every path at once, including paths added later; the deterministic floor is NOT narrowed by one order: every control that means get out plans the name flat (ADR-0086 chandelier cut, ADR-0065 orphan unwind, ADR-0027 firm breaker) so each lands exactly flat and passes untouched; STRICTLY ONE-WAY — every branch either returns the same answer as the ADR-0065 rule or returns false where that rule returned true, so it can never route an order the previous rule blocked, never enlarge one and never change a delta, and risk can only come off more slowly rather than go on faster, asserted over the cross product of forecast and position cases in `ConvictionFloorRoutingTest.theRuleIsStrictlyOneWay`; NO new dial and NO new number — the same 5.0, applied to one more branch (invariant 7 / ADR-0016), with exact decimal on the position arithmetic (invariant 1) and the Carver-scaled dimensionless forecast still compared as a double exactly as the floor always did; HONEST SCOPE — the book's deficit is its fee bill (`firmTotal -264.50191793` against `totalFees 346.044801`; `ALPHA -329.32225499` on `feesPaid 334.905421`, i.e. roughly flat gross of fees) and no source shows significant edge at any horizon on the clustered `stdCohortMeanBps`/`cohorts` denominator, but the sub-floor dribble is a MINORITY of the window's turnover — the majority is the breadth-collapse round trip, tracked separately, whose two attempted remedies ADR-0135 and its revert were BOTH scored ❌ BAD — so this is presented as a real but PARTIAL attack on the cost problem, not a fix for the deficit; exposure may sit marginally higher because the slow bleed-down of sub-floor positions stops, which at gross $54,238.11 against the $1,500,000 firm cap (3.6% used, $1,445,762 headroom) is deployment inside the ADR-0132 budget rather than cap pressure, with the pre-trade guardrail, firm caps and drawdown breaker untouched and still binding; rejected alternatives recorded so they are not re-attempted — waiving the floor only when `targetQty == 0` (simpler to read but strands the ADR-0118 trapped exit, reintroducing the very trap ADR-0065 prevented), raising the floor instead (a dial change with no provenance, and it would suppress entries the desk does want), and reviving the ADR-0101 cost-derived buffer width max(0.10, min(1, 2C/μ)) — with measured C above measured μ here it pins at the 1.0 cap, one full average position, which an aim clamped inside its own target by ADR-0102 can never cross, freezing the book, which is the exact failure ADR-0133 was written to patch and was itself scored ❌ BAD; VERIFY-BY next run — in `recent_orders` the count of `fusion reduce toward a smaller target` orders whose `[forecast=…]` has |forecast| < 5.0 must be 0 (it was 10: 6 BAC, 2 GOOG, 2 NVDA), while `fusion exit — target decayed to flat` must STILL appear at sub-floor forecasts — its absence would mean an exit had been trapped and would falsify this change rather than confirm it; `-Pci test` green) |
| [0137](0137-the-planner-may-not-plan-a-book-it-is-forbidden-to-hold.md) | The planner may not plan a book it is forbidden to hold | Implemented | ✔ Built (EVERY sizing control in the fusion pipeline is σ-RELATIVE — ADR-0083 shares a per-name cash budget out by measured σ, ADR-0079 scales the book by how much of it is one bet, ADR-0104 caps its ex-ante σ at the median of its own series — and NOT ONE of them states a NOTIONAL, so on a calm tape where a measured σ is small none of them binds: read from `/api/fusion/targets` at 2026-08-04T16:29Z `volBudgetLeverCap 1.0`, `bookVolBrake 1.0`, `portfolioRiskMultiplier 0.8726…`, and the planned target book stood at $1,356,452.14 gross (Σ |targetQty × price| over the 22 planned names) against the $500,000 `jethro.risk.max-gross-exposure` the deterministic pre-trade guardrail enforces on the routing book — 2.71× the gross it is PERMITTED to hold and 14.74× the $91,999.52 it actually held; that never put risk on, because the guardrail still refuses the order, but it made the target UNREACHABLE, and the whole ADR-0080/ADR-0094 trading path is a function of the DISTANCE to the target, so the inflation was paid four ways — the aim e-folds toward a target it can never reach and settles at a small constant fraction of it (live: PFE `targetQty 3319.769516`, `aim 530.737491`, `currentQty -267.0`, trading 43 shares per 30s cycle toward a long twelve times its own short), the per-cycle step `a × gap` inflates with `gap` so the desk paid that multiple in TURNOVER every cycle against a book whose dominant loss term is exactly turnover (`firmTotal -524.66210431` vs `totalFees 369.021411`, i.e. pre-fee trading of only -$155.64), the ADR-0094 band is itself proportional to |target| so an inflated book widens the band past the gap for most names while a handful churn (`insideBuffer 19` of 22), and a desk permanently in transit NEVER holds a position through the 3600s horizon its expectancy was measured over — the dimensional argument ADR-0080 exists to make, defeated at a different point in the pipeline; the fix is `GrossNotionalCap`, applied after ADR-0104 and before the ADR-0064 gate / ADR-0086 cut / ADR-0094 buffer so the operator's target book shows what will route: `plannedGross = Σᵢ |qᵢ·pᵢ·mᵢ|`, `GNM = min(1, capUsd / plannedGross)`, `qᵢ' = qᵢ · GNM`; NO NEW MONEY NUMBER — `capUsd` is WIRED from `jethro.risk.max-gross-exposure`, the guardrail's own owner-provenanced cap (invariant 7 / ADR-0016), so the decision is an identity the desk was violating rather than a dial: *do not plan a book you are forbidden to hold*; PROPERTIES all exact and provable — under the cap GNM = 1 and the book is returned byte-identical (silent until it binds), the `min` with 1 makes it strictly ONE-WAY so a small book is never levered UP to the cap (same safety property as ADR-0079/ADR-0104), GNM > 0 and uniform so no name flips side and the cross-sectional shape the σ controls chose is untouched (a LEVEL control, not a view and not a re-weighting), and the multiplier is computed in exact decimal rounded DOWN (scale 12) so the scaled gross is ≤ the cap by construction and never a rounding step above it; COVERAGE — a name with no price or no contract multiplier has no assertable USD notional and enters neither the sum nor the scaling, the same fall-back-rather-than-guess rule ADR-0079 applies to an uncovered covariance, and such a name is already planned flat; NOT a de-risking change and must not be read as one (ADR-0132) — it shrinks the TARGET, not the position, so that the aim can converge and the desk can HOLD through its horizon instead of chasing, and held gross is expected to RISE toward a book the desk can defend, from $91,999.52 against a $500,000 planning cap; the deterministic floor is untouched — guardrail, firm drawdown breaker, conviction floor, edge gate and instrument cap all still stand, and this control merely makes the planner AGREE IN ADVANCE with a limit already enforced downstream; worked example by hand and encoded exactly — A +100 @ 50.00 and B -200 @ 25.00 give plannedGross 10,000.00, cap 4,000.00, GNM 0.4 exactly, A' +40.000000 and B' -80.000000, gross' 4,000.00 to the cent with the 1:1 notional ratio preserved (`GrossNotionalCapTest`); VERIFY-BY next run — from `/api/fusion/targets`, Σ |targetQty × price| over the planned book must be ≤ 500,000 (it was 1,356,452.14), with `insideBuffer` falling from 19 of 22 and the per-cycle Σ |deltaQty × price| falling materially; `-Pci test` green) |
| [0138](0138-the-sensor-seed-extends-its-read-until-the-walk-is-satisfied.md) | The sensor seed extends its read until the walk is satisfied, and names why it stopped | Implemented | ✔ Built (ADR-0071 warms the continuous sensors from durable mark history and ADR-0114 fixed the UNITS — window and hole test denominated in the step the sensor consumes a name at — but both leave the one quantity that can only be known DURING the walk estimated BEFORE it: how far back to read, fixed at `LOOKBACK_MULTIPLE(2) × samples × step` with `step` the MEDIAN inter-print gap; the median is the wrong statistic for that job and the class's own doc says why — print gaps are heavy-tailed, the median describes the TYPICAL gap while the window must cover the SUM of `samples` gaps, which the tail dominates — so the window is systematically short by an amount varying with whatever the tail did on that boot, and the existing single widening cannot rescue it because when the median already equals the poll cadence there is nothing to widen to; the walk then ran off the oldest point it READ while the stored series continued below it (12h retention against seeds spanning tens of minutes) and returned short as though the history had ENDED, which is not confined to the sensor: a cold name contributes no forecast, the combined view falls to one effective source, and the live rule plans an unestimable name FLAT — worked in full, not buffered, reproduced by the loop on five consecutive boots and priced from the app's own `fills`⋈`orders` join at 10.04% of the firm's entire LIVE fee bill with ~69% of it inside ten minutes of a restart, the tightest instance a JPM round trip opened on a three-source view and closed 27 seconds later on a one-source view with nothing between them but the sensor losing the ability to see; the fix makes the window a CONSEQUENCE of the walk — on exhausting the points read while still short the window DOUBLES and the walk repeats, until the seed fills, a genuine hole truncates it, or the series demonstrably ends, with the SHALLOWEST filling window used so the replayed horizon stays as close as the store permits to what the live sensor would have consumed; "the series ends here" is inferred from the read itself with NO new store API and no extra scan — stopping at an oldest point more than one gap tolerance newer than the `since` asked for proves nothing was held in `[since, oldest)`, so the next older point is further off than the tolerance permits and a deeper read could only have broken there anyway; the step is MONOTONE NON-DECREASING across extensions because a deeper read is stride-downsampled more coarsely by the store and a NARROWER step would fabricate outages out of the finer series' normal print gaps; every seed now reports its TERMINATOR (`FULL`/`GAP_BREAK`/`HISTORY_EXHAUSTED`/`NO_HISTORY`), the wall-clock span covered, the step walked at and the read count, logged by all four sensors on a short seed so the next cycle grades this from the app's own log instead of a replication script; NOTHING fitted and nothing per-name — the loop measured the same name seeding full on one boot and short on the next (BAC 193→192, NEE 193→181→171), so seed depth is a property of the BOOT and only re-reading against the series can answer it; NOT the routing rule — ADR-0135's "hold instead of liquidate" is graded ❌ BAD and is not re-tried, the defect is a blind sensor and the fix belongs where the blindness is; NO money/risk/exposure number introduced — this class replays PRICES into a sensor publishing a conviction, with fusion, the edge gate, the conviction floor and the pre-trade floor all still between it and a fill, and the deterministic floor untouched; termination bounded (window doubles, capped at 12 attempts, once per name per process); the genuine cash-close halt still truncates and now NAMES itself as the reason; VERIFY-BY next run — zero `sensor still cold` WARNs at boot for any name with stored marks, no `fusion exit — target decayed to flat [… sources≤1]` order within five minutes of JVM start, the `sources ≤ 1` share of `totalFees` falling materially from 10.04%, and any remaining short seed naming its terminator and span in the log; `-Pci test` green) |
| [0140](0140-the-fusion-aim-is-durable-derived-state.md) | The fusion aim is DURABLE derived state | Implemented | ✔ Built (the desk could not OPEN a position — live `insideBuffer` **19** of **20** planned names, `currentQty` **0** for most, firm gross at **0.3%** of the cap while ADR-0132 asks for capital to be DEPLOYED, and the planner producing real targets the whole time (WMT `targetQty` **-846.588220**, KO **-599.105696**, JPM **139.136530**) against which it routed nothing; the mechanism is a COMPOSITION of three individually-sound things — ADR-0080 gives the aim a time constant of one whole evidence horizon (`a = 1 − e^(−c/h)` = **0.008298…** at 30 s/3600 s, so the aim reaches only `1 − e^(−t/h)` of its target after `t`), ADR-0094's band against a flat book reduces the release condition to `|aim|/|target| > bufferFraction × TARGET_ABS / |f|` = exactly **1/|f|** at the live `0.10`/`10.0`, so first-order time is `t > −h·ln(1 − 1/|f|)` — between ~900 s and ~2400 s at the live forecasts (WMT **-4.710957**, GOOG **-2.668468**, JPM **2.334897**, CAT **2.327157**, MCD **-2.180863**, KO **-2.107082**) — and the aim was RESET far more often than that by TWO independent mechanisms: `aims.keySet().retainAll(planned)` deleted the whole intent of any name absent from a SINGLE cycle's plan (membership demonstrably churns, consecutive snapshots reading `instruments` **21** then **20**), and the map was in-memory against observed process lifetimes of **1344–1439 s**; the signature is diagnostic and rules out a warming path — six names sharing one derived rate, one band and one seed (`currentQty` **0** for every one) showed `|aim|/|target|` spanning **12.7×** (AAPL **0.017274** … CVX **0.219300**) where a common clock forces IDENTICAL ratios, and every one sat below its own `1/|f|` threshold so `deltaQty` read **0.0**; DECISION — the aim is durable derived state and BOTH resets close: absence AGES an intent over a window that is DERIVED not dialled (inverting the ADR-0080 identity, `−1/ln(1−a) = h/c` = **120** cycles at the shipped config — exactly one evidence horizon, no number introduced, invariant 7 / ADR-0016), and the map is written through each cycle to `fusion_aim` (V48) and restored once per process, `NUMERIC(20,6)` against `BigDecimal` (exact decimal, invariant 1), scoped by `feed_mode` so a sim↔live switch starts fresh (ADR-0029 / invariant 8), derived data only (ADR-0014) with `fills` still the source of truth for positions (invariant 3); SAFE BY CONSTRUCTION — a restored aim is never acted on directly, it enters `nextAim` as the previous aim, is stepped at THIS cycle's rate and is then clamped by ADR-0102's `withinTarget` into [flat, current target], so it can never exceed the current target, never oppose it and never survive a change of view (a flipped forecast clamps it to flat in one step); NO band, width, rate, conviction floor, edge gate or cap is altered and the deterministic floor — pre-trade guardrail and firm drawdown breaker — is untouched; a store failure logs and leaves the in-memory aims in force (a broken DB must never stop the desk trading) and `aimStore == null` leaves every path byte-identical to ADR-0094; NOT the alternatives — widening/capping the BAND is exactly ADR-0133, scored ❌ BAD and reverted, and is not re-attempted; raising the RATE breaks the ADR-0080 identity outright (N round trips against one horizon's return, making the edge gate over-permissive by that factor) and the path is the correct SPEED, it was being RESTARTED; persisting across restarts ALONE is rejected on this cycle's own evidence, since four of six frozen names had release requirements inside one process lifetime and still routed nothing — that is the intra-boot churn reset, so closing one without the other leaves the freeze in place, which is why the earlier framing of this item failed its own test; ageing on WALL-CLOCK is rejected as a second input where the rate already carries the horizon; RISK ACCEPTED — if the aim path is itself wrong, persisting it makes the desk wrong for longer rather than being rescued by a restart, with the ADR-0102 clamp bounding magnitude and sign and the scorer's evaluation window plus auto-revert as the backstop; VERIFY-BY next run — at least one name's `|aim|/|targetQty|` exceeding the `1 − e^(−uptime/3600)` an in-memory reseed could produce, `insideBuffer` strictly less than `instruments` with a matching `fusion entry`/`fusion exit` row in `recent_orders` in the same window, and the `|aim|/|target|` spread across names collapsing toward a common value; `-Pci test` green with five new tests — the derived window exact at 120/30 cycles and its degenerate rates, a one-cycle absence resuming at exactly where two uninterrupted cycles reach, a full-horizon absence dropping the intent, a restart resuming through a store while a flipped target still clamps the restored aim to flat, and a null store leaving the path byte-identical) |
| [0139](0139-platform-verification-and-the-author-floors-are-alternative-credentials.md) | Platform verification and the measured author floors are alternative credentials, not a conjunction | Reverted | ↩ REVERTED 2026-08-05 — `scripts/score-change.py` scored the implementing commit `d51f179a2` ❌ BAD at the close of its ADR-0116 window (the ledger row carries the computed vector, t-statistic and cycle count); the scorer's own revert hit a conflict on the loop report files and did not land, so `SocialChannels.isCredible` was restored by hand to the conjunction the next cycle and the three tests asserting the alternative-credential shape were deleted, keeping this record. The defect-level verification PASSED on six consecutive independent boots — `counters.corroborated` graded as a RATE (the counter resets at boot) ran roughly three orders of magnitude above the pre-fix reading every time, while `manipulationSuspected` kept firing, so the pump filter was not disabled by the loosening — and the objective STILL got worse, which means the corroborations it unlocked never became SIZE: social reached the `contributions[]` of no planned name in the window, so the change bought the desk EVIDENCE rather than EDGE. The unresolved question it leaves is the one this ADR's own follow-ups named — if social's expectancy is real, why is nothing letting it size? That is a question about the GATE, not about who counts as credible, and it is where the next attempt belongs; re-loosening the credibility test is NOT to be re-attempted as specified. Recorded alongside it: social's measured expectancy was itself unstable across the window, moving materially cycle to cycle on a handful of additional resolved observations because the resolved calls sit in far fewer independent cohorts than their count suggests, so the edge that motivated this ADR is suggestive rather than established and the standing owner ask on `jethro.fusion.social.per-channel` stays NOT-YET-SUPPORTED. Originally built as: (ADR-0050 §2/§4 gives an uncurated STANDARD channel author FLOORS to clear and `SocialChannels`' own javadoc promises "unknown accounts are judged by the author FLOORS below" — but `isCredible` read `verified() && followers >= floor && accountAgeDays >= floor`, and the StockTwits adapter populates `verified` from `user.official`, which marks StockTwits' OWN corporate accounts rather than asserting anything about an organic author; a live read of the same endpoint the app polls (`streams/symbol/AAPL.json`, 30 messages) returns `official: false` for ALL 30 while `followers` is fully populated and spans -2 … 5,978 with one author above the configured 5,000 floor and join dates back to 2018 clearing the 180-day floor, so the conjunction short-circuited before either floor was ever evaluated and NO organic post could be credible — the two configured, provenanced floors were dead code; the cost is that the ADR-0050 §3 corroboration gate promotes only on ≥ k=2 DISTINCT CREDIBLE channels, the social half could never supply one, and of six wired news outlets only Yahoo resolves single-name tickers (all 24 live discovery candidates, over two days and up to 206 mentions, carry `sources: [news:yahoo]` and nothing else), so one credible channel stood against a threshold of two and the funnel reads 153,207 ingested → 5,289 kept → 18 ever corroborated; and the source being silenced is the one that MEASURES — on `/api/signals/telemetry` at the 3600 s horizon `social` is the only source positive at EVERY horizon and beating the desk's own measured round-trip cost, +8.856 bps on 373 resolved calls over 37 cohorts (t = +2.01 against the 1.5 hurdle) at hit rate 0.619, versus trend +1.540 (t=+0.83), reversion -0.286 and xsreversion -5.006, against ~1.009 bps/side of fee (the app's own fills: $39.187210 on $388,348.116512140580) plus ~0.75 bps of slippage per fill (`/api/tca`), while live `/api/fusion/targets` shows `social` in the contributions of ZERO of the nine planned names and the three sources actually sizing the book are exactly the three that do not beat their own trading cost; DECISION — verification and the measured floors become ALTERNATIVE credentials, `STANDARD → verified || (followers ≥ floor && ageDays ≥ floor)`: a platform that asserts an identity supplies one credential, a platform that does not leaves the account to be judged on the track record ADR-0050 already specified; NO money/risk/exposure number introduced or altered (invariant 7 / ADR-0016) — this decides only whose mention counts as independent evidence; both floor VALUES and their provenance are unchanged, k is unchanged so corroboration still needs two DISTINCT credible channels, the `manipulationSuspected` pump tell is unchanged, and the change is strictly ONE-WAY so nothing credible before it is non-credible after; NOT the alternatives — lowering k to 1 is rejected because k ≥ 2 IS the §3 adversarial control and at k=1 a single account promotes a subject, re-mapping the adapter's `official` is rejected as the wrong layer (`official` IS StockTwits' identity assertion; the bug is requiring a credential the platform does not issue), adding single-name news outlets is DEFERRED with a stated trigger (revive if corroboration stays dominated by one outlet), and dropping `verified()` entirely is rejected because it would strip a curated platform-verified account with a small following; the deterministic floor is untouched — edge gate, conviction floor, pre-trade guardrail, firm drawdown breaker and the ADR-0086 cut all still stand between a social view and a fill, and ADR-0049 still forbids a social subject originating an order; the REAL negative is recorded honestly — social's edge rests on few INDEPENDENT episodes (18 corroborations re-emitted each cycle, which is why 373 resolved calls sit in only 37 cohorts), so t = +2.01 is suggestive not established and broader coverage may dilute it, which is precisely why it ships: the ADR-0049 telemetry and the ADR-0064 edge gate will now measure it on a real sample and shrink its weight if the edge was an artifact — the same earn-your-weight path any source takes; VERIFY-BY next run — `/api/social` `counters.corroborated` above 18, ≥1 `signals[]` row on a TRACKED name with `channels ≥ 2`, and `social` present in `contributions[]` of ≥1 `/api/fusion/targets` row, none of which has happened to date; `-Pci test` green with four new tests covering the live organic shape, each floor still binding on its own, and the news⋈social cross-corroboration that could never previously fire) |
| [0133](0133-the-no-trade-band-is-never-wider-than-the-interval-it-polices.md) | The no-trade band is never wider than the interval it polices | Implemented | ✔ Built (the desk was DORMANT at the US open holding $14,215 against its own $303,271 target book, `insideBuffer` 19/18/20/20 of 20 across four consecutive re-plans with two planning nothing at all; the ADR-0094/0101 band is `width x |target| x TARGET_ABS / |forecast|` and the target is LINEAR in the forecast, so the two `|forecast|` factors cancel and the band is the position at a FULL-strength view regardless of the current one — while ADR-0102 confines the aim it is tested against to [flat, target], an interval whose width DOES shrink with conviction; the band therefore consumed `width x TARGET_ABS / |forecast|` of that whole interval and once `|forecast| < width x TARGET_ABS` (= 1.0 shipped) exceeded it entirely, so from flat `|gap| = |aim| <= |target| < band` at EVERY aim the hour-long ADR-0080 path can reach and the delta was exactly zero forever — a permanent veto, live on 6 of 20 names (CAT 0.208, XOM 0.200, HD 0.175, PG 0.127, JNJ 0.033, KO 0.000) with the other 14 locked out for 12-50% of their target; Carver’s rule cannot produce it because his distance is `|target - current|`, which at a typical forecast IS the average position, so his band is at most `width` of what it is compared against; `PositionBuffer.band` now caps the scale at `|target|` — one `min`, strictly one-way (it can only NARROW a band, never widen one), inert at `|forecast| >= TARGET_ABS`, no number introduced (the bound is the target the planner already computed, the same provenance ADR-0102 and ADR-0132 use); aim path, ADR-0101 width, ADR-0107 rating, ADR-0118 trapped exit, ADR-0132 destination clamp and unbuffered flat-target exits all byte-identical, and the whole deterministic floor untouched; exposure and turnover are EXPECTED to rise — deliberately, per the ADR-0132 objective — and the ADR-0116 scorer is the check; full `-Pci test` green) |
| [0132](0132-the-buffers-destination-never-sits-on-the-side-the-target-opposes.md) | The buffer's DESTINATION never sits on the side the target opposes | Implemented | ✔ Built (ADR-0102 bounds the desk's INTENT to the interval between flat and the target, but ADR-0094 trades to the near EDGE of the no-trade region — a whole band below that intent — and the band is scaled by the average position at the TARGET, so early on the ADR-0080 hour-long aim path it exceeds the aim and the region straddles flat, reaching onto the side the forecast opposes; a wrong-side holding inside it was frozen at deltaQty exactly 0.000000 indefinitely (live KO long 45 vs target −18.12, WMT long 9 vs −10.23) and one outside it was steered to a destination still on the wrong side (live GOOG short 9 vs target +32.814821 stopping at −2.956534, AMZN short 29 vs +89.459992 stopping at −6.620767) — carrying gross exposure the desk's own view contradicts plus the hedge notional sized against it; ADR-0118 diagnosed the identical AAPL plan but scoped its escape to `!mayIncrease`, which is dead code with the gate off under ADR-0122 and the ADR-0126 σ sensors warm; `PositionBuffer.onTargetSide` now replaces a destination on the opposite side of flat from the target with FLAT, before the ADR-0107 rating so it stays a rated unwind and not a liquidation — band, aim path, ADR-0101 width and rating all unchanged, no number introduced (the bound is flat); proved to fire ONLY on a holding the target opposes, to resolve to |held+delta|=0 so it can never open/enlarge/side-flip, and never to trade past the aim; flat-target exits keep exact semantics; full `-Pci test` green)
| [0127](0127-every-component-reads-the-dynamic-universe-not-a-hardcoded-list.md) | Every component reads the DYNAMIC universe (refdata master), never a hardcoded security list | Implemented | ✔ Built (invariant-9 enforcement across the history/training/backtest pipeline: `HistorySeeder`, `TrainingBarsLoader` and `SimSnapshotController` now iterate `InstrumentRefSource.instrumentIds()` — the refdata master, dynamic, grown by ADR-0060 discovery — instead of the legacy `jethro.trading.sim-instruments` config list they'd all been keying off, which silently excluded every promoted name and the ADR-0125 sector expansion; `HistorySymbols` converted from a static hardcoded `PROXY` map to a `proxyFor(id, assetClass)` DERIVATION (EQUITY→ticker, FX→lowercase pair, else skip; overrides only for ES→SPY/NQ→QQQ ETF proxies and GOOG→GOOGL); new `GET /api/universe` exposes the master (id+assetClass only, invariant 2) and `scripts/fetch_bars.py` now reads it and derives Stooq symbols instead of a hardcoded SYMBOLS map — so a name has history for the OOS gate the moment it's in the master, no edit anywhere, and fetch_bars fails honestly if the app is down rather than fetching a stale list; the walk-forward history for the 12 ADR-0125 names is now reachable, unblocking their OOS validation; full Spring context loads with InstrumentRefSource injected into the changed beans, trading+training tests green) |
| [0125](0125-sector-breadth-is-the-edge-lever.md) | Breadth, not more signal tuning, is the edge lever — widen the live universe across sectors | Implemented | ✔ Built (refdata `V48__sector_breadth_equities.sql` adds 12 large/mega-cap US equities across 7 low-correlation sectors — XOM/CVX (energy), UNH/PFE (healthcare), BAC (financials), PG/KO/WMT (staples), CAT (industrials), HD/MCD (consumer disc.), NEE (utilities) — each with master row (EQUITY/USD/×1), sim/alpaca/yahoo/finnhub symbology and stylized-stated adv_usd/spread_bps/display_name, roughly TRIPLING the live equity cross-section from ~8 correlated mega-cap tech names to ~20 across sectors; the loop's rigorous verdict was that no source has edge (reversion positive but only t≈1.3) and the root cause is BREADTH not signal — by Grinold's Fundamental Law IR ≈ IC·√breadth a signal on ~8 names that move together has almost no achievable IR, and on the live Alpaca feed the effective universe WAS those ~8 (futures/FX/rates are the ADR-0123 delayed cohort, untraded live); the `alpaca` symbology row makes each name real-time (not a delayed poll) and `TradingCoreLifecycle.alpacaSymbolMap()` auto-subscribes every master name with an alpaca symbol so no config list gates them (invariant 9); breadth raises the CEILING on IR, not IC — if signals still measure ~0 across a wide diversified cross-section that is a stronger no-edge verdict and the next lever is a new predictor through the ADR-0049 OOS gate; new names trade on the live signals immediately under exploration mode (ADR-0122), OOS validation pends `fetch_bars.py`; CUSIP/ISIN (display-only, invariant 2) and V32 hedge betas deferred not invented — both in the deferred register; additive/idempotent migration, reversible via the dynamic blacklist) |
| [0126](0126-no-position-in-a-name-the-desk-cannot-stop-out.md) | No position in a name the desk cannot stop out — an unarmed trailing stop is a veto on OPENING | Implemented | ✔ Built (`PositionBuffer.mayIncrease` now vetoes an INCREASE for two independent reasons — the ADR-0064/0072 edge gate, and a name whose ADR-0086 trailing-stop σ sensor has not warmed, i.e. one `FusionLifecycle.seedVolatility` already logs at WARN as “cannot be stopped out”; the clamp is reduce-only so the desk may always cut or be stopped out of such a name but may never open or enlarge one, and the aim is re-seeded to the position it will actually hold so intent cannot accumulate behind the veto and fire as one order when the sensor warms; closes the hole ADR-0125's widening exposed, where eight names with live published targets — KO, WMT, BAC, MCD, PG, XOM, NEE, HD — had never armed a stop and the two LARGEST absolute targets in the book (KO short 101.879300, WMT short 74.222500) were both unprotected, inverting the owner's thesis that risk must be contained before it is added; deliberately evaluated INDEPENDENTLY of the edge gate, unlike ADR-0075's clamp and ADR-0118's trapped-exit escape which both sit inside `if (gate != null && !gate.mayIncrease(...))` and are therefore unreachable while ADR-0122 holds `edge-gate.enabled=false` — a control an unrelated dial can silence is not a control; applied inside the buffer because that step re-derives every delta and discards any clamp applied upstream; no number introduced — the condition is the sensor's own warm-up state (`sigmaPerSample(...).isPresent()`, the same reading `TrailingRiskCut` requires before it will cut), exact decimal, above the deterministic floor; cost accepted is that a genuinely new name is un-tradeable for its σ warm-up, with a proxy-σ estimator deferred to its own ADR) |
| [0122](0122-paper-book-exploration-mode-edge-gate-off.md) | Exploration mode for the paper book — disable the edge gate AND the backtest-support veto so the models actually act | Proposed | ✔ Built (`jethro.fusion.edge-gate.enabled=false` + `jethro.fusion.require-backtest-support=false` — TWO suppressors sat between the models' views and a fill and both had to be off for a paper book to act; the book was dormant ($0 gross, PnL frozen six runs) because the ADR-0064 edge gate held the whole desk `mayIncrease:false`, requiring a source to beat cost at ~97.7% confidence read against Student-t on cohorts−1 df, which is structurally unbeatable while a cross-sectional source sits at 2–3 cohorts for hours and no source has positive mean expectancy; on real capital that discipline is correct but on a PAPER book whose owner-stated purpose is to WATCH the models act and perfect them it is the mechanism of dormancy; disabling via the supported `gateSupplier=null` path leaves EVERY deterministic floor standing — conviction floor (min-forecast-to-route=5.0, so high-conviction not noise-churn), no-trade band, firm/book exposure caps (hard pre-trade block, now monitored with headroom), firm-drawdown breaker, and the ADR-0116 scorer that auto-reverts a PnL-dropping change — so the owner's two guardrails (don't drop PnL, don't let exposure get ridiculous) stay enforced; early actions are EXPLORATORY model views under live measurement, NOT proven edge, and the durable fix remains an OOS-validated daily momentum signal so actions become justified; min-sample/t-hurdle retained unchanged for the TelemetryWeights path and for re-enabling; next blocker if still flat is the ADR-0049/0059 backtest-support veto; fully reversible via enabled=true) |
| [0123](0123-a-closed-market-skips-the-analysis-never-the-deploy.md) | A closed market skips the ANALYSIS, never the DEPLOY — the loop deploys on any cycle that pulls in code | Proposed | ✔ Built (`ops/improve-loop.sh` — the ADR-0115 market-hours branch now takes its `BEFORE` sha BEFORE the fast-forward and calls the same deploy-and-verify step the open-market path does, both of them through one shared `deploy_if_code_changed` function instead of a block inlined at the tail of one branch; the defect is that the closed branch still fetched and fast-forwarded — so a change pushed overnight landed in the working tree — then `exit 0`d before any deploy, having captured `BEFORE` AFTER the merge, which makes the loss PERMANENT rather than merely delayed: the commit is already an ancestor of HEAD when the next open-market cycle takes its own `BEFORE`, so no later `git diff BEFORE AFTER` can ever see it and `CODE_CHANGED` is empty forever — a change arriving while the market is closed was undeployable IN PRINCIPLE, and it is exactly the bug §2 of the open-market path had already fixed for itself on 2026-07-28, left standing in the closed path; it has already cost a full session — the owner’s ADR-0122 exploration-mode directive was committed 22:24 UTC on 2026-07-28, after the 20:00 close, and twenty-odd market-closed cycles fast-forwarded it in and deployed nothing, so at the 2026-07-29 open the JVM was still the process that booted 15:35 UTC the previous day: `/api/fusion/targets` reported `edgeGate.mayIncrease:false` with a POPULATED `sources` list (i.e. `gateSupplier != null`, the running config’s gate ENABLED) while the repo said `jethro.fusion.edge-gate.enabled=false`, ten minutes into the session every `deltaQty` was exactly 0 and gross was $0.00, and the ledger meanwhile scored `68ff73eb2` ⚠️ INCONCLUSIVE over 38 cycles of a book that placed ZERO orders — ADR-0110’s manufactured-evidence failure arriving through the one door §5 did not watch; a closed market is in fact the CHEAPEST time to bounce the JVM (no tape to miss, no live measurement to wipe, hours of warm-up before the open) and strictly better than the mid-session restart that is the only alternative once a change is stranded; the `reports/`-only filter is unchanged so a heartbeat or ledger write still bounces nothing, and NO money/risk/PnL number is touched — this is the loop’s own plumbing, not the desk’s; HONEST COSTS in the ADR — the loop may now restart the app unattended overnight when no report is generated and nobody is watching, so a change that crashes on startup takes the app down until the next open-market cycle notices (the uptime check detects and logs it, nothing rolls back), and this cycle’s scored window contains TWO deployments’ worth of behaviour (this fix plus ADR-0122 finally reaching the JVM, which will dominate the vector), so its verdict attributes cleanly to neither and says so in `docs/loop-findings.md`; rejected alternatives recorded so they are not re-attempted — leaving it to the operator to pull and restart by hand (the status quo dressed as a policy, and there is no human in the loop per ADR-0063; it also failed SILENTLY, the property that made it expensive), and DEFERRED not rejected, the two better fixes already in the register — diffing against the sha the running jar was built from (ADR-0110’s build-identity row, which would also catch a restart onto a stale jar; it loses here only because it needs build stamping to ship first and the book is dormant NOW) and teaching `scripts/score-change.py` to refuse to score a commit it cannot prove was deployed (the right eventual home for the rule, kept out for the same reason ADR-0110 kept it out — the scorer is the invariant-7 authority for every money number in the ledger and must not move in the same breath as the plumbing that feeds it; this ADR makes the stranding RARER, that one makes its verdicts HONEST, and both are wanted)) |
| [0124](0124-a-view-no-second-source-corroborates-is-not-a-position.md) | A view no second source corroborates is not a position — size on the sources' DISPERSION, not on their signs | Proposed | ✔ Built (`ForecastCombiner.combine` keeps the ADR-0119 `agreement` scalar and its place in the chain but redefines it from the sign ratio `|Σwᵢfᵢ|/Σwᵢ|fᵢ|` to the sources' DISPERSION about their own average — `s² = Σŵᵢ(fᵢ − μ̂)²/(1 − Σŵᵢ²)`, the unbiased weighted variance under the Bessel/Kish correction for reliability weights, and `agreement = |μ̂|/√(μ̂² + s²) = 1/√(1 + (s/μ̂)²)`, the amplitude form of the reliability ratio, monotone-decreasing in the sources' coefficient of variation and stated in amplitude units because it multiplies a forecast; the defect it closes is that the sign ratio is 1 BY CONSTRUCTION when only one source contributes — nothing to cancel against — which ADR-0119 recorded as a safety property (“every single-source name byte-identical”) but which reads downstream as CONFIDENCE, so an uncorroborated view earned the MAXIMUM scalar in the cross-section while corroborated names were discounted below it, an inversion of exactly the breadth the ADR-0076 DM is trying to reward; diagnosed on the live book of 2026-07-29 where `/api/fusion/targets` showed META and TSLA at `sources=1`, `agreement=1.000` carrying the LARGEST convictions in the book (META −15.41 against a best-corroborated −8.44 over three sources, target −78.3 shares against a holding of 0) while the DM pushed the other way at 1.000 vs 1.155, far too small to offset 1.000 vs 0.674 — and where those same single-source names are the ones whose oversized targets churned through the 30-second re-plan without ever filling (fourteen consecutive `fusion re-plan` cancellations on TSLA against a held position of one share); the repair is that `1 − Σŵᵢ²` is the RESIDUAL DEGREES OF FREEDOM of the weighted variance and is ZERO at one effective source, so the dispersion is UNESTIMABLE — which is not the same as zero — and the honest scalar is 0, self-healing rather than a ban since any second sensor warming sizes the name again, and it also catches the n=3-but-all-weight-on-one case for free; NO NUMBER introduced and nothing to calibrate (invariant 7 / ADR-0016) — formed from the running sums the average already holds plus one bounded second pass over the same ≤5 forecasts, no new input read, the forecast layer stays dimensionless and the `TargetPlanner` money boundary is unmoved in exact decimal (invariant 1), strictly above the deterministic floor; STRICTLY ONE-WAY like its predecessors — `s² ≥ 0` ⇒ `agreement ≤ 1` so the book can only SHRINK and the sign is never flipped, with equality exactly when the forecasts are IDENTICAL — and strictly SMOOTHER than what it replaces, which matters because ADR-0119 rejected a hard threshold as a discontinuity “that would flip a name between full size and flat on estimation noise” while the sign ratio is itself such a discontinuity in disguise, moving only when a sign flips; tests pin the arithmetic by hand — `oneSourceIsNotACorroboratedView` (the live META forecast alone ⇒ agreement 0 and value 0, the all-weight-on-one variant likewise, and a second sensor on the same view restoring it to 1), `sourcesThatShareASignButNotAMagnitudeAreNotFullyCorroborated` (+10 vs +20 ⇒ 0.9045340337332908, which ADR-0119 scored 1.0), `agreementIsMonotoneInHowFarApartTheSourcesAre` (the closed form `1/√(1 + 2·gap²/100)` swept for monotonicity), `weightsTiltTheAverageAndSetTheDiversificationMultiplier` (+20@3 vs −20@1 ⇒ exactly 1/3, which the sign ratio scored 0.5) and `agreementIsOneWayAndNeverTouchesSourcesThatSayTheSameThing` sweeping the grid for bounds, shrink-only and sign preservation; HONEST COSTS in the ADR — it NARROWS ADR-0111's remedy, since standing a measured-losing source down to zero weight can leave ONE surviving view which is now sized at zero rather than at the un-diversified value ADR-0111 restored (`standingDownADisconfirmedSourceStopsItDraggingTheAverage` pins it), and better source selection therefore means fewer tradable names; every multi-source name also shrinks somewhat because sharing a sign without sharing a magnitude is no longer full corroboration, so the whole book sizes down on the first cycle and a smaller book is a slower measurement; rejected alternatives recorded so they are not re-attempted — chance-correcting the sign ratio in Cohen's-κ form `(A_obs − A_null)/(1 − A_null)` with `A_null = √(Σmᵢ²)/Σmᵢ` (parameter-free and it does fix the inversion, but `E|Σsᵢmᵢ| = m_max` at n=2 so ANY disagreement between two sources is below chance and the rule collapses to bang-bang — the very discontinuity ADR-0119 rejected, through the back door), a diffuse prior `s² ≈ μ̂²` giving one source 1/√2 (an invented number wearing a formula, and at 0.707 META is STILL the largest forecast in the cross-section — a remedy that leaves the inversion in place is not a remedy), a hard ≥2-source tradability veto (an invented cut-off in the wrong layer — the error is in one name's conviction and belongs where conviction is formed — and blind to all-weight-on-one), and punishing n=1 in the DM instead (the DM answers a BREADTH question under Carver's cap; disagreement is a CONFIDENCE question, and overloading one dial with both makes neither readable); FOLLOW-UP recorded — an empirical-Bayes prior for `s²` pooled across the names where it IS estimable would let a single-source name size at a MEASURED discount instead of zero, the right long-run answer, deferred here only because it turns a pure per-name function into one carrying cross-sectional state, which is more than one attributable cycle should change) |
| [0104](0104-the-book-carries-no-more-risk-than-it-typically-carries.md) | The book carries no more risk than it typically carries — an absolute volatility anchor | Proposed | ✔ Built (`BookVolatilityBrake`, wired in `FusionLifecycle.tick` after ADR-0083/ADR-0079 and before the ADR-0064 gate, the ADR-0086 exit and the ADR-0094 buffer — the fused book's measured ex-ante volatility `sigma_planned = sqrt(e'.Sigma.e)` over the covered names (the SAME statistic, coverage rules and covariance ADR-0079 already prices concentration with) is capped at the MEDIAN of its OWN planned-sigma series over the last `span` planning cycles, every covered target scaled by `min(1, sigma_ref/sigma_planned)` with its order delta recomputed under the cycle's own band and rate; closes the gap that EVERY size control on this desk is RELATIVE — ADR-0083 is budget-neutral by construction and ADR-0079 scales back to the independence benchmark the per-name budget already implied, so the book's risk LEVEL was `unit-notional-usd x (how many names happened to clear the edge gate) x (how strong their forecasts happened to be)`, i.e. a property of the cross-section rather than of anybody's appetite: measured firm gross read $14.5k -> $62.1k -> $43.4k -> $46.6k -> $7.9k -> $27.6k across consecutive evaluations with the desk's appetite unchanged, which makes PnL-per-unit-of-exposure unmeasurable and puts the desk's largest risk in whichever cycles the cross-section happened to shout; the empirical claim is the standard volatility-targeting result that returns are NOT proportional to risk across risk states so trimming the hot states raises return per unit of risk (Harvey, Hoyle, Rattray, Sargaison & van Hemert, JPM 2018; Moreira & Muir, JF 2017; the scaling behind Moskowitz, Ooi & Pedersen JFE 2012); NO MONEY/RISK NUMBER IS INTRODUCED — the reference is not a chosen volatility target but the desk's own typical planned risk measured on whatever stream it is trading, so the same code self-calibrates to a sim, live or replay feed with no special-casing (invariant 9) and nothing is asserted that was not measured (invariant 7 / ADR-0016); STRICTLY ONE-WAY and asserted as tests — `min(1, .)` discards the case where planned risk sits below its own median so an estimated covariance can only ever shrink the desk, the multiplier is positive and uniform so no name flips side and the cross-sectional shape is untouched, the series samples the RAW pre-brake sigma so a cut cycle cannot drag the reference down and ratchet the book to zero, and it can NEVER set a target flat (the ADR-0086 cut, the ADR-0065 unwind, the ADR-0027 breaker and the pre-trade guardrail keep their exact semantics); silent below `min-sample`, on an incomplete covariance or on a degenerate sigma, leaving the book byte-identical; exact worked examples pinned as tests (one name at 100.00 x 1,000 units = $100,000 at 2%/day => sigma = $2,000/day; series {2000,2000,2000,8000} => median $2,000 => brake 8000 -> 0.25 => the 4,000-unit target scaled to exactly 1,000.000000 units and the braked book at sigma = $2,000 = sigma_ref precisely, by homogeneity of degree 1; two equicorrelated names at rho = 0.5 => 2000.sqrt(3) = $3,464.10/day); dials `span=120` (one hour at the 30s cadence, the SAME window `risk-cut.vol-span` and `stream-covariance.span` already use) and `min-sample=30` (the house `edge-gate.min-sample` convention, also one ADR-0080/0082 holding horizon of planning history) are estimation conventions and the control is scale-invariant in sigma, so neither can move a size by itself; deferred: persist the sigma series so the brake is not cold for 15 minutes after each redeploy, and anchor the LEVEL to a declared appetite rather than only the dispersion — both in the deferred register) |
| [0102](0102-the-desk-never-intends-past-or-against-its-own-target.md) | The desk never intends past — or against — its own target | Proposed | ✔ Built (`PositionBuffer.withinTarget` — the ADR-0080/0094 aim is clamped into the closed interval between FLAT and this cycle's target, `aim' = min(max(aim, min(0,T)), max(0,T))`, applied after the rate step and before the ADR-0101 band is measured against it; closes the gap that the aim recursion `aim + a(T − aim)` unrolls to an EWMA of the target sequence — a convex combination of PAST targets, which is NOT the hull of the current one, so whenever the target moves faster than `1/a` the intent can OUTGROW it (live EURUSD: intent −19268.293125 against a target of −13006.790342, 6261.502783 units ≈ $6,789 of notional the planner never asked for) or INVERT against it (live JNJ: intent +8.043404 against a target of −219.420787 while the book held +104, the desk's largest position long in the name its own strongest forecast said to be short, with the aim path walking it FURTHER long; AUDUSD −11494.134880 against +17285.020487 and GBPUSD −2024.437007 against +19015.931789, both about to OPEN the wrong way round from flat) — neither of which partial adjustment means, since Gârleanu & Pedersen's aim (JF 68(6) 2013 §III) is a weighted average of the CURRENT and EXPECTED FUTURE targets, every element a position the model wants, not of a realised past; the correction is worth ~6:1 on the desk's own measured numbers — being the wrong side of the passing source swings ±8.53 bps per 900s horizon against a 2×1.43 bps round trip, the same arithmetic ADR-0101 used for the width; STRICTLY ONE-WAY with both properties asserted as tests, `|aim'| ≤ |aim|` and `sgn(aim') ∈ {0, sgn(T)}`, so it can never open, enlarge or side-flip a position and every trade it newly admits moves the book toward flat or toward the target's own side; NO new dial and NO new number — the bound is the target the planner already computed — and rate, band width (ADR-0101), band location (ADR-0094), volatility budget (ADR-0083) and portfolio normaliser (ADR-0079) are untouched; the FLAT-TARGET branch returns BEFORE the clamp so the ADR-0086 chandelier cut, the ADR-0065 orphan unwind, the ADR-0027 breaker and the pre-trade guardrail keep their exact semantics; an inversion is cut ONCE to flat and the position then rebuilt through the buffer at the derived rate rather than round-tripped, exact-decimal worked example pinned as a test (JNJ: step 93.398… ⇒ clamped to 0 ⇒ sell −104.000000; next cycle aim = −7.193469 against a band of 219.420787 × 10 / 12.64 × 0.10 = 17.359240 ⇒ NO ORDER); feed-agnostic by construction since the bound is the desk's own target (invariant 9)) |
| [0101](0101-the-no-trade-buffer-is-the-desk-s-own-cost-to-edge-ratio.md) | The no-trade buffer's WIDTH is the desk's own cost-to-edge ratio, not a published convention | Proposed | ✔ Built (`PositionBuffer.widthFor` + `passingEdgeBps` — each name is buffered at `max(carverFraction, min(1, 2C/mu))` where `C` is that name's OWN measured round-trip cost in bps from the ADR-0072/0075 map, the desk blend for a name never filled exactly as `EdgeGate.Decision.mayIncrease` charges it, and `mu` is the GROSS expectancy in bps of the best-evidenced source that CLEARS the gate at the ADR-0082 rung the evidence selected — both already inside the `EdgeGate.Decision` the buffer is already handed, so no new input, no new dial and no new number; `2C/mu` is the no-trade half-width the desk's own arithmetic implies — closing a gap `g` is worth its cost when `1/2.lambda.sigma^2.g^2 > C.|g|`, i.e. `|g| > 2C/(lambda.sigma^2)`, and the aim's own first-order condition `a = mu/(lambda.sigma^2)` turns that into `band = a.(2C/mu)`, so the risk-aversion parameter this desk has never stated CANCELS and never has to be invented (Grinold & Kahn 2e ch.16; the traded-to-the-near-edge policy shape is unchanged and is the one proportional costs call for, Constantinides JPE 1986 / Davis & Norman Math.OR 1990); closes the gap that ADR-0094 fixed WHAT the band is measured against but left its WIDTH at Carver's published 0.10 — a convention is what you use when you cannot measure, and this desk measures both inputs every cycle for the edge gate: live, one source clears (`reversion`, 8.82 bps over 500 resolved on 65 cohorts, t = 9.3 at the 900s rung, every other source measured significantly NEGATIVE and pinned at the ADR-0097 MIN weight) against 0.49-1.01 bps of measured one-way slippage plus 1 bp of fee a side, so the implied width is several times 0.10 and the desk filled 2,254 orders in a day to hold an $18.1k book, paying $355.89 of ALPHA fees against $879.66 of ALPHA P&L — two fifths of what the strategy books make handed to the cost of getting there; STRICTLY ONE-WAY — the measured width is used only where it is WIDER than the convention and is capped at one average position, so it can only ever suppress a trade the desk would have made, never add or enlarge one; the AIM PATH IS UNTOUCHED quantity for quantity so intended exposure is unchanged by construction and this is a P&L improvement at constant risk; an EXIT is still not buffered at any width (a flat target snaps the aim to zero and trades in full), so the ADR-0086 cut, the ADR-0065 unwind, the ADR-0064 reduce-only clamp and the deterministic floor — pre-trade guardrail and firm breaker — are untouched; no gate, no passing source, a non-positive measured edge or a non-positive measured cost all assert NO width and leave Carver's convention exactly as today (invariant 7 / ADR-0016); `mu` is GROSS not net because the cost is charged once on the `C` side; feed-agnostic by construction since both inputs are measured off the running stream (invariant 9); exact-decimal worked examples pinned as tests (`C = 2.0`, `mu = 8.0` ⇒ width 0.50 exactly; target 200 at forecast 16 ⇒ average position `200 x 10/16 = 125` exactly, band `62.500000` measured against `12.500000` conventional, and the aim path `0 → 6.557800 → 12.898603` at the derived rate is outside the one and inside the other, the conventional band trading `0.398603` every 30s and the measured one nothing; `GOOGL` at 20.11 bps caps at 1.0; a name absent from the measured map takes the blend, `2 x 1.48/8.0 = 0.37`)) |
| [0100](0100-hedge-tracks-its-target-at-that-target-s-own-directional-efficiency.md) | The hedge closes the gap to its target at that target's own directional efficiency | Proposed | ✔ Built (`HedgeTargetChurn.efficiencyRatio` — Kaufman's efficiency ratio in exponential form, `E = |EWMA(step)| / EWMA(|step|)` at the same `λ = 0.94` RiskMetrics decay `CovMath` and the ADR-0098 `σ` already use, on the same raw-target series sampled once per hedge cooldown by the executing loop; both recursions start at ZERO rather than seeded so the `(1−λⁿ)` warm-up factor is identical in numerator and denominator and cancels exactly in the ratio, making `E` unbiased from its first reading — `n` steps of `+x` read 1 exactly, a perfect `+x,−x` round trip reads `(1−λ)/(1+λ) = 0.0309`, and `+x,−x,+x` reads 0.334183 against Kaufman's 0.3333 over the same window — plus `HedgeAdvisor.trackTowardTarget`, which closes only `E` of the gap `d − h` and ONLY when the move grows the overlay, the ADR-0080 asymmetry applied to the hedge path where it was missing; closes the RATE half of the problem ADR-0098 opened on the LEVEL, which the live cycle shows is the half left: the raw target is `−$6,039.12` against `σ_step = $958.51`, i.e. 6.3σ, so the ADR-0098 shrink removes only 16% and the overlay is free to chase a large target that WANDERS ACROSS ZERO because it is minus the net of a book that re-signs its names — eleven consecutive cooldowns sent BUY/BUY/BUY/BUY/BUY/SELL/SELL/SELL/SELL/SELL/BUY totalling 0.0545 ES traded to end up holding −0.00494, while the HEDGE book bled `−323.33 → −451.51 → −509.21 → −550.99 → −599.22` over five scored cycles and handed back 51% of what the strategy books made; the loss is directional not execution — `$42.75` of fees at a measured 0.204 bps round trip — because `−Σβ·E` on a mean-reversion book is a MOMENTUM position on ES, the one view the desk measures significantly negative; STRICTLY ONE-WAY — `q` lies on the segment between `h` and `d` in every branch so `|q| ≤ |d|` whenever the move grows the overlay, `E = 1` reproduces the prior behaviour exactly, and an estimated rate can only ever leave the hedge smaller than ADR-0098 already allows; ADR-0069's unwind promise is verbatim intact since a reduction — including a full unwind and the free leg of a move crossing flat — still trades in one cycle; a target genuinely going somewhere reads `E → 1` and is hedged in full, so the protection is present exactly when systematic exposure is actually building; NO new dial and NO new number, `E` is a ratio of the stream to itself so it self-calibrates on any feed (invariant 9), and the deterministic floor — pre-trade guardrail and firm breaker — is untouched) |

## Status vs Implementation

**Status** is the ADR lifecycle (Proposed / **Implemented** / Accepted / Superseded); **Implementation**
is what is actually in the code — the two diverge, which is why this column exists. Several ADRs are still
*Proposed* yet fully built (0025, 0026, 0027, 0043), and some *Accepted* ones are only partial.

**Implemented** is the status of an ADR authored by the continuous-improvement loop: the loop builds,
tests green, and deploys the change in the same commit as the ADR, so the record is of a change already
running — not a proposal awaiting a build. It still awaits owner ratification (Oleg may accept or reverse
it); *Implemented* asserts "the code is live", *Accepted* asserts "the owner has signed off". A human
authoring an ADR before building still uses *Proposed*.

**Implementation legend:** ✅ built & in the codebase (tests where applicable) · ◐ core built, parts
deferred · ○ accepted but no code yet · ⏸ deferred behind a stated trigger · ✖ superseded/rejected ·
— decision-only (no code artifact).

### Partial / not-built — what's missing (details in `deferred-register.md` / `gap-register.md`)

- **0005 Aurora PG** — Postgres + Flyway built (dev compose); managed **Aurora** is the prod shape,
  behind a production trigger (ADR-0013).
- **0007 Fargate/CDK** — `infra/` CDK **dev** stack exists; the Fargate/ALB/Aurora **prod** shape is
  not deployed.
- **0010 AI SPI** — inference SPI + **local Ollama** tier built; the **external frontier tier** is
  behind cost/latency triggers, not wired.
- **0011 FinOps** — module scaffolded only (no Java yet); **planned**.
- **0014 trading-core** — in-process ring buffer + **LMDB** warm/dedupe built; the **S3 Parquet
  write-behind tick archiver is NOT built**.
- **0035 RAG** — embeddings + outcome memory built; the **pgvector** store is still in progress.
- **0037 clean/comprehensive P&L** — the split + realized-FX translation are built; **unrealized-FX
  decomposition** and the Avro-event fields are deferred.
- **0038 proxy hedging** — **EQUITY** axis built; the **DV01-neutral rates** axis and **direct-FX**
  axis are unbuilt.
- **0039 hedge lifecycle** — advisory modes built; **AUTO order submission** + the firm-breaker
  one-shot de-risk are unbuilt.
- **0040 structural hedging** — the structural **sector tier** is built; per-sector **ETF proxies** +
  assigned-vs-realized beta telemetry are unbuilt.
- **0045 news advisory** — real **RSS news** + the **universe-discovery** register are built (via
  0050 §7); the **SLM sector-classify** step and the full queued pipeline are not built.
- **0046 no sim archive** — a scoping decision that only applies **once the 0014 archiver exists**;
  nothing to build until then.
- **0048 persist strategy actions** — **not started** (the Strategy-actions UI panel is still
  in-memory / lost on restart).
- **0050 social** — the adversarial pipeline + StockTwits/Telegram adapters + discovery are built
  (Phase 1 / 2a / 2b); **SLM classify (P3)** and outcome-scored **channel ranking (P4)** are not.

## Lifecycle

`Proposed` **or** `Implemented` → `Accepted` (or `Rejected`) → possibly `Superseded by ADR-XXXX`.

An ADR is **Proposed** when a human writes it before building. It is **Implemented** when the
continuous-improvement loop authors it, because the loop builds, tests green, and deploys the change in
the same commit — the record is of a change already running. Either way it becomes **Accepted** once the
owner (Oleg) signs off (`Implemented` means "the code is live", `Accepted` means "the owner ratified it").
Never edit the decision of an Accepted ADR — write a new ADR that supersedes it.
