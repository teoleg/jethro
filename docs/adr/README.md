# Architecture Decision Records

Index of ADRs for the Jethro trading platform. See [template.md](template.md) for the format.

| # | Title | Status | Implementation |
|---|-------|--------|----------------|
| [0001](0001-record-architecture-decisions.md) | Record architecture decisions | Accepted | ✅ Implemented |
| [0002](0002-backend-language-java-with-cpp-hot-paths.md) | Backend in Java 21, C++ reserved for latency-critical hot paths | Accepted | ✅ Implemented |
| [0003](0003-event-driven-service-architecture.md) | Event-driven services around a streaming backbone | Accepted | ✅ Implemented |
| [0004](0004-messaging-backbone-kafka.md) | Kafka (Amazon MSK) as the messaging backbone | Superseded by 0012 | ✖ Superseded |
| [0005](0005-data-storage.md) | Aurora PostgreSQL for state, Kafka + S3 for tick history | Accepted | ◐ Partial |
| [0006](0006-ui-stack.md) | UI in TypeScript + React (Vite), streaming over WebSocket | Superseded by 0028 | ✖ Superseded |
| [0007](0007-aws-runtime-ecs-fargate-cdk.md) | Runtime on ECS Fargate, infrastructure as code with AWS CDK (Java) | Accepted | ◐ Partial |
| [0008](0008-domain-model-books-instruments-positions.md) | Domain model: books, instruments, positions, marks | Accepted | ✅ Implemented |
| [0009](0009-market-data-provider-abstraction.md) | Market data provider abstraction | Accepted | ✅ Implemented |
| [0010](0010-ai-algo-engine-embedded-vs-external-model.md) | AI-driven algo engine — model inference SPI, external API first, embedded behind triggers | Accepted | ◐ Partial |
| [0011](0011-finops-cost-monitoring-in-platform.md) | FinOps in the platform — finops-service, tagged AWS costs, Costs tile in the UI | Accepted | ○ Not started |
| [0012](0012-redpanda-backbone-idempotent-consumers.md) | Redpanda as the Kafka-API backbone; at-least-once + idempotent consumers | Accepted | ✅ Implemented |
| [0013](0013-dev-cost-posture-single-node-aws.md) | Dev cost posture — one EC2 node runs the compose stack; managed services behind triggers | Accepted | ✅ Implemented |
| [0014](0014-trading-core-in-process-market-path.md) | Fuse the market path into trading-core — in-process ticks, durable log for transactions only | Accepted | ◐ Partial |
| [0015](0015-single-jvm-modular-monolith.md) | Single-JVM modular monolith — one app now, extraction seams preserved | Accepted | ✅ Implemented |
| [0016](0016-local-slm-tier-agentic-elements.md) | Local SLM inference tier — Ollama adapter first, agentic elements pulled forward | Accepted | ✅ Implemented |
| [0017](0017-attention-first-ui.md) | Attention-first UI — agents curate with a deterministic floor; grids become drill-down | Accepted | ✅ Implemented |
| [0018](0018-ai-trade-suggestions-human-in-loop.md) | AI trade suggestions — frontier tier proposes, deterministic guardrails gate, human executes | Accepted | ✅ Implemented |
| [0019](0019-simulated-auto-execution.md) | Simulated auto-execution — deterministic strategy may auto-trade in sim, hard-gated off real brokers | Accepted | ✅ Implemented |
| [0020](0020-enriched-risk-model.md) | Multi-asset quant foundation — OpenGamma Strata as the analytics substrate, exact money ledger on top | Accepted | ✅ Implemented |
| [0021](0021-operational-chat.md) | Operational chat — SLM parses the question, deterministic code answers, every turn audited to Postgres | Accepted | ✅ Implemented |
| [0022](0022-llm-hypothesis-layer-bounded-autonomy.md) | LLM hypothesis layer — model synthesises structured theses, quant layer computes, a deterministic risk envelope decides auto-execute vs human approval | Accepted | ✅ Implemented |
| [0023](0023-yahoo-market-data-adapter.md) | Yahoo Finance market-data adapter — a free, delayed, dev/demo-only provider behind the ADR-0009 port | Accepted | ✅ Implemented |
| [0024](0024-finnhub-realtime-market-data.md) | Finnhub real-time market data — free WebSocket equities feed, composed with Yahoo/sim for the rest | Accepted | ✅ Implemented |
| [0025](0025-realistic-execution-cost-model.md) | Realistic simulated execution — spread/fee cost model, working-order matching, cancel/TIF | Proposed | ✅ Implemented |
| [0026](0026-correlated-factor-market-simulator.md) | High-fidelity market simulator — correlated cross-asset factor model, regime switching, calibrated from real history | Proposed | ✅ Implemented |
| [0027](0027-evaluation-and-risk-rigor.md) | Evaluation & risk rigor — hypothesis outcome scoring, out-of-sample backtests, VaR + firm breaker, day boundary | Proposed | ✅ Implemented |
| [0028](0028-ui-static-pages-react-deferred.md) | UI stays server-served static pages; React deferred behind concrete triggers | Accepted | ✅ Implemented |
| [0029](0029-runtime-feed-switching.md) | Runtime feed switching and hard sim/live/replay data separation | Accepted | ✅ Implemented |
| [0030](0030-schema-registry-serde.md) | Schema-registry serde — writer-schema resolution on the wire | Accepted | ✅ Implemented |
| [0031](0031-sim-control-panel.md) | Sim control panel — live UI dials over the simulator for model testing | Accepted | ✅ Implemented |
| [0032](0032-empirical-sim-volume-liquidity.md) | History-anchored market simulation (sim price engine) | Accepted | ✅ Implemented |
| [0033](0033-flow-aware-algos-and-depth.md) | Flow-aware algorithms (volume confirmation + liquidity sizing) and synthesized depth | Accepted | ✅ Implemented |
| [0034](0034-news-driven-sim-shocks.md) | News-driven sim shocks — news moves the tape (correlated volume surge + bp momentum) | Accepted | ✅ Implemented |
| [0035](0035-rag-for-ai-layer.md) | Retrieval-augmented context for the AI layer — local embeddings + pgvector | Accepted | ◐ Partial |
| [0036](0036-ai-signal-balance.md) | AI signal balance — measure directional skew, feed it back to the model, never quota | Accepted | ✅ Implemented |
| [0037](0037-clean-vs-comprehensive-pnl.md) | Clean vs comprehensive P&L — lock realized FX at booking, break out translation | Proposed | ◐ Partial |
| [0038](0038-minimum-variance-proxy-hedging.md) | Minimum-variance proxy hedging — beta to index futures, DV01 to Treasury futures, direct FX | Proposed | ◐ Partial |
| [0039](0039-hedge-lifecycle-band-triggers.md) | Hedge lifecycle — always-flat deterministic hedging, advisory-first | Proposed | ◐ Partial |
| [0040](0040-structural-sector-hedging.md) | Structural sector hedging — a history-free fundamental floor under the statistical hedge | Accepted | ◐ Partial |
| [0041](0041-estimation-rigor.md) | Estimation rigor — covariance burn-in, par-curve bootstrap, honest VaR windows | Accepted | ✅ Implemented |
| [0042](0042-hedge-proxy-selection.md) | Hedge proxy selection — best measured proxy, tradability-gated, switch hysteresis | Accepted | ✅ Implemented |
| [0043](0043-per-instrument-strategy-selection.md) | Per-instrument strategy selection from out-of-sample results (no blind global algo) | Proposed | ✅ Implemented |
| [0044](0044-regime-aware-strategy-selection.md) | Regime-aware strategy selection from a price-derived trend detector (no regime oracle) | Accepted | ✅ Implemented |
| [0045](0045-news-advisory-signals.md) | News-driven advisory signals — multi-source RSS → queued SLM sector-classify → bounded, audited overlay | Accepted | ◐ Partial |
| [0046](0046-no-sim-tick-archive.md) | Don't archive SIM ticks — a seeded sim run is reproducible, not stored | Proposed | ⏸ Deferred |
| [0047](0047-postgres-vs-lmdb-role-split.md) | Keep PostgreSQL as the system-of-record; LMDB stays derived-only (re-evaluation) | Proposed | — n/a (decision) |
| [0048](0048-persist-strategy-actions.md) | Persist deterministic strategy actions (entries/exits + reasons) so the UI survives restart | Proposed | ○ Not started |
| [0049](0049-ai-signals-never-order-backtest-gated.md) | AI/news signals never place orders — the deterministic backtest is a hard gate | Accepted | ✅ Implemented |
| [0050](0050-social-media-adversarial-source.md) | Social media as an adversarial source — spam/credibility/corroboration controls | Accepted | ◐ Partial |
| [0051](0051-price-derived-volatility-regime.md) | Risk-off sizing from a price-derived volatility regime (no sim-regime oracle) | Accepted | ✅ Implemented |
| [0052](0052-live-strategy-tuning-panel.md) | Live, DB-persisted strategy tuning — every dial editable, every change audited as its own provenance | Accepted | ✅ Implemented |
| [0053](0053-learned-advisory-signal.md) | Learned advisory signal — a model predicts a tradeable label, backtest-gated (no price-curve oracle) | Accepted | ◐ Partial |
| [0054](0054-event-keyed-hypothesis-identity.md) | Event-keyed hypothesis identity — the model classifies the catalyst, deterministic code dedups on it | Accepted | ✅ Implemented |
| [0055](0055-signal-fusion-target-portfolio.md) | Signal fusion and a target portfolio — signals stop placing orders | Accepted | ✅ Implemented |
| [0056](0056-multi-source-market-data-coverage.md) | Multi-source market-data coverage — source precedence via a freshness-guarded merge | Accepted | ✅ Implemented (guard + Yahoo fallback + Alpaca; no priority table — freshness subsumes it) |
| [0057](0057-intraday-strategy-family.md) | Intraday strategy family — VWAP-deviation reversion first, pairs deferred | Proposed | ☐ Not started |
| [0058](0058-var-partial-history-coverage.md) | VaR coverage — include names with sufficient history, revalue on the common window | Proposed | ✅ Implemented (Oleg-directed batch) |
| [0059](0059-fusion-execution-gate.md) | Fusion execution gate — trade only OOS-validated, convicted names (stop the churn) | Proposed | ✅ Implemented (Oleg-directed batch) |
| [0060](0060-dynamic-discovery-driven-universe.md) | Dynamic discovery-driven universe — daily promotion into the refdata master, provisional refdata | Accepted | ✅ Implemented (gate + dry-run proposer; runtime refdata write path + eviction; a promoted name is a first-class tradable instrument in the refdata universe — no monitor-only; enabled. Owner dropped monitor-only probation) |
| [0061](0061-paper-execution-any-feed.md) | Paper auto-execution runs on any feed (amends ADR-0019's sim-only routing) | Accepted | ✅ Implemented (removed the `feedMode==SIM` veto in FusionExecutor + AUTO hedging; execution stays internal SimulatedExecutor — paper on real marks; ADR-0015 is the real-money guard) |
| [0062](0062-live-edge-gated-execution.md) | Gate execution on positive *measured live* edge, not only sim-OOS | Proposed | ◐ Partly built by ADR-0064 (the reduce-only, cost-aware half is live in fusion). Still open: the stricter per-`(name, algo)` positive-edge requirement to route at all |
| [0063](0063-continuous-improvement-agent.md) | Continuous improvement agent — automate the observe→diagnose→fix→rerun loop, gated on the PnL/exposure objective | Proposed | ◐ Scaffolding landed, inert (Claude Code headless on Max; one local cycle report→analyse→change→test→commit→push→rebuild+restart via `ops/loop-control.sh`; `report.md`+logs added to system-report.py). Never edits the deterministic floor (invariant 7 / ADR-0016); real money stays behind ADR-0015. Not enabled — awaits owner Accept + `loop-control.sh on`. Open: open-position restart safety; logs-in-bundle done) |
| [0064](0064-cost-aware-edge-gate.md) | Gate risk-increasing fusion trades on measured edge net of measured cost | Proposed | ✔ Built (fusion goes reduce-only unless a source's measured expectancy beats measured round-trip slippage with significance; monotone — it can only remove trades — and self-healing as the measurements move) |
| [0065](0065-target-book-spans-held-positions.md) | The fusion target book spans held positions, and a risk-reducing delta needs no conviction | Proposed | ✔ Built (a held name with no fresh forecast is planned flat and worked down at the existing adjustment rate; reducing deltas skip the ADR-0059 conviction floor and the ADR-0049 support veto; hedge book excluded; the deterministic floor — breaker + guardrail — untouched) |
| [0066](0066-ewmac-trend-forecast-source.md) | A continuous, self-calibrating trend sensor (EWMAC) as a fusion forecast source | Proposed | ✔ Built (`trend` publishes a vol-normalised, efficiency-ratio-weighted crossover per name into the forecast registry every cycle and records its calls in the phase-1 telemetry; self-normalising so it is scale- and feed-agnostic; no gate relaxed — edge gate, conviction floor, backtest veto, guardrail and breaker all still upstream of any fill) |
| [0067](0067-expectancy-weighted-source-trust.md) | Weight fusion sources by measured expectancy, not by hit rate | Proposed | ✔ Built (source trust is Φ of the source's own t-statistic of expectancy, replacing the floored hit-rate advantage that read every below-coin-flip source as identical; credibility shrinkage, min-sample floor and MIN/MAX bound unchanged; Σ-normalised, so it rotates conviction between sources and cannot resize the book) |
| [0068](0068-equity-relative-firm-risk-budget.md) | Equity-relative firm risk budget & bleed cutoff — the danger-cut machinery exists (guardrail + firm breaker) but its loss/drawdown dials are big-book absolutes that go toothless on a reset | Proposed | ✗ Not built — the dials are the deterministic floor, which the improvement loop may not edit; a drafted config recalibration was reverted unshipped. Both steps are Oleg's to ratify (see the ADR's status note) |
| [0069](0069-scale-relative-hedge-no-trade-band.md) | The hedge no-trade band is scale-relative, not an absolute dollar floor | Proposed | ✔ Built (`HedgeAdvisor.noTradeBand` = min(ADR-0039 $10k guard, 25% × max(|target|,|held|) notional), so an absolute guard can no longer strand a hedge smaller than itself; large-book behaviour bit-for-bit unchanged, and a zero target is now always reachable) |
| [0070](0070-range-reversion-forecast-source.md) | A continuous mean-reversion sensor (range position) as a fusion forecast source — the chop-regime counterpart of the ADR-0066 trend sensor | Proposed | ✔ Built (`reversion` publishes a bounded Donchian range-position reading per name, faded and weighted by `1 − ER` so it refuses to stand in front of a trend; self-normalising and scale-/feed-agnostic; records its calls in the phase-1 telemetry so it must earn its measured expectancy — deliberately NOT the trend source negated; no gate relaxed) |
| [0071](0071-sensor-warm-restart-from-mark-history.md) | Continuous forecast sensors warm-restart from the durable mark history instead of re-learning the stream after every redeploy | Proposed | ✔ Built (`SensorWarmup` replays the LMDB `md.marks` series into the `trend` and `reversion` sensors on first sight of a name, thinned to the sensor's own cadence and stopping at a hole; seed prices are never recorded as telemetry calls, and the store is now namespaced by feed mode per invariant 8 — no sizing, gate or floor changed) |
| [0072](0072-per-name-execution-cost-in-the-edge-gate.md) | Execution cost is a per-name property — once a source clears the desk-wide ADR-0064 hurdle, each name is re-tested against its own measured round-trip cost before risk may be put on in it | Proposed | ✔ Built (the gate carries the passing source's gross expectancy plus the per-instrument measured round trip from the ADR-0025 TCA table, and fusion clamps a name to reduce-only when its own round trip costs more than that edge; unmeasured and rate-quoted names are never vetoed on an assumed cost; no new dial, no gate loosened, deterministic floor untouched) |
| [0073](0073-feed-mode-scoped-daily-close-series.md) | The daily close series is feed-mode scoped — a handover between feeds is not a market move | Proposed | ✔ Built (`daily_close` gains `feed_mode`, PK `(day, instrument, feed_mode)`; both session writers stamp `Provenance.mode()` and the bootstrap history is tagged `SEED`; `DailyCloseSeries` admits only the running mode + seed and takes a return only within one stream, so VaR and per-name vol stop measuring a feed handover as a ±40–75% market day; V47 re-tags history deterministically from `firm_equity.feed_mode`; no risk formula, dial or gate changed) |
| [0074](0074-credibility-counts-the-sample-the-estimate-was-made-from.md) | Source trust is shrunk over the sample its estimate was made from — credibility counts RESOLVED observations (flats included, as the expectancy itself does) and the hard min-sample floor is removed | Proposed | ✔ Built (`TelemetryWeights` credibility switches from wins+losses to `resolved`, so a flat-heavy source is no longer permanently "thin"; the 1.0 floor — which was strictly MORE trusting than the shrunk value for every below-average source, laundering measured-negative evidence into neutral — is gone, leaving Bühlmann `shrinkage-k` as the single continuous thin-sample dial; `jethro.fusion.weights.min-sample` removed; weights are still ratios normalised downstream, so conviction rotates and the book cannot be scaled; deterministic floor and every gate untouched) |
| [0075](0075-per-name-cost-on-both-sides-of-the-edge-gate.md) | The edge gate tests every name against its own measured round-trip cost on BOTH sides — the desk-wide verdict is the same test at the cheapest round trip the desk can actually pay, not a blended hurdle | Proposed | ✔ Built (`EdgeGate` evaluates `(avgReturnBps − cost_n)/stdErrorBps ≥ tHurdle` per name, with an unfilled name charged the desk's measured blend so its bar is unchanged; strictly tighter than the ADR-0072 raw-mean veto for any name costing more than the blend, and only a name measured cheaper than the blend can gain permission; a measured-negative source still clears nothing at any cost; no new dial, deterministic floor untouched) |
| [0076](0076-diversification-multiplier-on-the-weights-actually-used.md) | Size the diversification multiplier from the concentration of the weights actually used, not from the source count | Proposed | ✔ Built (`ForecastCombiner` computes `DM = 1/√(Σwᵢ² + ρ(1−Σwᵢ²))` over the normalised weights — the count formula evaluated at the inverse-Herfindahl EFFECTIVE number of sources; identical to the old rule at equal weights (cold start, `weights.mode=equal`), never larger than it by Cauchy–Schwarz, and never below 1; a source held at the MIN floor for measured-negative expectancy no longer buys a full extra unit of diversification leverage; amends the ADR-0067 invariant to "re-weighting can only SHRINK the book"; no new dial, deterministic floor untouched) |
| [0077](0077-expectancy-standard-error-across-emission-cohorts.md) | Measure a source's expectancy standard error across emission cohorts (Fama–MacBeth), not across observations — a whole cross-section emitted in one burst is ONE draw of the market, not 23 | Proposed | ✔ Built (`SignalScoring.aggregate` groups resolved observations into cohorts by entry time within `jethro.signals.cohort-window-seconds`, takes the point estimate as the mean of cohort means and `stdErrorBps = sd(cohort means)/√B`, and reads 0 when B<2; measured 2.23× understatement on the live `trend` cohorts (t −5.25 → −2.31) and `reversion`'s entire +4.03 bps reading is one cross-section; identical to the i.i.d. estimator for a staggered emitter and identical in the point estimate for equal-sized cohorts; `resolved`/`minSample`/`tHurdle` unchanged; strictly widens the standard error, deterministic floor untouched) |
| [0078](0078-fusion-sizes-in-the-instruments-own-contract-terms.md) | The fusion order path sizes and rounds in the instrument's OWN contract terms — cash-at-risk becomes a quantity by dividing by `price × contractMultiplier`, the money value of one unit, not by price | Proposed | ✔ Built (`TargetPlanner.targetQuantity` divides by `price × multiplier` so `|qty| × price × multiplier` — the same expression `PositionRisk` uses for `netExposure` — equals the `unitNotional` asked for in every asset class; `FusionPlanner`/`FusionLifecycle` gain a `multiplierFor` lookup wired to `InstrumentRefSource`, and an unknown spec plans FLAT rather than sizing as if it were a share; `TargetPlanner.tradableQuantity` rounds toward zero at whole units for multiplier 1 (byte-identical for every equity and FX name) and at the order/fill quantity scale for a CONTRACT, which is what the ADR-0039 hedge advisor already submits on ES; equities and FX unchanged exactly, futures/bonds/swaps no longer carry 50×–80,000× the intended exposure; no new dial, deterministic floor untouched) |
| [0079](0079-portfolio-diversification-multiplier-on-the-target-book.md) | Scale the fused target book by its MEASURED portfolio diversification, not name by name — a cross-section that is really one bet must not carry N per-name budgets of it | Proposed | ✔ Built (`PortfolioRiskNormaliser` computes `PDM = min(1, σ_indep/σ_actual)` over the EWMA(λ=0.94) daily-return covariance `CovMath` already prices parametric VaR and the ADR-0038 hedge advisor with, and scales every covered target before the edge gate clamps; `σ_indep` is the risk the per-name `unit-notional-usd` dial ALREADY implies, so no book-level money number is introduced and Oleg's dial keeps its meaning; uncorrelated names give PDM 1 and a byte-identical book, N perfectly correlated names give 1/√N, the cap at 1 means an internally-hedged book is never levered up (the ADR-0076 one-way property), and the uniform positive scale flips no name's side; an uncovered name enters neither the sums nor the scaling — no measurement, no claim; `portfolioRiskMultiplier` + `covarianceCoveredNames` surfaced on the target book; no new dial, deterministic floor untouched) |
| [0080](0080-holding-period-matches-the-evidence-horizon.md) | The desk's HOLDING PERIOD is derived from the horizon its edge is measured over — one measurement horizon held per round trip paid, so the edge gate stops crediting an hour of return against one round trip while the desk turns over every 43 seconds | Proposed | ✔ Built (`TargetPlanner.adjustmentRateFor` returns `a = 1 - exp(-cycle/horizon)`, the unique fraction whose exposure time constant `τ = -cycle/ln(1-a)` equals `jethro.signals.horizon-seconds` exactly; `FusionConfig` derives it from the same property the telemetry is configured by so the two cannot drift, and `jethro.fusion.adjustment-rate=0` now means DERIVE with a positive value as an escape hatch — the dial is replaced by an identity, not a chosen number; `TargetPlanner.orderDelta` splits the gap at flat and rates ONLY the risk-increasing part, so a cut trades in full in one cycle (previously an asymptotic ~7-cycle grind paying a round trip per step and leaving a one-unit residual) while adding is ~60× slower; attacks the measured churn — 11 MSFT legs in 13 minutes netting to exactly zero, `-$895.94` realised on `$140.73` of fees; no new dial, deterministic floor untouched) |
| [0081](0081-the-hurdle-is-read-against-the-distribution-the-statistic-follows.md) | The edge gate's significance hurdle is read against the distribution its statistic ACTUALLY follows — Student's t on the cohorts its standard error was estimated from, not a fixed normal quantile | Proposed | ✔ Built (new pure `Significance` supplies the Student-t upper tail via the regularized incomplete beta identity `P(T>t) = ½·I_{ν/(ν+t²)}(ν/2, ½)` with Lentz continued fraction + Lanczos log-Γ, reusing the single existing Φ; `EdgeGate.Params.tHurdle` is reinterpreted as a CONFIDENCE level converted once to `α = 1 - Φ(tHurdle)` — 2.0 ⇒ α = 0.02275, value and calibration unchanged — and `SourceEdge.clears` tests `p ≤ α` on `cohorts - 1` degrees of freedom, since ADR-0077's standard error is ESTIMATED from those cohorts; a cross-sectional source emits ONE cohort per measurement horizon, so B is single-digit for hours and at B=3 a fixed 2.0 was a ~91% test wearing a 97.7% label; exactly a no-op as df → ∞, strictly conservative below it; evidence ordered by p-value, which is the only statistic comparable across sources with different df; degenerate inputs read p = 1; numerics pinned to published t-table points, the Cauchy and df=2 closed forms, and exact factorials; no new dial, no sizing change, deterministic floor untouched) |
| [0082](0082-the-evidence-horizon-is-measured-not-dialled.md) | The horizon the desk's edge is measured over is CHOSEN BY THE DATA, not by a dial — every call is graded over a geometric ladder of horizons and the edge gate selects the rung the evidence supports, paying the multiple-testing haircut for having looked | Proposed | ✔ Built (new pure `HorizonLadder` builds the ladder `base, base/4, base/16` at the 4x span ratio the trend sensor already uses -- shipped `jethro.signals.horizon-rungs=3` => 3600/900/225s, 1 rung reproduces the old behaviour exactly -- and selects the rung with the smallest best p-value among those whose gate opens, ties breaking to the LONGER horizon; `SignalTelemetry.record` opens one observation per rung on one shared entry instant and mark, with `horizon_seconds` in the open-call key and the resolved query so the per-rung sample limit stops a 16x-faster rung starving the base; `EdgeGate.Params.alpha()` becomes `(1-Phi(tHurdle))/m` (Bonferroni -- valid under the strong positive dependence between nested measurements, so EVERY rung including the base faces a strictly HARDER bar than before) and `Decision` states the horizon it judged; the selected rung drives the gate verdict, the ADR-0055 source weights AND -- via ADR-0080's identity evaluated per cycle in `FusionLifecycle` -- the holding period, so grading, weighting and holding can no longer disagree; attacks the measured deadlock: `reversion` +13.51bps at 88% but only 3 cohorts because a cross-sectional source emits ONE cohort per horizon, book flat at $0 gross with PnL frozen ten cycles; shortening the horizon is NOT a way to buy significance -- expectancy shrinks with the period while the round trip does not, so a short rung clears only if the edge is genuinely fast, and both cases are pinned as worked tests; no money dial, deterministic floor untouched) |
| [0083](0083-per-name-budget-is-split-by-measured-volatility.md) | Split the per-name cash budget by each name's MEASURED volatility, not equally — every covered name contributes the same standalone RISK instead of the same cash | Proposed | ✔ Built (new pure `VolatilityBudget` scales each covered target by `kᵢ = σ_ref/σ̃ᵢ` over the same EWMA(λ=0.94) daily-return covariance that prices parametric VaR, the ADR-0038 hedge advisor and the ADR-0079 multiplier, where `σ_ref` is the HARMONIC mean of the winsorised σ — the unique reference for which `Σkᵢ = |C|`, so the covered budgets sum to exactly what they summed to before and `jethro.fusion.unit-notional-usd` is UNCHANGED, keeping Oleg's meaning as the cash for a name of TYPICAL volatility; closes the volatility half of the concentration problem whose correlation half ADR-0079 prices — its own `σ_indep = √(Σ eᵢ² Σᵢᵢ)` benchmark is unequal across `i` precisely because `Σᵢᵢ` is; measured 7.4x σ dispersion on the live universe (EURUSD 0.435%/day to NVDA 3.236%/day), so flat cash silently concentrated the book; applied BEFORE the correlation control since "how much of this book is one bet" is only well posed once the names are comparable; each covered name's standalone risk `notionalᵢ × σ̃ᵢ` is identical by construction, the book is capped at the gross it replaced so an estimated σ can never lever it up, `kᵢ > 0` flips no name's side, and an uncovered name keeps the flat dial — no measurement, no claim; nearest-rank winsorisation bites only above `100/p` covered names, documented with the structural `Σkᵢ = |C|` bound below it; `volBudgetNames`/`volBudgetDispersion`/`volBudgetLeverCap` surfaced on the target book; one convention dial (`vol-budget.winsor-pct`, moves no total), no money dial, deterministic floor untouched) |
| [0084](0084-the-desk-posts-to-enter-and-crosses-to-exit.md) | The desk POSTS to enter and CROSSES to exit — a risk-INCREASING fusion delta rests as a DAY LIMIT at its own arrival mark, a risk-REDUCING one still crosses as MARKET | Proposed | ✔ Built (`FusionExecutor` prices a risk-increasing delta at the instrument's mark read from the SAME cache the order module stamps as the TCA arrival price — so a passive fill's measured implementation shortfall is exactly zero rather than the difference between two clocks — stated at the 6dp price scale and rounded AWAY from the side being bought (DOWN for a BUY, UP for a SELL) so rounding can only improve the posted price; exits stay MARKET, the same asymmetry ADR-0065/0080 already apply to the gates and the adjustment rate, because a cut that waits for a better price is not a cut; `FusionLifecycle` retires its own working orders — scoped by the `fusion:` idempotency-key prefix, so never the hedge advisor's or an operator's — at the TOP of each tick before the book is read, so the plan is never stacked on stale intent; new `OrderService.workingOrders()`/`lastPrice(id)`/`cancel(id, reason)`; attacks the measured deadlock directly — reversion's +5.63bps at 1.35bps standard error over 17 cohorts can survive a 1.95bps round trip, while every tradable name's MEASURED round trip is its full spread (MSFT 2.87 to GOOGL 20.11) and the only name that clears is ES at 0.35bps, which the ADR-0049 selector never measures and so vetoes: thirteen cycles at $0 gross; the hurdle, alpha, Bonferroni haircut and degrees of freedom are ALL untouched — only the measured cost the same test reads falls; no dial and no money number is introduced, the limit IS the mark; deterministic floor untouched)
| [0086](0086-the-fusion-desk-cuts-on-its-own-volatility.md) | The fusion desk CUTS a position on its own measured volatility — a name is worked flat once its mark has retraced from the best level seen since the position was opened by more than k x the name's own sigma over the desk's own holding horizon | Proposed | ✔ Built (new pure `StreamVolatility` measures per-name sigma from the MARK STREAM — EWMA of squared log returns at the fusion cadence, `sigma_h = sigma_d * sqrt(h/d)`, running mean until a full span is absorbed and silent before that (ADR-0066's lesson), seeded on first sight from the durable mark store since its warm-up exceeds the process lifetime (ADR-0071) — because EVERY sigma the desk currently prices risk with comes from `daily_close` and covers 3 of 23 planned names and NONE of the names actually held, so the parametric VaR reports its whole covered exposure as skipped; new `TrailingRiskCut` trails from the PEAK (max for a long, min for a short — the chandelier exit, LeBeau/Kaufman ch.23) rather than from entry, so it implements let-winners-run and is strictly tighter than an entry stop, never looser; runs LAST, after the edge gate, and can only ever set a target FLAT and clamp the delta to a reduction, so an estimated sigma can never lever the book up and an unmeasured name is left exactly as planned; the cut period and the re-arm cooldown are both the DERIVED holding horizon (ADR-0080 at the ADR-0082 rung), not dials; closes the gap that the fusion path — the sole order origin, carrying the only source clearing the gate, whose mean-reversion payoff is short optionality — had no per-name cut at all while the legacy path has had one since ADR-0019; one dial, `risk-cut.sigma-multiple=3.0`, PLACEHOLDER — Oleg to set, the conservative end of the 2.5-3x ATR chandelier convention read against a horizon sigma; worked example pinned as a test (0.3 ES short, trough 5440: 5600 = 2.9412% holds, 5605 = 3.0331% cuts against a 2.9851% trigger, delta +0.300000 in one cycle); deterministic floor untouched. Note: ADR-0085 was written and then reverted by the improvement loop's scorer, so that number is retired and unused.) |
| [0089](0089-the-fusion-desk-measures-correlation-on-the-stream-it-trades.md) | The fusion desk measures CORRELATION on the stream it trades — the two sizing controls that look at the book rather than at a name are given a covariance that actually covers the names being planned | Proposed | ✔ Built (new pure `StreamCovariance` measures the EWMA of return cross-products over SYNCHRONISED per-cycle mark snapshots, fed the same prices the plan was made from and seeded by the new `SensorWarmup.jointSeedSamples` — the durable mark history replayed as snapshots on ONE bucket grid of the store's provider clock, last mark carried forward exactly as the live mark-cache read does, truncating at a hole on the existing gap tolerance; closes the gap that ADR-0079's concentration multiplier and ADR-0083's volatility budget both read a `daily_close` covariance which under ADR-0073 needs several admissible sessions in the RUNNING feed mode, so on a young stream it covers none of the planned book — the parametric VaR on the same covariance reports its whole exposure as skipped — and both controls fall through to leave the book exactly as planned, while the planner is short nearly every name at once and gross rises cycle over cycle; ONE estimator per cycle chosen by coverage of the book being planned with ties to the incumbent daily estimate, never a blend of two sampling periods, disclosed as `covarianceBasis` on the target book; both consumers are homogeneous of degree ZERO in the covariance (PDM is a ratio of two quadratic forms; the vol budget divides by a harmonic mean of the same cross-section and winsorises by rank) so the per-interval sampling period cannot move a size and no trading-day convention is invented — pinned as a test; one-way by construction, PDM capped at 1 and the vol budget capped at the gross it replaced, so a fresh statistic can only ever shrink the book; worked example pinned as a test (two names printing 100/110/99 together ⇒ ρ = 1 ⇒ PDM = 1/√2, the 1/√N ADR-0079 promises; reversed, the cap discards a ratio above 1); two convention dials only (`stream-covariance.enabled`, `.span=120` = one hour at the 30s cadence, the same span as the ADR-0086 σ sensor), `unit-notional-usd` untouched, deterministic floor untouched) |
| [0090](0090-a-change-of-view-is-not-an-exit.md) | A change of view is NOT an exit — only a FLAT target is worked at full speed; every other move of the target is a rebalance worked at the derived partial-adjustment rate, in both directions | Proposed | ✔ Built (one branch in `TargetPlanner.orderDelta`, narrowing ADR-0080's Decision 2: that ADR rated only the risk-INCREASING half of a gap and traded every reduction in full, justified as "a cut that waits is not a cut" — but the method never sees WHY the target moved, so it read a mere change of view as a danger cut; with a mean-reverting source dominating the fused forecast the combined view crosses the held position many times inside one measurement horizon, and each crossing liquidated the whole accumulated position and started rebuilding the other way, so τ_in = h while τ_out = 0 and the desk paid the full cost of a round trip while never reaching the size at which the measured edge could pay for it — visible order by order in the live window as a seven-minute 3-share grind into AAPL followed by a single 101-share reversal, with the firm's fee bill the large majority of its total loss on a gross book two orders of magnitude below the notional traded; every control that actually means "get out" says so by setting the target FLAT, so the `target = 0` branch leaves the ADR-0086 chandelier stop, the ADR-0065 orphan unwind and the ADR-0027 breaker / pre-trade guardrail bit-for-bit unchanged; what remains is a rebalance, and rating it makes the policy the exponential smoother ADR-0080's own identity describes — `posₜ₊₁ = (1−a)·posₜ + a·aimₜ`, e-folding toward the aim in exactly one horizon in BOTH directions, pinned as a test over three legs (up, down, through flat); it can never lever the book up since `a ≤ 1`; accepted trade-off stated in the ADR: ADR-0083's volatility budget and ADR-0079/0089's concentration multiplier express themselves by SCALING a target rather than flattening it, so their de-risking is also worked over one horizon now — they are sizing controls, not danger detectors, and the danger detectors flatten; worked example pinned as a test (long 101 → target −210 at the shipped a = 0.008298707361: −2 shares where ADR-0080 traded −102; target 0 → the whole gap, unchanged); no new dial and no new money/risk number — only which half of the existing rate applies) |
| [0091](0091-the-desk-does-not-net-against-its-own-hedge.md) | The desk does NOT net against its own hedge — the fusion planner measures its gap against the books it ROUTES INTO, never against the hedge overlay | Proposed | ✔ Built (`FusionConfig.firmPositions` → `routedBookPositions(risk, hedgeBook)`, which skips positions in the configured `jethro.hedge.book` exactly as the neighbouring `heldInRoutedBooks` already did; the two suppliers handed to the fusion lifecycle disagreed — one said the hedge book is not ours, the other summed its quantities as ours — and the failure the first was written to prevent arrived by the second route: with `current = s + h` the planner reads its gap as `target − (s + h)` and opens an equal, opposite `−h` in a STRATEGY book every time the hedger acts, while the hedger's own target (measured over cash equities only) is unchanged by that sale, so neither loop sees the other's control variable and the fixed point is `s = target − h`: the firm holds `|target − h| + |h|` of the contract where the economics call for `|target|`, the hedge is EXACTLY cancelled — on the balance sheet and in the fee bill, contributing nothing to net — and the offsetting leg grows with `h`, a ratchet with no fixed point in gross; measured live on the ADR-0042 selected proxy: `MACRO −0.060884 ES` against `HEDGE +0.050697 ES`, firm gross $30,408 for a firm net of −$2,776, 47% of the firm's gross exposure carrying 9% of its net, with the planner's own target book reporting `ES: current = +0.011200` — a POSITIVE current on a contract the strategy book is short — and the hedge book the firm's single worst position, its loss exceeding the whole firm's and almost all of it realised on round trips rather than lost on a view; worked example pinned as a test (at convergence the old read leaves the strategy leg at `target − h = −0.210797` and the new one at `target = −0.160100`, the gross removed being exactly `h`, and one cycle of the old loop plans a sale of precisely what the hedger just bought); a signed `BigDecimal` sum with one book filtered out — no new dial, no new money or risk number, no rounding introduced, every instrument the hedge book does not hold unaffected quantity for quantity, hedger and deterministic floor untouched) |
| [0092](0092-a-forecast-scalar-is-measured-not-promised.md) | A forecast scalar is MEASURED, not promised — each continuous source is rescaled to the size its own readings actually are | Proposed | ✔ Built (new pure `ForecastScalars` keeps one EXPANDING mean of |claim| per source, pooled across instruments, and `ForecastRegistry` publishes `clamp(claim × TARGET_ABS / max(TARGET_ABS, mean|claim|))`; every continuous mapper's uncapped arithmetic moves into a `…Claim(…)` accessor the `from…` mapper is defined in terms of, so the measured quantity and the published one cannot drift; closes the gap that the four phase-2 scaling constants are CLAIMS about how big a source's readings are which nothing has ever checked — measured live, trend runs `E|f| = 15.2` against a promised 10 with a median of 18.9 and 11 of 23 names pinned exactly at the ±20 cap, reversion `14.7` with 6 of 23 pinned, so over a third of the cross-section is clipped and a clipped forecast carries NO cross-sectional information: eleven names reading the identical +20 are indistinguishable to the planner and the source has degenerated from a forecast into a sign function, while sizing is linear in the forecast so the inflated scale is also an inflated book (~$381k planned against ~$19k held) and the turnover bill to grind toward it; expanding rather than decaying per Carver, Systematic Trading 2015 ch.7 — the estimand is a structural property of the sensor, not a regime, so there is no half-life to invent; measured on the CLAIM never the output, so rescaling cannot feed back into its own estimate, and causal — the scalar applied to a reading comes from the readings strictly before it, so a name's forecast cannot depend on where in the cycle's cross-section it was published; deliberately ONE-WAY, `max(TARGET_ABS, …)` bounds the scalar at 1 so an over-delivering source is scaled DOWN and an under-delivering one is left exactly as it is rather than levered up on an estimate — ADR-0087 and ADR-0088 were both reverted for growing exposure with no PnL and an estimate that can only shrink cannot repeat that; ordinal sources (hypothesis conviction, social channel counts) excluded as a declared scale with no claimed E|reading| to hold them to; below min-sample the scalar is 1.0, bit-for-bit the previous behaviour; worked example pinned as a test (a source measured at E|claim| = 15 ⇒ scalar 2/3; two names claiming 18 and 24 went 900 vs 1,000 shares — an 11% gap on a reading 33% stronger — and now go 600 vs 800, the full 33% expressed and both smaller; a claim of 45 still clips, the cap is un-jammed not removed); two convention dials only (`forecast-scalar.enabled`, `.min-sample=30`, the same small-sample convention as `edge-gate.min-sample`), no money number authored, `unit-notional-usd` untouched, deterministic floor untouched) |
| [0094](0094-the-no-trade-band-is-measured-against-the-aim-not-the-target.md) | The no-trade band is measured against the AIM, not the target — a buffer the desk can actually be inside | Proposed | ✔ Built (new pure `PositionBuffer` runs LAST in the fusion cycle and re-derives every delta from an **aim** — the ADR-0080 exponential path `aim + a·(target − aim)`, seeded at the held position — trading only when `|aim − held|` exceeds `positionBufferFraction ×` the name's average position, and then only back to the buffer's NEAR EDGE; closes the finding that the ADR-0055 band (`|target| × bufferFraction` against the gap TO the target) has never suppressed one order and structurally cannot, because ADR-0080 partial adjustment deliberately holds the desk below its target so the gap is a large fraction of it every cycle — measured live, all thirteen planned names with a non-zero gap traded at exactly the derived rate `0.032784` while the desk held 5–30% of its own target (AAPL −7 against −142, JNJ 47 against 260, GOOG 38 against 131), and the resulting turnover cost `ALPHA` `$268.61` of fees against roughly `$283` of gross alpha, i.e. 95% of the gross; cost here is PROPORTIONAL to quantity (fee a fixed fraction of notional, slippage in bps) so the lever is suppressing the target's oscillation rather than the order count, and under proportional costs the optimal policy is provably a no-trade region traded at its boundary (Constantinides JPE 1986; Davis & Norman Math. of OR 1990) — Gârleanu-Pedersen's smooth partial adjustment is the QUADRATIC-cost solution; exposure unchanged by construction because the aim path IS the position the old policy converged to; buffer width 0.10 is Carver's published convention (Systematic Trading 2015; Advanced Futures Trading Strategies 2023), dimensionless, and the average position is DERIVED per name from this cycle's own arithmetic `|target| × TARGET_ABS / |forecast|` — no money number authored, Oleg's `buffer-fraction=0.5` left exactly as set and documented as superseded at the routing stage; worked example pinned as a test (AAPL forecast −9.64, target −142.319300, held −7: band 14.763413, aim −11.436294, gap −4.436294 ⇒ NO ORDER where the old policy sold 4 shares every cycle; once intent has drifted to −40 the gap of 33 breaches the buffer and 18.236587 trades); a FLAT target is never buffered — the ADR-0086 cut, the ADR-0065 orphan unwind and every 'get out' control snap the aim to zero and trade the whole position exactly as ADR-0090 works it — and a shut ADR-0064/0075 gate clamps the order reduce-only AND re-seeds the aim to where the desk will actually be, so intent cannot accumulate behind it; firm breaker, pre-trade guardrail and ADR-0049 support untouched; `position-buffer.enabled=false` restores the ADR-0080 deltas byte for byte) |
| [0097](0097-a-source-must-earn-the-right-to-steer-the-book.md) | A source must EARN the right to steer the book — a view that fails the desk's own significance test is held at the MINIMUM combination weight | Proposed | ✔ Built (`EdgeGate.demonstratesEdge` — the desk's existing ADR-0077/0081/0082 significance test at ZERO cost, factored out of `SourceEdge.clears` so the gate and the weights cannot drift — admits a source to the vote, and `TelemetryWeights.compute(stats, params, admission)` holds every non-admitted source at `Params.min()`; closes the gap that the desk runs the SAME statistic through two consumers with two answers: the gate asks whether risk may be taken and the weights ask whose view counts, but the weighting statistic is `Φ(t)`, a PROBABILITY whose entire dynamic range is `t ∈ [−2.5, 2.5]`, so past the hurdle it is flat and cannot tell a source that barely clears from one that clears fivefold — measured live, the edge gate passed exactly ONE source (reversion, t = 10.65, p = 2.9e-13) while `social` (t = 1.82, p = 0.039, FAILING the desk's own test) carried 83% of its weight, and on the desk's LARGEST position (`JPM`, $13,288 gross, planned −162.58 shares) social's `−16.00 × 1.757 = −28.11` out-voted reversion's `+1.29 × 2.106 = +2.72` roughly ten to one and reversed the sign of the only view with a demonstrated edge; the test is gross of execution cost deliberately, keeping ADR-0067's separation intact (cost decides whether to trade, weights decide whose view counts — charging it here would charge one measurement twice); STRICTLY ONE-WAY since `min` is the lower clamp bound `compute` already applies, so a demotion can only lower a weight and a measured-negative source is never inverted into a contrarian bet — unlike ADR-0087 (removed this floor) and ADR-0093 (replaced `Φ(t)` with an effect size), both of which the scorer reverted and neither of which this repeats; inert when no source is admitted, which is safe because the gate's test is this test at a non-negative cost so nothing can clear the gate that fails admission and the desk is reduce-only there anyway, and which avoids the one case where demotion would RAISE the ADR-0076 multiplier by flattening the weight vector; where it does bite it concentrates the weights, raising `Σw̄²` and lowering `DM` — `JPM` goes 1.1944 → 1.1299 and its fused view −7.66 → ≈−1.5, under `min-forecast-to-route`, so the desk stops adding to a $33k short taken on evidence it had already judged insufficient; worked example pinned as a test (STRONG 500/39 cohorts +10.0 bps sd 6.0 ⇒ SE 0.960769, t 10.4083, p ≈ 4e-13 ⇒ admitted; WEAK 51/31 cohorts +3.25 bps sd 10.7 ⇒ SE 1.921797, t 1.69112, p ≈ 0.0505 > α 0.0227501 ⇒ held at 0.25 exactly, STRONG bit-for-bit unchanged), plus one-way, all-fail-is-a-no-op, null-admission-is-byte-identical and cost-monotonicity tests; NO new dial and no new statistic — `edge-gate.min-sample` and `edge-gate.t-hurdle` are the gate's own, no money/risk/exposure number is authored, the combiner normalises by `Σw` so this rotates conviction and cannot scale the book, and the deterministic floor is untouched) |
| [0098](0098-the-hedge-neutralizes-only-exposure-that-stands-clear-of-its-own-churn.md) | The hedge neutralizes only the systematic exposure that stands clear of its OWN churn | Proposed | ✔ Built (`HedgeTargetChurn` — an EWMA(λ=0.94, the same RiskMetrics decay `CovMath` uses for the firm covariance) of the squared STEP in the raw hedge-target notional, sampled once per hedge cooldown — plus `HedgeAdvisor.shrinkToChurnBoundary`, the soft threshold `T' = sign(T)·max(0, |T| − k·σ)` applied to the sized target before the ADR-0039 delta/band path; closes the gap that ADR-0039 holds the book target-flat with a ZERO rebalance floor, so the target is `−Σβ·E` at every evaluation no matter how small or how noisy that number is — measured live, `Σβ·E = −$195.49` against $37,162 of firm gross (0.5%: the strategy book is already nearly beta-neutral) while the desk sent one ES order per cooldown, alternating BUY/SELL at $250–$3,000 a time, and the HEDGE book lost $509.21 against a firm total of $326.72 — of which under $50 is fees plus 355 ES fills at a measured 0.204 bps, so the loss is DIRECTIONAL, a signed proxy bet taken on noise, and adversely signed at that since `−Σβ·E` on a mean-reversion book is long the proxy after a rally and short after a selloff, i.e. a momentum position on ES, the one view the desk measures as significantly negative; STRICTLY ONE-WAY — `|T'| ≤ |T|` and the sign can never flip, so an over-estimated σ can only make the hedge smaller and never lever the book up; a REAL hedge is untouched (the ADR-0038 $456,000 worked example at σ = $900 goes 1.676711 → 1.673402 ES, a 0.2% trim) and a target inside its own churn is set flat with the residual unwound by the ordinary delta path; σ is MEASURED from the running stream, not dialled, so it is feed-agnostic and needs no re-tuning on a live feed; the first step seeds the EWMA rather than decaying from zero and σ is published only from the second step, so warm-up cannot bias it low and a warming estimator shrinks nothing; the executing `HedgeLifecycle` is the ONLY writer of the series so a `/api/hedging` poll reads σ and never samples it; `rawTargetNotionalUsd` + `churnSigmaUsd` surfaced on the endpoint for audit; `churn-sigma-multiple = 0` restores pre-ADR-0098 behaviour exactly and every existing advisor test passes unchanged through the no-σ overload; exact-decimal worked examples pinned as tests (oscillating 0→1000→0 ⇒ σ = 1000.00 exactly; 0→100→300 ⇒ v = 0.94·10,000 + 0.06·40,000 = 11,800 ⇒ σ = 108.63; −$1,799,999.88 raw at σ = $280,000 ⇒ −5.428571 ES, exactly one contract less); the one dial `jethro.hedge.churn-sigma-multiple = 1.0` is a one-standard-error soft threshold marked `PLACEHOLDER — Oleg to set`, no money level is authored, and the deterministic floor — pre-trade guardrail and firm drawdown breaker — is untouched) |
| [0099](0099-a-name-is-charged-its-own-quoted-round-trip-before-its-first-fill.md) | A name is charged its OWN quoted round trip before its first fill, not the blend of the names the desk already trades | Proposed | ✔ Built (`QuotedSpreadCost` — `roundTripBps = 20000·(ask−bid)/(ask+bid)`, one full quoted touch = one round trip (Grinold & Kahn 2e ch.16; Almgren & Chriss 2000), written over `(ask+bid)` so the `/2` in the midpoint cancels into a single exact division — plus `withQuotedFallback`, merged into the ADR-0075 per-name cost map in `FusionConfig.selectRung` from the live `QuoteCache` (ADR-0025); closes the gap that ADR-0075 tests every name against its OWN measured round trip but a name is only measured AFTER it has traded, so an unfilled name is charged the desk BLEND and the veto arrives one discovery loss too late — the desk's two worst names are its two most expensive ever filled, GOOGL at a measured 20.11 bps round trip (−$161.84) and SAP at 8.16 bps (−$85.46), together over half of what the whole firm has made, both entered while charged the 1.48 bps blend against a source whose measured net edge is +9.07 bps at t = 9.32, and six names the desk has NEVER filled (`BRK.B`, `NFLX`, `ORCL`, `GS`, `TSLA`, `GOOGL`) quote the same 20.0 bps touch with ~$120k of planned gross queued behind them; the estimator is validated against the desk's own realised TCA and errs conservatively where it errs — ratio 0.99 on GOOGL and 0.98 on SAP where ADR-0084's passive limit is superseded and the desk crosses, ~1.5–2.1 on the tight names where it does not; MEASURED ALWAYS BEATS QUOTED, the first fill displacing the estimate permanently, so this is only ever a bootstrap; STRICTLY ONE-WAY via `max(deskBlend, quoted)` — no name's hurdle can fall below today's and `EdgeGate`'s test is monotone in cost, so it can only ever remove a trade and never add one; the desk-wide verdict is PROVABLY untouched since that verdict is taken at the cheapest round trip in map-plus-blend and nothing added is below the blend, so `mayIncrease()`, the selected rung and the source weights that read it cannot move; rate-quoted names excluded on the execution-cost model's own rule (`"SWAP".equals(assetClass)`) because a bp of a swap RATE is additive and not a fraction of notional; held positions in an excluded name stay reduce-only, never force-closed; NO new dial and NO new number — the value is the live quote or the existing blend, it is feed-agnostic by construction (invariant 9: a venue's top of book live, the ADR-0025 synthesised touch in sim), and the deterministic floor is untouched) |
| [0106](0106-a-round-trip-that-pays-the-desk-is-not-a-cost.md) | A round trip that pays the desk is not a measurement of cost | Proposed | ✔ Built (`QuotedSpreadCost.withQuotedFallback` — a NON-POSITIVE measured per-name round trip is treated as NO measurement and takes the ADR-0099 fallback `max(deskBlend, quotedRoundTrip)` (or the blend where the name has no two-sided quote), while a positive measurement is still returned untouched so ADR-0084’s genuine passive saving is preserved (AAPL measures 0.889 against a 2.00 quoted touch — one half-spread, exactly what posting to enter should save); closes the gap that the ADR-0075 cost `C` is an implementation shortfall against the ARRIVAL mark and so carries the price DRIFT over the fill window as well as the spread, and under ADR-0084 the desk posts to enter — a passive order fills precisely when the price comes to it, so entry drift is systematically favourable and on a name where it exceeds the exit’s half-spread the round trip comes out NEGATIVE, i.e. signal booked as an execution rebate; live `NQ` read −0.25445 bps and, because ADR-0075 states the desk-wide verdict at the CHEAPEST entry in the map, that one reading became the desk’s cost and `netEdgeBps = avgReturnBps − cheapest` came out ABOVE the expectancy actually measured for every source at once (`reversion` graded 8.683 against a measured 8.429, `trend` −5.977 against −6.232) — the gate that exists to make the desk pay for its trading handing it 0.25 bps of edge no signal produced — while ADR-0101’s `if (!(costBps > 0))` guard collapsed that same name to the desk’s NARROWEST no-trade buffer (0.10 Carver floor against the 0.3192 its blend implies), re-trading the least honestly-priced name the most often; corrects the `EdgeGate` javadoc’s prior claim that “a negative, i.e. price improvement, is a real reading the gate must use” — true of a ONE-WAY slippage, false of a ROUND TRIP, whose sign is fixed by construction since both legs cross the touch or wait for it and the fee is charged twice; STRICTLY ONE-WAY and asserted as tests — every per-name hurdle is ≥ what it is today and `EdgeGate`’s test is monotone in cost, so this can only ever REMOVE a trade and never add one, and the desk-wide cheapest can only rise, so exposure cannot grow because of it; NO new dial and NO new number — the threshold is the SIGN of a cost, arithmetic rather than appetite, and the replacement value is the name’s own live quote or the desk’s own blend, both already measured (invariant 7 / ADR-0016); exact worked example pinned as tests (`NQ` quote `20000 × (19786.142008 − 19785.884790) / (19786.142008 + 19785.884790) = 0.12999… bps` ⇒ `max(1.3449575609756097, 0.12999…)` = the blend; desk-wide cheapest moves from −0.25445 to `ES`’s real 0.41852998; `reversion` net edge `8.42860925 − 0.41852998 = 8.01007927`, t = 9.198 against a 2.0 hurdle — NO source changes verdict, the one that passed still passes and the four that failed still fail); feed-agnostic (invariant 9) — reads only the desk’s own measured series and the running feed’s own quote, no level, no class assumption, no sim special-case; deferred: decompose the TCA shortfall into spread and drift so the bias is removed on EVERY name rather than only where it changes sign — the reading suggests the whole series is biased low, which would make every cost-keyed control on this desk too permissive) |
| [0107](0107-a-change-of-view-is-still-not-an-exit.md) | A change of view is still not an exit — an exit is what a control ORDERED | Proposed | ✔ Built (`PositionBuffer.bufferedDelta` — the unbuffered full-speed branch is keyed on the planner's TARGET being flat rather than on the AIM reading zero, and the part of a gap that crossed flat in ONE step is worked at the ADR-0080 derived rate `a = 1 − e^(−c/h)`; closes the gap that ADR-0090's rule ("a change of view is a rebalance, not an exit") was keyed on a flat target in `TargetPlanner.orderDelta`, but ADR-0094 moved the order derivation into `PositionBuffer`, which keyed it on `aim.signum() == 0` — faithful when only a flat target could zero the aim, and no longer so once ADR-0102's clamp began holding an inverted intent at flat in one step, which on this desk is the STEADY STATE and not a corner case since the only source clearing the edge gate is a mean-reverting one whose forecast crosses the held position repeatedly inside the horizon the rate is derived from: every crossing was read as a cut and liquidated the whole accumulated position in one MARKET order, the exact round trip ADR-0090 exists to prevent, and ADR-0102's own worked example records it as intended behaviour (`gap = 0 − 104 ⇒ delta = −104.000000`); the clamp is right, classifying its output as an exit is what was wrong — nothing ORDERED that cut, and every control that means "get out" says so by planning the name FLAT; the cost is turnover, which is the dominant identified drag here (fees dominate turnover and are a function of quantity traded, and the tape shows the signature plainly: AAPL bought 1/3/3/4/5/7/8 then sold 22 and 13, JNJ bought 19/9/28/6 then sold 14/14/34), compounded by ADR-0084's execution asymmetry — an entry POSTS and pays no spread while the crossing-liquidation CROSSES and pays the full spread on everything it built for free — and it is also why the desk holds a small fraction of its own planned book, never getting far before the next crossing empties it; STRICTLY ONE-WAY and asserted as a test over every held/aim/target sign combination — `|edge| ≤ |gap|` and `a ∈ [0,1]`, so the order is never larger and never of a different sign than before, it can never open or enlarge a position, can never slow a cut a RISK CONTROL ordered (a flat target returns on the first branch, unbuffered and unrated, so the ADR-0086 chandelier stop, the ADR-0065 orphan unwind, the ADR-0027 breaker and the pre-trade guardrail keep their exact semantics) and can never slow a SAME-SIDE de-risk, which reaches the buffer by a rated aim step and is excluded by the `sgn(aim) = sgn(held)` branch; the aim path is byte-identical so INTENDED position is unchanged; NO new dial and NO new number — the rate is ADR-0080's, the band is ADR-0101's measured width, the bound is ADR-0102's target (invariant 7 / ADR-0016); exact-decimal worked example pinned as tests, correcting ADR-0102's own (JNJ held +104 against target −219.420787 at forecast −12.64: band `173.592395 × 0.10 = 17.359240`, step `93.397005` clamped to 0, gap `−104.000000`, edge `−86.640760` to the near buffer edge, all of it unwinding ⇒ `× 0.0327838995179941 = −2.840422` instead of `−104.000000`; next cycle aim `−7.193469` against a book still long 101 ⇒ gap `−108.193469`, edge `−90.834229`, delta `−2.977900` — the unwind cannot stall because the aim marches to the new side as the holding shrinks, and the position e-folds to flat over the ONE measurement horizon the edge gate priced); the honest trade-off is that positions persist longer so average gross exposure RISES — the claim under test is that turnover saved plus edge captured over the horizon it was graded on exceed the carry, and the loop's scorer settles it; feed-agnostic (invariant 9) — every input is measured off the running stream) |
| [0108](0108-the-evidence-budget-is-counted-in-cohorts.md) | The evidence budget is counted in COHORTS, not rows — a wide source must not starve itself | Proposed | ✔ Built (`SignalTelemetryStore.resolvedCohorts` + `SignalScoring.aggregate(String, List<Cohort>, long)` — each source's expectancy is estimated from the most recent `jethro.signals.cohort-limit` emission COHORTS per horizon instead of the most recent `sample-limit` ROWS, with the ADR-0077 gap-cut grouping pushed into SQL where the rows live and each cohort returned as its sufficient statistics `(n, Σx, Σx², wins, losses)`; closes the gap that the row bound predates ADR-0077 — it dates from ADR-0055 phase 1 when every observation WAS an independent draw — so since ADR-0077 the statistics have been bounded in the wrong unit and the budget was spent at a rate set by CROSS-SECTION WIDTH: a source calling ~23 names per burst was pinned at ~25–60 independent draws forever and could never accumulate another however long the desk ran, while a one-name-at-a-time source got the full 500 (live: `reversion`@225s and `trend`@225s both capped at 500 rows ⇒ 58 cohorts, against `momentum`@225s uncapped at 234 rows ⇒ 186 cohorts), so the edge gate's power was capped by breadth rather than by evidence and the desk has been REDUCE-ONLY for its whole life on this feed — `mayIncrease: false`, `deltaQty: 0` on every name of a nine-name planned book against a live `grossExposure` of `$0.00`, and a book at zero exposure earns nothing whatever the rest of the machinery does; verified against the live table with the gate's own arithmetic (α = Φ̄(2.0)/3 = 0.007583 after the ADR-0082 haircut, Student's t on cohorts−1 df, net of the cheapest round trip the desk can pay, `ES` at 0.4214 bps): `reversion`@225s moves from 500 rows / 58 cohorts / 10.35 bps / t = 0.99 / p = 0.16237 — shut — to 4 856 observations / 250 cohorts / 6.268 bps / t = 2.50 / p = 0.00653, OPEN, the point estimate FALLING while the standard error falls faster (√(250/58)), i.e. a smaller and better-estimated edge rather than a friendlier test; the same read demotes `trend`, which today carries the ENTIRE fused book (all nine targets read `sources: 1`, weight 1.23) on a 3 600 s reading of +72 bps that clears nothing (t = 0.88), to a measured-significantly-NEGATIVE −5.19 bps t = −2.47 at the rung the desk would actually hold, i.e. ADR-0097's MINIMUM combination weight; permission arrives NAME BY NAME under the ADR-0075 per-name cost test and is deliberately narrow at first — only `ES` (0.4214) clears today, `AAPL` (0.8765) reads p = 0.01098 and the desk blend p = 0.01778 — widening only as √B accrues; NO arithmetic changed — expectancy is still the mean of cohort means, the standard error still sd(cohort means)/√B, and `SignalScoringTest` pins the in-memory and SQL groupings to the same `Stats` on the same data so the two cuts cannot drift; reads LESS from the database than the bound it replaces (one row per cohort, not one per observation) while covering an order of magnitude more history; the number is NOT a money/risk dial — a measurement sample size that sizes nothing (invariant 7 / ADR-0016) — and its error direction is stated: lowering it widens the standard error, the strictly conservative way to be wrong; `t-hurdle`, `min-sample`, `hypotheses`, the cost model and the per-name veto are untouched, and nothing below the deterministic floor moves; feed-agnostic (invariant 9) and still `feed_mode`-scoped (ADR-0029); the superseded `resolvedObservations` + `Resolved` are REMOVED so there is no second row-bounded way to sample the table; rejected alternative recorded so it is not re-attempted — cross-sectional demeaning of each cohort was measured on the live table first and moves `reversion`@3600s only from t = 1.04 to t = 1.14 because point estimate and standard error shrink together (222 → 26 bps, 929 → 98 bps) while any one-name cohort collapses to identically zero; the honest trade-off is that gross exposure rises from `$0.00`, which a book at zero cannot avoid, and the loop's scorer settles whether the desk earns on it) |
| [0104](0104-the-book-carries-no-more-risk-than-it-typically-carries.md) | The book carries no more risk than it typically carries — an absolute volatility anchor | Proposed | ✔ Built (`BookVolatilityBrake`, wired in `FusionLifecycle.tick` after ADR-0083/ADR-0079 and before the ADR-0064 gate, the ADR-0086 exit and the ADR-0094 buffer — the fused book's measured ex-ante volatility `sigma_planned = sqrt(e'.Sigma.e)` over the covered names (the SAME statistic, coverage rules and covariance ADR-0079 already prices concentration with) is capped at the MEDIAN of its OWN planned-sigma series over the last `span` planning cycles, every covered target scaled by `min(1, sigma_ref/sigma_planned)` with its order delta recomputed under the cycle's own band and rate; closes the gap that EVERY size control on this desk is RELATIVE — ADR-0083 is budget-neutral by construction and ADR-0079 scales back to the independence benchmark the per-name budget already implied, so the book's risk LEVEL was `unit-notional-usd x (how many names happened to clear the edge gate) x (how strong their forecasts happened to be)`, i.e. a property of the cross-section rather than of anybody's appetite: measured firm gross read $14.5k -> $62.1k -> $43.4k -> $46.6k -> $7.9k -> $27.6k across consecutive evaluations with the desk's appetite unchanged, which makes PnL-per-unit-of-exposure unmeasurable and puts the desk's largest risk in whichever cycles the cross-section happened to shout; the empirical claim is the standard volatility-targeting result that returns are NOT proportional to risk across risk states so trimming the hot states raises return per unit of risk (Harvey, Hoyle, Rattray, Sargaison & van Hemert, JPM 2018; Moreira & Muir, JF 2017; the scaling behind Moskowitz, Ooi & Pedersen JFE 2012); NO MONEY/RISK NUMBER IS INTRODUCED — the reference is not a chosen volatility target but the desk's own typical planned risk measured on whatever stream it is trading, so the same code self-calibrates to a sim, live or replay feed with no special-casing (invariant 9) and nothing is asserted that was not measured (invariant 7 / ADR-0016); STRICTLY ONE-WAY and asserted as tests — `min(1, .)` discards the case where planned risk sits below its own median so an estimated covariance can only ever shrink the desk, the multiplier is positive and uniform so no name flips side and the cross-sectional shape is untouched, the series samples the RAW pre-brake sigma so a cut cycle cannot drag the reference down and ratchet the book to zero, and it can NEVER set a target flat (the ADR-0086 cut, the ADR-0065 unwind, the ADR-0027 breaker and the pre-trade guardrail keep their exact semantics); silent below `min-sample`, on an incomplete covariance or on a degenerate sigma, leaving the book byte-identical; exact worked examples pinned as tests (one name at 100.00 x 1,000 units = $100,000 at 2%/day => sigma = $2,000/day; series {2000,2000,2000,8000} => median $2,000 => brake 8000 -> 0.25 => the 4,000-unit target scaled to exactly 1,000.000000 units and the braked book at sigma = $2,000 = sigma_ref precisely, by homogeneity of degree 1; two equicorrelated names at rho = 0.5 => 2000.sqrt(3) = $3,464.10/day); dials `span=120` (one hour at the 30s cadence, the SAME window `risk-cut.vol-span` and `stream-covariance.span` already use) and `min-sample=30` (the house `edge-gate.min-sample` convention, also one ADR-0080/0082 holding horizon of planning history) are estimation conventions and the control is scale-invariant in sigma, so neither can move a size by itself; deferred: persist the sigma series so the brake is not cold for 15 minutes after each redeploy, and anchor the LEVEL to a declared appetite rather than only the dispersion — both in the deferred register) |
| [0102](0102-the-desk-never-intends-past-or-against-its-own-target.md) | The desk never intends past — or against — its own target | Proposed | ✔ Built (`PositionBuffer.withinTarget` — the ADR-0080/0094 aim is clamped into the closed interval between FLAT and this cycle's target, `aim' = min(max(aim, min(0,T)), max(0,T))`, applied after the rate step and before the ADR-0101 band is measured against it; closes the gap that the aim recursion `aim + a(T − aim)` unrolls to an EWMA of the target sequence — a convex combination of PAST targets, which is NOT the hull of the current one, so whenever the target moves faster than `1/a` the intent can OUTGROW it (live EURUSD: intent −19268.293125 against a target of −13006.790342, 6261.502783 units ≈ $6,789 of notional the planner never asked for) or INVERT against it (live JNJ: intent +8.043404 against a target of −219.420787 while the book held +104, the desk's largest position long in the name its own strongest forecast said to be short, with the aim path walking it FURTHER long; AUDUSD −11494.134880 against +17285.020487 and GBPUSD −2024.437007 against +19015.931789, both about to OPEN the wrong way round from flat) — neither of which partial adjustment means, since Gârleanu & Pedersen's aim (JF 68(6) 2013 §III) is a weighted average of the CURRENT and EXPECTED FUTURE targets, every element a position the model wants, not of a realised past; the correction is worth ~6:1 on the desk's own measured numbers — being the wrong side of the passing source swings ±8.53 bps per 900s horizon against a 2×1.43 bps round trip, the same arithmetic ADR-0101 used for the width; STRICTLY ONE-WAY with both properties asserted as tests, `|aim'| ≤ |aim|` and `sgn(aim') ∈ {0, sgn(T)}`, so it can never open, enlarge or side-flip a position and every trade it newly admits moves the book toward flat or toward the target's own side; NO new dial and NO new number — the bound is the target the planner already computed — and rate, band width (ADR-0101), band location (ADR-0094), volatility budget (ADR-0083) and portfolio normaliser (ADR-0079) are untouched; the FLAT-TARGET branch returns BEFORE the clamp so the ADR-0086 chandelier cut, the ADR-0065 orphan unwind, the ADR-0027 breaker and the pre-trade guardrail keep their exact semantics; an inversion is cut ONCE to flat and the position then rebuilt through the buffer at the derived rate rather than round-tripped, exact-decimal worked example pinned as a test (JNJ: step 93.398… ⇒ clamped to 0 ⇒ sell −104.000000; next cycle aim = −7.193469 against a band of 219.420787 × 10 / 12.64 × 0.10 = 17.359240 ⇒ NO ORDER); feed-agnostic by construction since the bound is the desk's own target (invariant 9)) |
| [0101](0101-the-no-trade-buffer-is-the-desk-s-own-cost-to-edge-ratio.md) | The no-trade buffer's WIDTH is the desk's own cost-to-edge ratio, not a published convention | Proposed | ✔ Built (`PositionBuffer.widthFor` + `passingEdgeBps` — each name is buffered at `max(carverFraction, min(1, 2C/mu))` where `C` is that name's OWN measured round-trip cost in bps from the ADR-0072/0075 map, the desk blend for a name never filled exactly as `EdgeGate.Decision.mayIncrease` charges it, and `mu` is the GROSS expectancy in bps of the best-evidenced source that CLEARS the gate at the ADR-0082 rung the evidence selected — both already inside the `EdgeGate.Decision` the buffer is already handed, so no new input, no new dial and no new number; `2C/mu` is the no-trade half-width the desk's own arithmetic implies — closing a gap `g` is worth its cost when `1/2.lambda.sigma^2.g^2 > C.|g|`, i.e. `|g| > 2C/(lambda.sigma^2)`, and the aim's own first-order condition `a = mu/(lambda.sigma^2)` turns that into `band = a.(2C/mu)`, so the risk-aversion parameter this desk has never stated CANCELS and never has to be invented (Grinold & Kahn 2e ch.16; the traded-to-the-near-edge policy shape is unchanged and is the one proportional costs call for, Constantinides JPE 1986 / Davis & Norman Math.OR 1990); closes the gap that ADR-0094 fixed WHAT the band is measured against but left its WIDTH at Carver's published 0.10 — a convention is what you use when you cannot measure, and this desk measures both inputs every cycle for the edge gate: live, one source clears (`reversion`, 8.82 bps over 500 resolved on 65 cohorts, t = 9.3 at the 900s rung, every other source measured significantly NEGATIVE and pinned at the ADR-0097 MIN weight) against 0.49-1.01 bps of measured one-way slippage plus 1 bp of fee a side, so the implied width is several times 0.10 and the desk filled 2,254 orders in a day to hold an $18.1k book, paying $355.89 of ALPHA fees against $879.66 of ALPHA P&L — two fifths of what the strategy books make handed to the cost of getting there; STRICTLY ONE-WAY — the measured width is used only where it is WIDER than the convention and is capped at one average position, so it can only ever suppress a trade the desk would have made, never add or enlarge one; the AIM PATH IS UNTOUCHED quantity for quantity so intended exposure is unchanged by construction and this is a P&L improvement at constant risk; an EXIT is still not buffered at any width (a flat target snaps the aim to zero and trades in full), so the ADR-0086 cut, the ADR-0065 unwind, the ADR-0064 reduce-only clamp and the deterministic floor — pre-trade guardrail and firm breaker — are untouched; no gate, no passing source, a non-positive measured edge or a non-positive measured cost all assert NO width and leave Carver's convention exactly as today (invariant 7 / ADR-0016); `mu` is GROSS not net because the cost is charged once on the `C` side; feed-agnostic by construction since both inputs are measured off the running stream (invariant 9); exact-decimal worked examples pinned as tests (`C = 2.0`, `mu = 8.0` ⇒ width 0.50 exactly; target 200 at forecast 16 ⇒ average position `200 x 10/16 = 125` exactly, band `62.500000` measured against `12.500000` conventional, and the aim path `0 → 6.557800 → 12.898603` at the derived rate is outside the one and inside the other, the conventional band trading `0.398603` every 30s and the measured one nothing; `GOOGL` at 20.11 bps caps at 1.0; a name absent from the measured map takes the blend, `2 x 1.48/8.0 = 0.37`)) |
| [0100](0100-hedge-tracks-its-target-at-that-target-s-own-directional-efficiency.md) | The hedge closes the gap to its target at that target's own directional efficiency | Proposed | ✔ Built (`HedgeTargetChurn.efficiencyRatio` — Kaufman's efficiency ratio in exponential form, `E = |EWMA(step)| / EWMA(|step|)` at the same `λ = 0.94` RiskMetrics decay `CovMath` and the ADR-0098 `σ` already use, on the same raw-target series sampled once per hedge cooldown by the executing loop; both recursions start at ZERO rather than seeded so the `(1−λⁿ)` warm-up factor is identical in numerator and denominator and cancels exactly in the ratio, making `E` unbiased from its first reading — `n` steps of `+x` read 1 exactly, a perfect `+x,−x` round trip reads `(1−λ)/(1+λ) = 0.0309`, and `+x,−x,+x` reads 0.334183 against Kaufman's 0.3333 over the same window — plus `HedgeAdvisor.trackTowardTarget`, which closes only `E` of the gap `d − h` and ONLY when the move grows the overlay, the ADR-0080 asymmetry applied to the hedge path where it was missing; closes the RATE half of the problem ADR-0098 opened on the LEVEL, which the live cycle shows is the half left: the raw target is `−$6,039.12` against `σ_step = $958.51`, i.e. 6.3σ, so the ADR-0098 shrink removes only 16% and the overlay is free to chase a large target that WANDERS ACROSS ZERO because it is minus the net of a book that re-signs its names — eleven consecutive cooldowns sent BUY/BUY/BUY/BUY/BUY/SELL/SELL/SELL/SELL/SELL/BUY totalling 0.0545 ES traded to end up holding −0.00494, while the HEDGE book bled `−323.33 → −451.51 → −509.21 → −550.99 → −599.22` over five scored cycles and handed back 51% of what the strategy books made; the loss is directional not execution — `$42.75` of fees at a measured 0.204 bps round trip — because `−Σβ·E` on a mean-reversion book is a MOMENTUM position on ES, the one view the desk measures significantly negative; STRICTLY ONE-WAY — `q` lies on the segment between `h` and `d` in every branch so `|q| ≤ |d|` whenever the move grows the overlay, `E = 1` reproduces the prior behaviour exactly, and an estimated rate can only ever leave the hedge smaller than ADR-0098 already allows; ADR-0069's unwind promise is verbatim intact since a reduction — including a full unwind and the free leg of a move crossing flat — still trades in one cycle; a target genuinely going somewhere reads `E → 1` and is hedged in full, so the protection is present exactly when systematic exposure is actually building; NO new dial and NO new number, `E` is a ratio of the stream to itself so it self-calibrates on any feed (invariant 9), and the deterministic floor — pre-trade guardrail and firm breaker — is untouched) |

## Status vs Implementation

**Status** is the ADR lifecycle (Proposed / Accepted / Superseded); **Implementation** is what is
actually in the code — the two diverge, which is why this column exists. Several ADRs are still
*Proposed* yet fully built (0025, 0026, 0027, 0043), and some *Accepted* ones are only partial.

**Implementation legend:** ✅ built & in the codebase (tests where applicable) · ◐ core built, parts
deferred · ○ accepted but no code yet · ⏸ deferred behind a stated trigger · ✖ superseded/rejected ·
— decision-only (no code artifact).

### Partial / not-built — what's missing (details in `deferred-register.md` / `gap-register.md`)

- **0005 Aurora PG** — Postgres + Flyway built (dev compose); managed **Aurora** is the prod shape,
  behind a production trigger (ADR-0013).
- **0007 Fargate/CDK** — `infra/` CDK **dev** stack exists; the Fargate/ALB/Aurora **prod** shape is
  not deployed.
- **0010 AI SPI** — inference SPI + **local Ollama** tier built; the **external frontier tier** is
  behind cost/latency triggers, not wired.
- **0011 FinOps** — module scaffolded only (no Java yet); **planned**.
- **0014 trading-core** — in-process ring buffer + **LMDB** warm/dedupe built; the **S3 Parquet
  write-behind tick archiver is NOT built**.
- **0035 RAG** — embeddings + outcome memory built; the **pgvector** store is still in progress.
- **0037 clean/comprehensive P&L** — the split + realized-FX translation are built; **unrealized-FX
  decomposition** and the Avro-event fields are deferred.
- **0038 proxy hedging** — **EQUITY** axis built; the **DV01-neutral rates** axis and **direct-FX**
  axis are unbuilt.
- **0039 hedge lifecycle** — advisory modes built; **AUTO order submission** + the firm-breaker
  one-shot de-risk are unbuilt.
- **0040 structural hedging** — the structural **sector tier** is built; per-sector **ETF proxies** +
  assigned-vs-realized beta telemetry are unbuilt.
- **0045 news advisory** — real **RSS news** + the **universe-discovery** register are built (via
  0050 §7); the **SLM sector-classify** step and the full queued pipeline are not built.
- **0046 no sim archive** — a scoping decision that only applies **once the 0014 archiver exists**;
  nothing to build until then.
- **0048 persist strategy actions** — **not started** (the Strategy-actions UI panel is still
  in-memory / lost on restart).
- **0050 social** — the adversarial pipeline + StockTwits/Telegram adapters + discovery are built
  (Phase 1 / 2a / 2b); **SLM classify (P3)** and outcome-scored **channel ranking (P4)** are not.

## Lifecycle

`Proposed` → `Accepted` (or `Rejected`) → possibly `Superseded by ADR-XXXX`.

An ADR is **Proposed** when written, **Accepted** once the owner (Oleg) signs off.
Never edit the decision of an Accepted ADR — write a new ADR that supersedes it.
