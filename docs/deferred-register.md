# Deferred-work register

Every deliberately deferred item lives here with its trigger, so nothing depends on a
code comment or somebody's memory to resurface (deferred items must never get silently
lost). Decision-level deferrals made inside ADRs (managed Kafka, per-module extraction,
C++ hot paths, licensed feeds, …) stay governed by their ADRs' explicit triggers and are
not duplicated here — this register is for **implementation-level** items noted in code
or docs.

Rules:

- Adding a "deferred / tracked / not faked / revisit" note in code REQUIRES a row here in
  the same PR.
- When an item ships, delete its row in the shipping PR (git history is the archive).
- Rows carry the source reference so the note and the register can't drift apart.

| Item | Why deferred / trigger to build | Source reference |
|---|---|---|
| Edge gate: commission leg of the measured round-trip cost | The ADR-0064 hurdle uses price slippage only. `execution_quality` stores the fill commission as CASH without the contract multiplier, so it cannot be expressed in bps of notional without approximating — left out rather than guessed, making the hurdle a LOWER bound on true cost. Trigger: add notional (or the multiplier) to the TCA row, then include the fee leg | ADR-0064; `ExecutionQualityRepository.averageSlippageBps()` |
| Edge gate: per-instrument rather than desk-wide cost | ADR-0064 compares every source against one fill-weighted average cost, so a cheap name is held to an expensive name's hurdle and vice versa. Trigger: enough per-instrument TCA depth to estimate a per-name cost without over-fitting a thin sample | ADR-0064 §Follow-ups |
| Hedging: DV01-neutral rates axis + FX axis | The advisor is EQUITY-axis only (ADR-0038 §2/§3 unbuilt): DV01-neutral rates hedge onto ZT/ZF/ZN/ZB via the CTD contract DV01, and a direct FX-pair hedge on the book's net foreign value. Trigger: a book carries rates (bucket DV01) or non-USD exposure worth hedging | ADR-0038; `HedgeAdvisor` (EQUITY only) |
| Hedging: AUTO order submission + firm-breaker one-shot de-risk | Mode AUTO is modelled but does not yet submit hedge orders; the ADR-0039 breaker de-risk (flatten-all-axes-then-freeze, skipping stale/quarantined proxies) is unbuilt. Needs idempotent hedge keys through the order path + the sim gate/envelope. Trigger: sign-off to let the advisor act, not just advise | ADR-0039 §3/§4; `HedgeAdvisor.Mode.AUTO` note |
| Hedging: per-book (vs firm-level) exposure + caps | v1 aggregates equity exposure firm-wide; ADR-0039 targets per-book modes/caps so each desk hedges its own book. Trigger: two books with materially different hedging needs | ADR-0039; `HedgeAdvisor` javadoc |
| Hedge axis: index-future delta (strategy ES/NQ positions) | The equity hedge axis sums single-name EQUITY only; a strategy position in ES/NQ is equity beta the hedger neither sees nor hedges. Needs β(NQ→ES) refdata. Trigger: strategy futures notional becomes a material share of equity-axis risk | 2026-07-18 math review P2-1; `HedgeAdvisor` axis predicate |
| Impact model: temporary vs permanent split | Square-root impact prints fully into the fill price with no decay; Almgren–Chriss decomposes temporary/permanent. Second-order at the 2% ADV cap. Trigger: participation caps loosen, or TCA shows systematic reversal after our own fills | 2026-07-18 math review P2-3; `SimulatedExecutor.withImpact` |
| Momentum: act on the lagged-σ z (baselineZ) | The firing z uses in-window σ, which the signal move itself inflates; the honest lagged-baseline z is computed but reporting-only. Trigger: OOS backtest evidence that baselineZ improves expectancy — change by evidence, not decree | 2026-07-18 math review P2-2; `MomentumStrategy` |
| SOFR fixings archive (replace flat-fixings approximation) | Seasoned swap accrual prices elapsed fixings flat at the current short rate; a fixings time series makes carry exact. Trigger: real curve/fixings source lands (with the ADR-0041 bootstrap) | ADR-0041 follow-ups; `SwapPricingService.seasonedProvider` |
| Structural hedge: sector-ETF proxies + universe expansion + beta telemetry | ADR-0040 structural tier hedges every equity to ES at an assigned beta; the per-sector ETF proxies (XLK/XLF/…), the universe expansion into the thin balancing sectors (staple/utility defensive + energy/industrial cyclical), and assigned-vs-realized beta telemetry (promote to statistical once data confirms) are unbuilt. Trigger: those sector-ETF instruments exist in refdata / a name added per thin sector | ADR-0040; `V32__hedge_groups_and_betas.sql` (hedge_proxy=ES follow-up note) |
| Real corporate-action data source (splits, dividends, symbol changes) | The MarkCache jump guard quarantines large jumps but cannot detect ordinary dividends (~0.5–2%) or distinguish a split from a crash without an authoritative feed; needs a data-source decision (likely its own ADR) | `MarkCache` javadoc; ADR-0024 impl note |
| Configurable per-book base currency (non-USD reporting) | Rollups convert to USD at the live FX mark today; a non-USD-based book needs a base-currency attribute and report-currency plumbing. Trigger: first book that reports in EUR/GBP | `RiskProjection` javadoc |
| FX cross-pair conversion (non-USD crosses, e.g. EURGBP) | `FxConversion` builds its matrix from spot-vs-USD marks only; crosses would triangulate through USD. Trigger: first non-USD-quoted instrument or cross pair in refdata | `FxConversion.java` comment |
| Ring-buffer overwrite-oldest drop policy | Drop-newest + MarkCache conflation preserves freshness today; overwrite-oldest only pays off if consumer stalls appear. Trigger: measured drops with a healthy consumer (perf guardrails + `droppedCount` metric make this observable) | `TickRingBuffer` javadoc |
| Historical replay of hypothesis theses | Theses are validated by the sim-tape OOS backtest (advisory) + live track record (the gate); replaying a thesis against real daily history (walk-forward style) needs thesis→rule translation. Trigger: enough `hypothesis_record` history to make replay meaningful | `HypothesisEvaluator` javadoc |
| Full CTD basket / conversion-factor model from a real deliverable universe | Bond-future DV01 uses CME deliverable-window duration + 6% CF pivot today; the full model needs a real bond universe (issues, coupons, CFs). Trigger: rates PnL attribution noticeably off vs the window model, or a real rates feed | ADR-0020 impl notes |
| Pi soak test for Postgres/Redpanda round-trip budgets | Unit-scale perf guardrails deliberately exclude environment-bound round trips. Trigger: a measured latency complaint on the Pi, or before any real-money broker work | `docs/architecture/perf-budgets.md` |
| CI schema-registration + BACKWARD-compat gate | ADR-0030 registers schemas at runtime (register-on-encode) and sets BACKWARD best-effort; a CI step should register every `common-messaging` schema against a throwaway registry and FAIL the build on an incompatible edit, so a breaking change is caught pre-merge, not at first publish. Trigger: next schema edit, or CI hardening pass | ADR-0030 follow-ups |
| Sim-panel: auth gate + panel-driven flag on events | ADR-0031 `/api/sim/*` is sim-gated (`feedMode==SIM`) but unauthenticated like the rest of the `:8080` edge; and a panel-driven session is labelled UI-only (`SimControl.anyDialActive()`), not stamped onto emitted events. Trigger: auth on `/api/admin/*` lands (share it), or a need to tell panel-driven marks from organic sim in the durable log | ADR-0031 follow-ups; `SimControlController` |
| Unrealized FX decomposition (price vs FX attribution on open foreign positions) | ADR-0037 locks *realized* FX at booking and breaks out realized translation, but *unrealized* on an open foreign position still moves with both the local price and spot, undecomposed. Trigger: a P&L-explain / attribution need on open foreign risk | ADR-0037 follow-ups; `RiskProjection.Acc` |
| `comprehensivePnl` / `fxTranslationPnl` on the risk-snapshot Avro event | ADR-0037 splits clean vs comprehensive in-process; the published `book-risk` event still carries only realized/unrealized. Additive fields (invariant 4) would let downstream consumers see the split. Trigger: first consumer that needs comprehensive P&L off the topic | ADR-0037 follow-ups; `RiskSnapshotPublisher` |
| Real per-currency cash ledger (foreign cash as a revaluing position) | ADR-0037's locked-realized + translation line is the reporting equivalent of a real FX/treasury cash account; a true ledger (cash accounts, sweeps, funding) is the full-fat answer. Trigger: real multi-currency cash management / funding | ADR-0037 alternatives |
| Regime detector: walk-forward-on-live gate + per-regime effectiveness telemetry | ADR-0044 drives the algo pick from a price-derived efficiency-ratio detector, gated by the regime-AVERAGED OOS median — so a genuinely trend-profitable algo whose all-regime median is negative is still vetoed. The regime-specific validator (walk-forward on the recent live tape) and per-regime "did the trend-matched algo earn its keep" telemetry are unbuilt. Trigger: measured evidence the averaged gate suppresses real regime edge | ADR-0044 follow-ups; `StrategySelector.gate` note; `TrendDetector` |
| Learned-signal backtest: per-name transaction cost from the execution-cost model | The ADR-0053 walk-forward gate charges one blended `jethro.training.backtest.cost-bps` (=10) to every simulated trade; the honest cost is per-name (spread_bps + fee_bps already modelled in `ExecutionCostSource`), so a tight name is under-credited and a wide one over-credited. Trigger: the learned signal clears the blended-cost gate and per-name cost would change which names ship | ADR-0053; `WalkForwardBacktest` cost; `jethro.training.backtest.cost-bps` |
| Regime detector: efficiency-ratio window/bands calibration | The ER window (20) and TREND/CHOP bands (0.5/0.3) are an initial modelling choice (mine, arbitrary), not a market convention. Should be validated/tuned against measured OOS behaviour, and the variance-ratio alternative compared. Trigger: OOS evidence a different window/band or metric improves regime-matched expectancy | ADR-0044; `jethro.strategy.trend.*` in application.properties |
| OOS selector backtest: allocation-lean run (scaled-long, not per-tick BigDecimal) | The hourly OOS `BacktestEngine` allocates a per-tick BigDecimal `mark[]` + per-eval `Observation` list across seeds×algos×instruments — a large transient spike that on a small heap (512m) drove ZGC allocation stalls and froze the whole JVM (~1h cadence). Mitigated now by a pre-run heap guard (skip when headroom thin) + a bigger default heap; the durable fix is to run the backtest in scaled-long money types (invariant 1) so it barely allocates. Trigger: the guard trips often enough that the selector stops refreshing on the target box | `StrategySelector.lowHeap` guard; `BacktestEngine.run`; `scripts/run-local.sh` HEAP note |
| Refdata attribute-coverage guard (test) | No DB-backed migration test exists, so a universe expansion that adds an instrument to the master but forgets its attribute rows (display_name/adv/spread/symbology) is only caught by eye — exactly how NVDA/JNJ/JPM/AUDUSD shipped with missing UI data (fixed in V34). Add a coverage test asserting every EQUITY/FX master row has display_name+adv_usd+spread_bps+yahoo (equities also finnhub). Trigger: a Flyway/Testcontainers migration harness lands | CLAUDE.md refdata rule; `V34__backfill_universe_expansion_refdata.sql` |
| Narration loops: gate Ollama calls on change-since-last-run | The risk-commentator (`jethro.ai.interval-seconds`) and hypothesis (`jethro.hypothesis.interval-seconds=20`) loops call the local SLM every cycle unconditionally — even with no new marks/news — which pins llama-server resident and drove its native leak (fixed operationally by short `keep-alive` + `num-ctx` cap + the `ChatModelRecycler` force-unload backstop). The durable fix is to skip the inference when nothing changed since the last run, so an idle box unloads the model naturally and the recycler rarely fires. Trigger: a change-signal (ticks-since / news-since) is cheap to thread through both loops | `RiskCommentatorLifecycle.runOnce`; `HypothesisLifecycle`; `ChatModelRecycler` |
| Refdata coverage guard test: every price-quoted name has ≥1 live-source symbology | No test asserts each EQUITY/FX/FUTURE master row carries at least one feed symbology (yahoo/finnhub), so a name can ride a single delayed source — or none — and blank a position (observed: JPM short → mark 0 → 0 exposure). Fold into the existing refdata attribute-coverage guard once a Flyway/Testcontainers harness lands. Trigger: migration test harness exists | ADR-0056; CLAUDE.md refdata rule; `V34__backfill_universe_expansion_refdata.sql` (finnhub rows) |
| Alpaca stream: msgpack decoding fallback | The Alpaca adapter (ADR-0056) expects JSON text frames (Alpaca officially supports JSON streaming). Its v2 stream CAN emit msgpack binary; a binary frame is logged loudly and drops (no silent loss) but yields no marks until decoded. If a connection lands on msgpack, add a small msgpack decode path (org.msgpack:msgpack-core) in `AlpacaWebSocketClient.onBinary`. Trigger: a live connection logs the "BINARY (msgpack) frame" error | ADR-0056; `AlpacaWebSocketClient` |
| daily_close feed-mode scoping (VaR/history contamination root fix) | `daily_close` is shared across sim/live and SIM-CONTAMINATED at the tail (the training layer already keeps a separate `training_bars` store to dodge this). ADR-0058 makes VaR coverage robust to it, but the durable fix is per-mode scoping (ADR-0029 follow-up) so live VaR/history never sees sim closes — a schema + write-path change, keeping the real seed usable by both modes. Trigger: this batch's VaR fix proves insufficient, or the ADR-0029 mode-scoping work is picked up | ADR-0058; ADR-0029 follow-ups; `VarService.dayVectors`; `TrainingBarsStore` |
| Rates-futures live marks from the curve (ZN/ZF/ZB/ZT) | Under a LIVE provider (yahoo/alpaca), the curve emits tenor-point marks (USD.SOFR.*/USD.TSY.*) but NOT the future contracts — the sim adapter prices those from the curve via `linkedIds`, the live background (Yahoo poll) does not. So held rates futures show mark 0 / has_mark N (phantom-flat, unvalued, inflate VaR skipped-exposure). Fix: derive an ongoing futures price from the curve (duration/CTD model that already backs DV01) and emit it in the live-background path — finance-sensitive, so NOT rushed in the 2026-07-23 batch (tiny positions, low materiality). Trigger: material rates exposure under a live feed, or the next rates-pricing pass | 2026-07-23 report ③; `CorrelatedMarketDataAdapter.linkedIds`; `TradingCoreLifecycle.liveBackground` |
| SAP live mark under a live provider | SAP (EQUITY, EUR/Frankfurt, yahoo symbol SAP.DE) showed mark 0 / has_mark N in the 2-day report — Yahoo not returning SAP.DE (off-hours Frankfurt + possible coverage). Lower priority than a US name (single foreign name); confirm whether SAP.DE polls at all and whether a session-hours/coverage gap. Trigger: SAP carried as a material position, or a foreign-listing coverage pass | 2026-07-23 report ③; `TradingCoreLifecycle.yahooSymbolMap` |
| Integer-lot sizing for futures on BOTH order paths | Both order paths now submit fractional futures: the HedgeAdvisor always did (ES 0.036 contracts), and ADR-0078 made the fusion path do so deliberately (a correctly-sized $50k ES position is ~0.18 contracts, and whole-unit rounding would erase it entirely). Fine in paper on any feed; NOT tradable live, where futures trade in whole contracts. The real fix is not rounding but a **unit notional large enough that one contract is a sensible clip**, or an explicit odd-lot policy — a sizing/appetite decision that wants provenance, not a loop-invented number. Trigger: a real broker is wired (ADR-0015), or a futures proxy is carried live | 2026-07-23 report ⑤; ADR-0078; `HedgeAdvisor` sizing; `TargetPlanner.tradableQuantity` |
| ADR-0060: replace a discovered name's PROVISIONAL adv/spread with measured values | A promoted name trades immediately on a PROVISIONAL (flagged) liquidity-tier adv/spread — honest but a guess, and adv/spread size and cost its orders. Once the name has accumulated enough of its OWN tape (VolumeStats/ADV, ADR-0032), replace the provisional attributes with measured ones. Not a trading gate (the name already trades) — an accuracy improvement on sizing/cost. Trigger: a discovered name accumulates enough own-tape volume to measure ADV honestly | ADR-0060 §2; `UniversePromotionService` (provisional attrs); `jethro.universe.dynamic.provisional-*` |
| ADR-0060: promoted names join the trading universe only at the NEXT session (no mid-session hot-subscribe) | A name promoted at runtime is written to refdata immediately (visible in books/refdata/Discover), but the market-data adapter builds its symbol subscription set — and the sim/OOS universe — at BOOT (`TradingCoreLifecycle.start`, `BacktestService.universe()`), so a promoted name only starts ticking / being OOS-evaluated / trading after the next restart. Fix: a runtime "add-symbol" path into the running adapter + selector, or a periodic re-read. Trigger: trading a discovered name in-session (before a restart) becomes important | ADR-0060; `TradingCoreLifecycle.effectiveSimInstruments`/`yahooSymbolMap` (read at start); `BacktestService.universe` |
| ADR-0060: live provider-asset feed-coverage confirmation (replace the directory proxy) | The promotion gate's feed-coverage check uses `CompanyDirectory.covers()` (the SEC company-tickers list) as a conservative proxy — a strict floor that never over-admits but isn't a live confirmation the configured provider can actually stream the name. Phase 2 should confirm against the real provider asset list (Alpaca /v2/assets tradable, Finnhub profile) before admitting. Trigger: Phase 2 refdata write path is built | ADR-0060 §1; `CompanyDirectory.covers`; `UniversePromotionLifecycle` |
| ADR-0065: sub-unit residual on an unwound position | Partial adjustment toward a flat target approaches zero asymptotically, and the order path refuses a sub-unit clip (`FusionExecutor` truncates to whole units for multiplier-1 names — ADR-0078 leaves that rounding exactly as it was for equities and FX), so a whole-unit instrument settles ONE unit short of flat rather than at flat. Bounded and non-growing (a stub, not a drift), but it keeps the name in the held set and replanned every cycle forever. Fix needs a minimum-clip or close-the-remainder rule — deliberately NOT invented here, since it is a sizing rule that wants provenance. Trigger: the stub residual becomes material, or a minimum-clip policy is set | ADR-0065; `TargetPlanner.orderDelta`; `FusionExecutor.route` (whole-unit truncation) |
| ADR-0068: equity-relative firm risk budget & bleed cutoff (mechanism) | The firm gross budget + drawdown breaker are absolute-dollar dials sized for a big book; on a reset (~$0) book they go toothless (the desk bled with a $50k drawdown breaker). Interim fix: loss/drawdown dials lowered to PLACEHOLDER values in application.properties. Durable fix: make the firm bleed cutoff = `drawdownFrac × capital` and gross budget = `grossMultiple × capital` (a configured account size) so they auto-scale and a reset can't defeat them — code in RiskLimits / RiskLimitProperties / FirmBreakerMonitor / PreTradeGuardrail + tests. Trigger: now (safety); the placeholders stand until then | ADR-0068; `FirmBreakerMonitor`; `PreTradeGuardrail`; `jethro.risk.firm.*` / `max-firm-drawdown` |
| Loop RAG (ADR-0035 Stage 2): retrieve findings + trade outcomes | `docs/loop-findings.md` accumulates one lesson per cycle and is read in full each cycle. Once it outgrows the prompt, embed each finding + trade outcome (SingleFlightEmbeddingClient exists) into pgvector and retrieve the semantically relevant ones per situation instead of reading all. Trigger: findings corpus outgrows context | ADR-0035; `docs/loop-findings.md`; `SingleFlightEmbeddingClient` |
| ADR-0069: size the hedge no-trade band from measured round-trip cost, not a convention | The band is `min(absolute guard, 25% × hedge scale)`; the 25% is the standard 5/25 rebalancing convention, not a number measured on this platform. The economically correct form is "trade when the risk reduction is worth the round-trip cost" — needs a per-proxy cost estimate wired from the ADR-0025 model plus a stated exchange rate between dollars of residual exposure and dollars of cost (a risk-appetite decision, not a loop-invented number). Trigger: hedge trade cost becomes material, or Oleg sets the appetite | ADR-0069; `HedgeAdvisor.noTradeBand`; `jethro.hedge.no-trade-band-fraction` |
| ADR-0078: minimum order notional instead of scale-based rounding | ADR-0078 rounds a fusion delta toward zero at whole units (multiplier 1) or at the position quantity scale (contracts). The cleaner general rule is a minimum ORDER NOTIONAL — one threshold in dollars that suppresses dust identically across asset classes, instead of a rounding rule that means different things per multiplier. Not introduced with the ADR because it is a money number and would need provenance (invariant 7 / ADR-0016); the ADR-0055 no-trade band and ADR-0059 conviction floor already suppress small corrections. Trigger: dust orders are observed on a contract-quoted name, or Oleg sets a minimum clip | ADR-0078; `TargetPlanner.tradableQuantity`; `jethro.fusion.buffer-fraction` |
| ADR-0084: measure the opportunity cost of an entry that never fills | A posted (risk-increasing) fusion entry may not trade, and nothing records that. TCA measures the cost of fills that HAPPENED; an entry the market never came to is a missed forecast whose PnL is invisible, so the desk cannot see whether posting is winning on cost and losing more on non-fill. Fix: record every cancelled-unfilled passive order against the forecast return over the same horizon the signal telemetry already resolves, and surface fill-rate + unfilled-forecast-PnL alongside slippage. Trigger: the desk trades passively for a full session, or measured fill rate falls low enough to matter | ADR-0084; `FusionExecutor.cancelStalePassiveOrders`; `ExecutionQualityRepository`; `SignalTelemetry` |
| ADR-0084: re-check the pre-trade guardrail when a posted order fills | The ADR-0018 guardrail is evaluated at SUBMIT. With MARKET orders that was also the fill instant; a posted LIMIT can fill up to one planning cycle (30s) later, against a book that has moved. Bounded by the per-cycle cancel/re-post and the per-name budget, and every order was approved when sent — but the durable fix is to re-run the guardrail inside `OrderService.tryFill` for working LIMIT orders, which is an order-module change that touches the deterministic floor and so is NOT made casually by the improvement loop. Trigger: the desk carries material passive intent, or a fill is observed that the guardrail would have rejected at fill time | ADR-0084; ADR-0018; `OrderService.tryFill`; `PreTradeGuardrail` |
| ADR-0086: reconcile the mark-stream sigma with the daily-close sigma | The risk cut measures per-name sigma from the MARK STREAM because the `daily_close` estimate every other risk control uses covers 3 of 23 planned names and none of the names actually held. Two live volatility estimates of the same name that can disagree is a latent inconsistency: the sizer and the cutter should not price a name's risk differently. Fix: once daily-close coverage reaches the traded set, either derive one from the other or state explicitly which question each answers. Trigger: parametric VaR covers the held book | ADR-0086; `StreamVolatility`; `InstrumentVolService`; `VarService.covarianceSnapshot` |
| ADR-0086: re-arm the risk cut on the forecast changing sign, not only on elapsed time | A stopped name is reduce-only for exactly one holding horizon and is then re-entered on whatever the forecast says — including the same view that was just cut, unchanged. The economically right re-arm is "the desk has a genuinely NEW view", i.e. the combined forecast has crossed through zero since the cut. Not built with the ADR because a benched-forever name is a worse failure than an early re-entry and the time rule cannot cause it. Trigger: repeated cut-and-immediately-re-enter cycles are observed on the same name | ADR-0086; `TrailingRiskCut.isStopped`; `FusionLifecycle.applyRiskCut` |
| ADR-0086: sigma-multiple needs an owner number or an out-of-sample calibration | `jethro.fusion.risk-cut.sigma-multiple=3.0` is a PLACEHOLDER — the conservative end of the 2.5-3x ATR chandelier convention, not a figure measured on this desk. It sets how much of an adverse move is tolerated before a position is realised as a loss, so it gates money. Fix: calibrate against realised cut outcomes (cut-and-recovered vs cut-and-saved) out of sample, or take Oleg's number. Trigger: enough observed cuts to measure, or Oleg sets it | ADR-0086; `TrailingRiskCut.Params`; `jethro.fusion.risk-cut.sigma-multiple` |
| ADR-0089: shrink the mark-stream covariance (Ledoit-Wolf) before it sizes a wide book | `StreamCovariance` estimates an N×N covariance from an EWMA span of joint samples (120 at the 30s cadence). Against a couple of dozen names that is full rank but noisy — error ~ O(N/T) — and the noise moves how big the ADR-0079 haircut is. Safe today because nothing inverts Σ (both consumers are quadratic forms) and both are one-way, so noise moves the size of the cut and never its sign. The refinement is shrinkage toward a structured target (Ledoit-Wolf) rather than a longer span, which would just make the estimate stale. Trigger: the planned universe grows toward the sample span, or measured PDM is observed swinging cycle to cycle | ADR-0089; `StreamCovariance`; `PortfolioRiskNormaliser.multiplier`; `jethro.fusion.stream-covariance.span` |
| ADR-0089: reconcile the mark-stream covariance with the daily-close one (extends the ADR-0086 σ row) | The desk now carries TWO live estimates of the same statistic for the pairs as well as the diagonals — a stream covariance sizing the fusion book and a daily-close covariance pricing parametric VaR and the ADR-0038 hedge advisor — and they can disagree about how correlated the book is. Today the choice is made by coverage with ties to the daily estimate, so they never both size at once, but the sizer and the risk engine can still report different concentrations. Fix: once daily-close coverage reaches the traded set, derive one from the other or state explicitly which question each answers. Trigger: parametric VaR covers the held book | ADR-0089; ADR-0086; `StreamCovariance`; `VarService.covarianceSnapshot`; `FusionLifecycle.sizingCovariance` |
| ADR-0089: measure the non-synchronous-trading (Epps) bias in the stream covariance | A name that has not printed between two grid points is carried forward and contributes a zero return, which biases its measured correlation toward zero — the Epps effect at a 30s sampling interval. That biases the ADR-0079 haircut toward NOT shrinking the book (the conservative direction for a one-way control), so it is a loss of protection, never a source of leverage. Fix: measure per-name print frequency at the fusion cadence and either lengthen the grid for sparse names or apply a lead-lag (Hayashi-Yoshida) estimator. Trigger: a materially sized name is observed printing less often than the fusion interval | ADR-0089; `SensorWarmup.jointSeedSamples`; `StreamCovariance.update` |
| ADR-0099: a liquidity-seeking execution policy for wide names, instead of not trading them | ADR-0099 makes a name whose quoted round trip eats the measured edge reduce-only. That is the right answer when the desk's only choice is "cross or don't", but it is not the only choice: a genuine passive/liquidity-seeking policy (rest inside the touch, work the order over the holding horizon, size to the quoted depth) could make a 20 bps name tradable at a fraction of its touch. Not built with the ADR because it is a much larger decision than a cost estimate — it changes the order path, needs a fill-probability model, and interacts with ADR-0084's re-plan cancellations. Trigger: the excluded wide names are observed carrying forecasts the desk would otherwise want to size, or quoted depth becomes available for them | ADR-0099; ADR-0084; `QuotedSpreadCost`; `FusionExecutor` |
| ADR-0099: a stale or momentarily-wide quote locks a name out until it re-quotes | The estimate is read once per fusion cycle from the last-value `QuoteCache`, so a name whose quote widens on a thin patch (or goes stale on a halt) is charged that width for as long as it persists and stays reduce-only. The cost of the error is a missed entry — the cheap direction — and mark-cache staleness already governs whether the name is planned at all, so nothing unsafe follows. Fix: age the quote explicitly (skip a quote older than the fusion freshness window) or smooth the touch over a short span before charging it. Trigger: a name the desk wants is observed locked out by a transient quote rather than a persistent one | ADR-0099; `FusionConfig.quotedRoundTripByInstrument`; `jethro.fusion.freshness-seconds` |
| ADR-0104: persist the book's planned-σ series so the brake is not cold after every restart | The reference median is built from an in-memory rolling window, so the brake is silent for its first `min-sample` planning cycles (30 = fifteen minutes at the 30s cadence) after every deploy. On a desk that redeploys each improvement cycle that is a material share of every measurement window, and the control is weakest exactly when the book is being rebuilt from warm-restart state. Fix: seed the series from a durable store on first tick, the same shape ADR-0071 already uses for the σ sensor's mark history. Not built with the ADR to keep the change one coherent control. Trigger: the brake is observed binding materially once warm, or deploy cadence stays inside the warm-up | ADR-0104; ADR-0071; `BookVolatilityBrake`; `jethro.fusion.book-vol-brake.min-sample` |
| ADR-0104: anchor the book's risk LEVEL to a declared appetite, not only to its own median | The brake anchors the DISPERSION of planned risk — the book never carries more than it typically carries — but the reference is the desk's own median, so it drifts with the desk and says nothing about whether that level is the right one. The honest form is a declared σ or VaR target; it is not built because the number would be the loop's own invention and the firm's declared appetites (`max-firm-drawdown`, `firm.max-loss-pnl`) sit two orders of magnitude above this book and would never bind. Trigger: Oleg states a book-level volatility or VaR appetite, or firm equity grows until the declared breaker levels bind | ADR-0104; ADR-0027; `BookVolatilityBrake`; `jethro.risk.max-firm-drawdown` |
| ADR-0106: decompose the TCA shortfall into spread and drift | The per-name round trip is an implementation shortfall against the ARRIVAL mark, so it mixes the spread the desk crossed with the price drift over the fill window. Under ADR-0084 the desk posts to enter and a passive order fills precisely when the price comes to it, so entry drift is systematically FAVOURABLE — which means the whole measured cost series is biased LOW, not just the names where it crosses zero into the impossible. ADR-0106 only guards the sign; every cost-keyed control (the ADR-0075 gate hurdle, the ADR-0072/0099 per-name veto, the ADR-0101 `2C/mu` buffer width) is therefore still too permissive by an unmeasured amount. Fix: record the decision mark alongside the arrival mark and attribute shortfall to spread vs drift, or measure round-trip cost on matched entry/exit pairs rather than on one-way legs doubled. Trigger: measured cost is observed materially below the quoted touch on names the desk crosses, or turnover cost becomes the binding constraint again | ADR-0106; ADR-0084; ADR-0075; `ExecutionQualityRepository.averageSlippageBps`; `FusionConfig.roundTripByInstrument` |
| ADR-0106: `turnover_cost_by_name` in the loop report has been erroring for several cycles | The improvement-loop report's turnover/cost-by-name aggregate fails with `column "qty" does not exist`, so the one table that would grade a cost decision like ADR-0106 — how much the desk traded per name and what it paid — has been blank in every recent cycle. Not folded into ADR-0106 because it is a report-query fix and the loop ships one coherent change per run. Fix: correct the column reference in the report SQL against the current `orders`/`fills` schema. Trigger: next cycle that has no higher-value lever, or any cycle whose diagnosis needs per-name turnover | ADR-0106; ADR-0063; `ops/` report generator SQL; `logs/report.md` |
| ADR-0109: the mark source changed under a running session without an epoch change (invariant 8) | The contaminated cohort ADR-0109 defends against was produced by a real defect: within ONE `feed_mode=SIM` session the mark stream for a name switched tape — entry marks are the simulator's start levels (`GOOG 174.77`, `NQ 19 773.35`, `AAPL 188.94`, `MSFT 429.14`, `ES 5438.30`) while the exit marks four minutes later are alpaca's (`GOOG 323.58`, `NQ 28 677.00`), and 21 of the 23 names in that burst never crossed the handover at all so nothing flagged it. Invariant 8 requires a sim↔live switch to start a NEW `sessionEpoch`/namespace rather than continuing; here the session continued and every derived series — signal telemetry, the σ sensors, the stream covariance, the daily-close series — silently spans two tapes. ADR-0109 makes the EXPECTANCY robust to it; it does not fix it, and the other consumers are still exposed. Related and unexplained: the existing mark-jump quarantine (`jethro.trading.mark-jump-bps.EQUITY=2000`) did NOT fire on an 85% one-name move, and `/api/marks/quarantined` is empty — worth tracing separately, since that guard is the deterministic defence this ADR is only reusing the threshold of. Fix: identify where a name's mark provider can change mid-session and either force an epoch roll (invariant 8) or refuse the crossing mark; then establish why the jump guard was silent. Trigger: next cycle with no higher-value PnL lever, or any observed recurrence in `/api/signals/discards` | ADR-0109; invariant 8; ADR-0029; `MarkCache` jump guard; `MarkPublisher`; `SignalTelemetry.resolveDue`; `/api/signals/discards` |
| ADR-0110: the scorer does not know whether the commit it is scoring is the one running | ADR-0110 verifies the DEPLOY (a process started by this deploy is answering) but `scripts/score-change.py` still scores whatever the app reports, with no link between the sha in the ledger row and the binary that produced the numbers. When the deploy silently failed, the scorer wrote ⚠️ MIXED 'no material change' twice for code that had never run — a verdict indistinguishable, in the ledger and to the next agent, from a genuine finding about the change. The right rule is that a commit which cannot be proven deployed is NOT SCORED: carry the baseline forward and mark the row NOT DEPLOYED. Not built with ADR-0110 because the scorer is the invariant-7 / ADR-0016 authority for every money number in the ledger and should not move in the same breath as the plumbing that feeds it. Fix: stamp the pending baseline with the app's boot time (or, better, a build identity), and have the scorer compare it against the app it measures. Trigger: the first deploy failure that survives ADR-0110's fallback, or any ledger row whose verdict is later found to describe undeployed code — **FIRED 2026-07-29**: `68ff73eb2` was scored ⚠️ INCONCLUSIVE over 38 cycles against a JVM that booted before it existed, the commit having been stranded by the market-closed deploy gap ADR-0123 closes. ADR-0123 makes stranding rarer; this row is what makes the resulting verdicts honest, and is now the higher-priority half | ADR-0110; ADR-0063; `scripts/score-change.py`; `reports/.pending-baseline.json`; `ops/improve-loop.sh` |
| ADR-0110: verify a build IDENTITY, not merely that a restart happened | The deploy check compares the answering process's boot time against the moment the deploy started, which catches the failure that actually occurs (no restart at all) but not a restart onto a STALE jar — a build that failed while the previous artefact stayed on disk would restart cleanly and verify. The strictly correct check is the git sha baked into the jar at build time, compared against the running app. Not built with ADR-0110 because it needs build-info plumbing in `app/` and the ADR is deliberately confined to the loop's own scripts. Fix: add the sha to the boot jar's manifest / `/actuator/info` and compare it with `git rev-parse HEAD` after the deploy. Trigger: a deploy is observed restarting the app onto a jar that does not contain the commit, or the scorer row above is built (it wants the same identity) | ADR-0110; `ops/improve-loop.sh`; `app/build.gradle`; `/api/ops/jvm` |
| ADR-0113: the stream COVARIANCE estimator still advances on the CYCLE clock, not on prints (the σ half is closed by ADR-0116) | ADR-0113 gates the two continuous forecast sensors on the market's own clock. **ADR-0116 has now closed the σ half of this row** — `StreamVolatility` is gated on the mark's provider timestamp at `FusionLifecycle.applyRiskCut`, using the `markTimeFor` the lifecycle already holds for the ADR-0071 seed, so no planner-target record had to change. The COVARIANCE half remains open: `FusionLifecycle` still feeds `StreamCovariance` (ADR-0089, which sizes the book for concentration) one synchronised snapshot per planning cycle whether or not each member printed, so a quiet name is carried forward and contributes a zero return, biasing its measured correlation toward zero. Deliberately NOT fixed by the same gate: a covariance is a statement about CONTEMPORANEOUS returns, so a name cannot simply be dropped from a joint snapshot without breaking the pairing that makes it a covariance at all — the carry-forward is the Epps bias already registered above, and its fix is a grid/estimator change (Hayashi-Yoshida, or a grid stepped on the fastest member's cadence) rather than a per-name print gate. Trigger: a materially sized name is planned while printing less often than the fusion interval, or the measured concentration haircut is observed swinging with which names happened to print | ADR-0116; ADR-0113; ADR-0089; `PrintClock`; `StreamCovariance.update`; `FusionLifecycle.sizingCovariance` |
| ADR-0116: `VolatilityRegime` is the last risk sensor still counting in cycles | The same defect ADR-0113 fixed in the forecast sensors and ADR-0116 in the risk cut's σ remains in `VolatilityRegime` (ADR-0051), which `StrategyLifecycle` feeds from the whole mark-cache snapshot once per 5 s cycle. A frozen tape is a run of exactly-zero relative-vol readings, which decays the EWMA baseline the ratio divides by — and because the baseline is deliberately frozen against UPWARD moves while ELEVATED (ADR-0051's anti-washout rule), any collapse is permanent for the process lifetime. Observed live at 13:31Z on 2026-07-28, after one closed session: `/api/market/regime` reporting `volRatio` of 4.6e21 and `regime: ELEVATED` on a calm tape whose breadth read CHOP. Not folded into ADR-0116 because it is a different feed point in a different module (`trading-core/algo-engine` cannot see `app.fusion.PrintClock`) and needs a second decision the σ gate does not — whether the market reading should be taken over the names that printed THIS cycle or over every name holding a full window, since the composition of that mean would otherwise lurch at every session boundary. Money impact is currently indirect: the regime scales new-entry size only on the legacy strategy path, which stands down while fusion is the sole order origin (ADR-0055 phase 5), so today it is a corrupt badge and a dormant sizing control rather than a live loss. Fix: gate the sensor's per-name windows on the provider clock and take the market reading across all full windows. Trigger: fusion routing is turned off or the strategy path resumes sizing, or the ELEVATED badge is used for any decision | ADR-0116; ADR-0113; ADR-0051; `VolatilityRegime.update`; `StrategyLifecycle.runOnce`; `MarketRegimeController` |
| ADR-0114: the covariance joint seed is still bucketed on the poll cadence, not on the names' own prints | ADR-0114 re-denominates the per-name sensor seed (`SensorWarmup.seedPrices`) into the step at which the live sensor actually consumes a name — `max(poll interval, that name's median inter-print gap)` — but `jointSeedSamples`, the ADR-0089 covariance seed, still lays its snapshot grid and measures its hole tolerance in poll intervals. On the same live measurement that motivated ADR-0114 (median inter-print gaps of 19.9 s on the Treasury curve, 90 s on NQ, 778 s on GBPUSD, 1,199 s on ES against a 30 s fusion cadence) that grid carries most names forward at most grid points, which is the Epps bias already registered above, and its lookback is too narrow to reach the needed snapshot count for any slow name — live it seeds 37 synchronised snapshots of 2 names and the covariance stays cold. Not folded into ADR-0114 because the derivation is genuinely different: a covariance is a statement about CONTEMPORANEOUS returns, so the grid must be common to all names and the right step is the FASTEST member's cadence (widening it to the slowest would throw away the fast names' information), whereas the per-name seed correctly uses each name's own. Fix: derive the joint grid step from the fastest member's measured print cadence, size the lookback in that step, and keep the carry-forward rule for the slower members. Trigger: the fusion covariance is observed still cold after ADR-0114 lands, or a materially sized name is planned while the concentration haircut is unavailable | ADR-0114; ADR-0089; ADR-0113; `SensorWarmup.jointSeedSamples`; `FusionLifecycle` covariance seed |
| Half-day early-close (13:00 ET) session times | The ADR-0115 session gate uses a flat 16:00 ET close, so on the ~2 NYSE half-days/year it admits opening trades 13:00–16:00 after the real close. Trigger: model the disclosed early-close dates in `UsTradingCalendar`. | ADR-0115; `UsTradingCalendar.isRegularSessionOpen` |
| Fusion executor stops EMITTING out-of-hours orders | ADR-0115 refuses out-of-hours opening at the pre-trade floor, but the fusion re-plan still emits them every ~30 s only to be rejected (log noise). Trigger: make the executor session-aware so it holds rather than emits. | ADR-0115; `FusionExecutor` re-plan |
| ADR-0118: a rated delta on a whole-unit name rounds toward zero, so small gaps trade nothing | `TargetPlanner.tradableQuantity` rounds toward zero (deliberately: rounding must only ever trade LESS than planned), and the ADR-0080 rated delta is `a x gap` with `a = 1 - e^(-interval/horizon)` = 0.008298755… at the shipped 30 s cadence against the 3600 s rung. So on any name quoted per unit — every equity and FX pair — a gap smaller than `1/a` ≈ 120 units produces `|delta| < 1` and rounds to NO ORDER, every cycle, with no residual carried. At AAPL's ~$337 that is a ~$40k gap before a single share trades, against a book whose whole gross is under $800. ADR-0118 frees the specific position that motivated it (the buffer, not the rounding, was suppressing that one — the delta there was exactly zero, so an accumulator would have carried nothing), but the rounding trap is a separate mechanism and is untouched: it will bind on ordinary rebalances the moment the edge gate reopens and the desk is trading toward targets again. Fix: carry the untraded remainder per name and add it to the next cycle's delta, so the realised trade rate matches the rated one in expectation while every order stays a whole unit; needs reset rules on a side change, a target change of sign, and a flat plan so stale intent cannot accumulate. Trigger: the edge gate reopens and the desk is observed holding a stable gap it never closes, or per-name turnover shows a name planned every cycle and filled in none | ADR-0118; ADR-0080; ADR-0090; ADR-0094; `TargetPlanner.tradableQuantity`; `TargetPlanner.orderDelta`; `jethro.fusion.adjustment-rate` |
| True change-vs-market attribution (A/B / shadow book) | ADR-0116 scores risk-adjusted PnL over a window, still confounded by market direction — a change held through a rally scores well regardless. Trigger: a counterfactual (shadow book on the pre-change code, or A/B) to attribute a change's effect cleanly. | ADR-0116; `evaluate_window` |
| Improve page shows an 'evaluating' state during the hold window | While a change accumulates evidence the loop holds (no new change), but `run-status` still labels the cycle 'changed'/'no-change'. Trigger: pass whether a NEW baseline was recorded this cycle so the heartbeat can read 'evaluating (n/K)'. | ADR-0116; `score-change.py cmd_status` |
